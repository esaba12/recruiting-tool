---
phase: 02-unified-attention-feed-today
plan: 2
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/components/TodayTab.jsx
autonomous: true
requirements: [ATTN-01, ATTN-02]

must_haves:
  truths:
    - "TodayTab.jsx renders all 9 attention sections (Overdue Follow-Ups, Stale Applications, High Urgency Contacts, Keep in Touch Due, Job Boards Needs-Review, Want to Schedule, OA-Due, OA-Needs-Check, Timeline Finds) in D-02's exact time-sensitivity order, combining what used to be 5 separate surfaces (per D-01, ATTN-01)."
    - "Every contact-shaped and application-shaped attention item deep-links to its full ContactDetailModal/ApplicationDetailModal record in one click, with the cross-tab 'Find people ->' relay intact via onFindPeople (per the corrected D-04, ATTN-02)."
    - "Timeline Finds items stay approve/dismiss-only per D-04b's deliberate, documented scope-narrowing of ATTN-02 for that one category, and every carried-forward inline action (mark followed up, mark scheduled, mark completed, triage chips, Met, Log) is preserved with no new action types invented, per D-05."
  artifacts:
    - "app/src/components/TodayTab.jsx (new file — default export TodayTab, plus local Section, RowCap, OverdueRow, ScheduleRow, HighUrgencyRow, KeepInTouchRow, ApplicationRow, OaRow, changeAppTriage, handleMet, hasNewerInteraction)"
  key_links:
    - "TodayTab.jsx's prop contract (contacts, apps, interactions, calls, relationships, onFindPeople, onRefresh, onRefreshRelationships, isDemoMode) -> Plan 02-03's App.jsx call sites, which must supply exactly this shape or the component silently loses a section's data or its Find-people relay."
    - "TimelineFindsPanel's onPendingChange prop (Plan 02-01) -> this plan's page-level all-sections-empty guard, the only way TodayTab can know Timeline Finds' live pending count without duplicating its internal state."
---

<objective>
Build `app/src/components/TodayTab.jsx` — the unified attention feed component that is the heart of this phase. It combines what used to be 5 fragmented surfaces (`ActionsTab.jsx`'s 6 categories, Keep in Touch's standalone view, Job Boards' Needs-Review bucket, Timeline Finds' buried Calendar panel) into 9 sections rendered in D-02's locked time-sensitivity order, with every item deep-linking to its full record (per the corrected D-04) and every existing inline action carried forward unchanged (per D-05).

This plan builds the file as a self-contained, fully-working component across 3 tasks, but does NOT wire it into `App.jsx`'s nav/routing yet — it is created but unreachable until Plan 02-03's cutover lands, exactly like `01-02-PLAN.md`'s `Mono.jsx` was built and verified in isolation one wave before its rollout consumers.

Purpose: this is the single artifact that makes ATTN-01/ATTN-02 true — every later plan (the nav cutover, the old-surface retirement) depends on this file existing and working correctly first.
Output: `app/src/components/TodayTab.jsx`, complete and internally self-testable (via `esbuild` syntax checks), ready for Plan 02-03 to import.
</objective>

## Artifacts This Phase Produces

New files:
- `app/src/lib/attention.js` — attention-derivation module (`overdueFollowUps`, `staleApplications`, `highUrgencyContacts`, `wantToSchedule`, `oaDue`, `oaNeedsCheck`, `needsReviewApps`, `activeApps`, `keepInTouchDue` re-export) (Plan 02-01)
- `app/src/components/TodayTab.jsx` — the unified attention feed component (this plan): default export `TodayTab`, plus local `Section`, `RowCap`, `OverdueRow`, `ScheduleRow`, `HighUrgencyRow`, `KeepInTouchRow`, `ApplicationRow`, `OaRow`

Modified files:
- `app/src/components/TimelineFindsPanel.jsx` — Section-style chrome refactor, new `onPendingChange` prop (Plan 02-01)
- `app/src/lib/icons.js` — `NAV_ICON.today` added (Plan 02-01), `NAV_ICON.actions` removed (Plan 02-03)
- `app/src/components/layout/Sidebar.jsx` — `NAV_ITEMS` gains `today` (first), loses `actions` (Plan 02-03)
- `app/src/App.jsx` — `AppInner`/`DemoApp` render `TodayTab` instead of `ActionsTab`; `NETWORK_VIEWS` drops `keepintouch`; `DEMO_NAV_ITEMS` swaps `actions` for `today` (Plan 02-03)
- `app/src/components/OverviewTab.jsx` — nudge sections removed (Plan 02-03)
- `app/src/components/CalendarTab.jsx` — `TimelineFindsPanel` mount removed (Plan 02-03)

Deleted files:
- `app/src/components/ActionsTab.jsx` (Plan 02-03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unified-attention-feed-today/02-CONTEXT.md
@.planning/phases/02-unified-attention-feed-today/02-RESEARCH.md
@.planning/phases/02-unified-attention-feed-today/02-PATTERNS.md
@.planning/phases/02-unified-attention-feed-today/02-UI-SPEC.md
@app/src/lib/attention.js
@app/src/components/TimelineFindsPanel.jsx
@app/src/components/ActionsTab.jsx
@app/src/components/KeepInTouchTab.jsx
@app/src/components/PipelineTab.jsx
@app/src/components/ApplicationDetailModal.jsx
@app/src/components/jobBoards/helpers.js
@app/src/components/jobBoards/JobCard.jsx
@app/src/lib/keepInTouch.js
@app/src/lib/affinity.js
@app/src/components/ui/Mono.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TodayTab scaffold — Section wrapper, attention-derivation wiring, Contact-row sections (Overdue Follow-Ups, High Urgency Contacts, Want to Schedule), Keep-in-Touch section</name>
  <files>app/src/components/TodayTab.jsx</files>
  <read_first>
    app/src/components/ActionsTab.jsx (full — `Section` lines 113-123, `hasNewerInteraction` lines 129-131, `OverdueContactRow` lines 133-183, `ScheduleQueueRow` lines 185-231, `highUrgencyContacts`'s original `ActionRow` usage lines 95-107); app/src/components/KeepInTouchTab.jsx (full — card markup lines 40-87, `TIE_LABEL` const line 8); app/src/lib/attention.js (Plan 02-01's exact export names); .planning/phases/02-unified-attention-feed-today/02-PATTERNS.md §"app/src/components/TodayTab.jsx (new component)" (Imports pattern, Section wrapper, Type 1 + Type 2 row patterns); .planning/phases/02-unified-attention-feed-today/02-UI-SPEC.md §"Feed Composition Contract" Type 1 + Type 2 row specs, §"Color" accent-tier table + icon table, §"Typography" Mono-usage table.
  </read_first>
  <action>
    Create `app/src/components/TodayTab.jsx`. Import `useState` from `react`; `STATUS_COLOR, URGENCY_COLOR, TYPE_COLOR, fmt, daysUntil, Badge, EmptyState` from `../shared.jsx`; `updateContact, addInteraction` from `../db.js`; `logMetWithContact` from `../lib/quickLog.js`; `overdueFollowUps, highUrgencyContacts, wantToSchedule, keepInTouchDue` from `../lib/attention.js`; `lastPointOfContact` from `../lib/keepInTouch.js`; `tieStrengthBucket` from `../lib/affinity.js`; `statusIconFor` from `../lib/icons.js`; `DraftPanel` from `./DraftPanel.jsx`; `ContactDetailModal` from `./ContactDetailModal.jsx`; `LogInteractionModal` from `./LogInteractionModal.jsx`; `MetButton` from `./MetButton.jsx`; `Mono` from `./ui/Mono.jsx`; the lucide-react icons `CalendarClock, AlertTriangle, HeartHandshake, UserPlus`.

    Build a local, unexported `Section({ title, subtitle, accent, icon: Icon, children })` component matching `ActionsTab.jsx`'s `Section` (lines 113-123) exactly in structure, re-keyed to the 4 locked families per UI-SPEC's Color table: a `border` lookup mapping `danger`→`border-danger-200`, `warning`→`border-warning-200`, `ink`→`border-ink-200`, `accent`→`border-accent-200` (default `border-ink-200`); a `heading` lookup mapping the same 4 keys to `text-danger-700`/`text-warning-700`/`text-ink-700`/`text-accent-700` (default `text-ink-700`). The outer element is `<div className="bg-white rounded-xl p-5 shadow-sm border ${border}">`; the `<h2 className="text-sm font-semibold ${heading} mb-1 flex items-center gap-1.5">` renders `{Icon && <Icon size={16} strokeWidth={2} />}` before the `title` text; an optional `subtitle` renders as `<p className="text-xs text-ink-400 mb-3">`; `children` render inside a wrapping element (this task's sections pass their rows directly — Task 2 introduces the shared `RowCap` wrapper, do not build row-capping in this task).

    Build a local `hasNewerInteraction(contact, interactions)` helper, ported verbatim from `ActionsTab.jsx:129-131`.

    Build 3 row components sharing the Contact-Row shape (UI-SPEC's Type 1), each taking an `onOpen(contact)` callback prop that the caller uses to open its own `ContactDetailModal`:
    - `OverdueRow({ contact: c, interactions, onRefresh, onOpen })` — port `OverdueContactRow` (`ActionsTab.jsx:133-183`) verbatim, with two changes: (1) the name/company block's `onClick` becomes `() => onOpen(c)` instead of toggling `expanded` — the existing "Draft follow-up"/"Details" text button (currently the inner button at `ActionsTab.jsx:162-164`) keeps its own separate `onClick={() => setExpanded(e => !e)}` unchanged, now the sole way to reach the inline `DraftPanel`; (2) Mono-wrap the overdue meta line's date and day-count individually per the Typography table: the text becomes `Was due `, then `<Mono>{fmt(c.followUpDate)}</Mono>`, then ` (`, then `<Mono className="text-danger-600 font-medium">{Math.abs(daysUntil(c.followUpDate))}d</Mono>`, then ` ago)` — replacing the plain-text version at `ActionsTab.jsx:160`.
    - `ScheduleRow({ contact: c, onRefresh, onOpen })` — port `ScheduleQueueRow` (`ActionsTab.jsx:185-231`) verbatim with the same two kinds of changes: name/company block's `onClick` becomes `() => onOpen(c)`; the existing "Draft outreach"/"Hide" toggle button keeps its own separate click handler unchanged; the schedule-by meta line's date and day-count get individually Mono-wrapped the same way as `OverdueRow`'s.
    - `HighUrgencyRow({ contact: c, onOpen })` — new row (the source `ActionsTab.jsx` High Urgency section used the fully inert generic `ActionRow`, which had no click behavior and no expand affordance at all — do not add a `DraftPanel`/expand toggle here; none existed before, and per D-05 no new action types are introduced). Left block, wrapped in a `<div className="min-w-0 cursor-pointer" onClick={() => onOpen(c)}>`: name (`text-sm font-medium text-ink-900`), `{[c.company, c.role].filter(Boolean).join(' · ')}` (`text-xs text-ink-500`), and — if `c.email` is set — a `mailto:` link (`text-xs text-accent-500 hover:underline`, `onClick={e => e.stopPropagation()}`). Right block (`text-right shrink-0 space-y-1`): `<Badge label="HIGH" color={URGENCY_COLOR.HIGH} />`, then a meta line in `text-xs font-medium text-ink-500` reading (when `c.followUpDate` is set) `Due <Mono>{fmt(c.followUpDate)}</Mono>` or otherwise the plain text `No follow-up date set` — this ports `ActionsTab.jsx:95-107`'s exact data fields into a dedicated clickable row instead of the generic inert `ActionRow`.

    Build `KeepInTouchRow({ contact: c, interactions, onOpen, onLog, onMet })`: port the per-contact card body from `KeepInTouchTab.jsx:40-87` verbatim — the `<div key={c.id} className="bg-white rounded-xl px-4 py-3 shadow-sm border ...">` block, its internal `onEdit`/`onLog`/`onMet` call sites remapped to this component's `onOpen`/`onLog`/`onMet` props — with exactly one visual change: the conditional border class changes from `overdue ? 'border-accent-200' : 'border-ink-100'` to `overdue ? 'border-warning-300' : 'border-ink-100'` (the overdue-highlight-via-border *behavior* is preserved; only its color family moves from `accent` to `warning`, per UI-SPEC's tier assignment for Keep in Touch Due). Define a local `const TIE_LABEL = { strong: 'Close tie', moderate: 'Moderate tie', weak: 'Weak tie', cold: 'Not yet connected' }`, matching `KeepInTouchTab.jsx:8` exactly (it is a private const there, not importable). Mono-wrap per the Typography table: `<Mono>{status.overdueDays}d</Mono> overdue`, `every <Mono>{status.cadence}d</Mono>`, and `<Mono>{fmt(last.date)}</Mono>` for the last-point-of-contact date; `Due today` and `Never contacted` stay plain text (no numeral present).

    Add a local `async function handleMet(contact)` at the top level of the file's default-exported component (see below): `await logMetWithContact(contact); onRefresh?.()` — matching `App.jsx`'s `NetworkTab.handleMet` exactly.

    Scaffold the default-exported function: `export default function TodayTab({ contacts, apps, interactions = [], calls = [], relationships = [], onFindPeople, onRefresh, onRefreshRelationships, isDemoMode = false })`. Inside it compute `const overdueContacts = overdueFollowUps(contacts)`, `const highUrgency = highUrgencyContacts(contacts)`, `const scheduleContacts = wantToSchedule(contacts)`, `const keepInTouch = keepInTouchDue(contacts, interactions)`. Add state: `const [selectedContactId, setSelectedContactId] = useState(null)` and `const [logContact, setLogContact] = useState(null)`. Return a `<div className="space-y-4">` wrapper containing 4 `.length > 0 &&`-guarded `Section`s covering this task's categories — Overdue Follow-Ups (`accent="danger"`, `icon={CalendarClock}`, `title={`Overdue Follow-Ups (${overdueContacts.length})`}`, rows via `OverdueRow`), Keep in Touch Due (`accent="warning"`, `icon={HeartHandshake}`, `title={`Keep in Touch Due (${keepInTouch.length})`}`, rows via `KeepInTouchRow` with `onOpen={c => setSelectedContactId(c.id)}`, `onLog={c => setLogContact(c)}`, `onMet={handleMet}`), High Urgency Contacts (`accent="danger"`, `icon={AlertTriangle}`, `title={`High Urgency Contacts (${highUrgency.length})`}`, rows via `HighUrgencyRow`), Want to Schedule (`accent="ink"`, `icon={UserPlus}`, `title={`Want to Schedule (${scheduleContacts.length})`}`, rows via `ScheduleRow`) — render them in this order for now; Task 3 reassembles the full 9-section page in D-02's final order, so exact ordering here does not matter yet. Every row's `onOpen` prop should call `setSelectedContactId(x.id)`. At the bottom of the returned JSX, mount `{selectedContactId && <ContactDetailModal contact={contacts.find(c => c.id === selectedContactId)} contacts={contacts} interactions={interactions} contactRelationships={relationships} onClose={() => setSelectedContactId(null)} onSaved={() => { setSelectedContactId(null); onRefresh?.() }} onRefreshRelationships={onRefreshRelationships} />}` and `{logContact && <LogInteractionModal contacts={contacts} contact={logContact} onClose={() => setLogContact(null)} onSaved={() => { setLogContact(null); onRefresh?.() }} />}`.
  </action>
  <acceptance_criteria>
    - `app/src/components/TodayTab.jsx` exists, default-exports a `TodayTab` function accepting `{ contacts, apps, interactions, calls, relationships, onFindPeople, onRefresh, onRefreshRelationships, isDemoMode }`.
    - `Section`, `OverdueRow`, `ScheduleRow`, `HighUrgencyRow`, `KeepInTouchRow`, `hasNewerInteraction`, `handleMet` are all present as named identifiers.
    - `overdueFollowUps, highUrgencyContacts, wantToSchedule, keepInTouchDue` are imported from `../lib/attention.js` (not reimplemented locally).
    - `border-warning-300` appears (KeepInTouchRow's re-tiered overdue border).
    - `npx esbuild` parses the file with zero syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd app && F=src/components/TodayTab.jsx; [ -f "$F" ] || { echo "FAIL: file missing"; exit 1; }; for sym in "function Section" "function OverdueRow" "function ScheduleRow" "function HighUrgencyRow" "function KeepInTouchRow" "function hasNewerInteraction" "export default function TodayTab"; do grep -q "$sym" "$F" || { echo "FAIL: missing $sym"; exit 1; }; done; grep -qF "from '"'"'../lib/attention.js'"'"'" "$F" || { echo "FAIL: attention.js not imported"; exit 1; }; grep -qF "border-warning-300" "$F" || { echo "FAIL: KeepInTouch re-tier missing"; exit 1; }; npx esbuild "$F" --bundle=false --format=esm --outfile=/dev/null && echo "TASK1 TodayTab.jsx VERIFIED"'</automated>
  </verify>
  <done>TodayTab.jsx exists with a working scaffold rendering Overdue Follow-Ups, High Urgency Contacts, Want to Schedule, and Keep in Touch Due sections, each wired to open ContactDetailModal on row click while preserving every existing inline action (mark followed up, draft follow-up/outreach expand, mark scheduled, Met, Log).</done>
</task>

<task type="auto">
  <name>Task 2: Application-row and OA-row sections (Stale Applications, Job Boards Needs-Review, OA-Due, OA-Needs-Check) + shared row-cap helper</name>
  <files>app/src/components/TodayTab.jsx</files>
  <read_first>
    app/src/components/ActionsTab.jsx (`OaRow` lines 236-277, `ActionRow` lines 279-293 — visual shape only, not its lack of click behavior); app/src/components/PipelineTab.jsx (the clickable-row `onClick` pattern lines 163-206, the `stopPropagation` pattern on the JD link at line 190); app/src/components/jobBoards/JobCard.jsx (the `BUCKET_TAG` chip usage lines 78-91, the `toggleStatus` `stopPropagation` pattern lines 26-29); app/src/components/jobBoards/helpers.js (`BUCKET_CONFIG`, `BUCKET_TAG`, `BUCKET_TO_TRIAGE` exact values); app/src/db.js — grep it first to confirm the exact exported names `updateApplicationTriage`/`archiveApplication`/`updateApplication` before importing them; .planning/phases/02-unified-attention-feed-today/02-PATTERNS.md §"Type 3 — Application Row" and §"Type 4 — OA Row"; .planning/phases/02-unified-attention-feed-today/02-UI-SPEC.md §"Feed Composition Contract" Type 3/Type 4 specs and §"Per-section row cap ('Show N more')".
  </read_first>
  <action>
    Extend `app/src/components/TodayTab.jsx` (created by Task 1 — add to the same file; do not create a second file). Add imports: `staleApplications, needsReviewApps, oaDue, oaNeedsCheck` from `../lib/attention.js`; `updateApplicationTriage, archiveApplication, updateApplication` from `../db.js`; `BUCKET_CONFIG, BUCKET_TAG, BUCKET_TO_TRIAGE` from `./jobBoards/helpers.js`; extend the existing `../shared.jsx` import (from Task 1) to also pull in `STAGE_COLOR, daysSince` rather than adding a second `../shared.jsx` import statement; `ApplicationDetailModal` from `./ApplicationDetailModal.jsx`; the lucide-react icons `Hourglass, Inbox, ClipboardCheck, Search`.

    Build a local `RowCap({ items, cap = 5, tier, renderItem })` component: `const [expanded, setExpanded] = useState(false)`; render `<div className="divide-y divide-ink-100">{(expanded ? items : items.slice(0, cap)).map(renderItem)}</div>`; when `items.length > cap`, render below it a text control — `+{items.length - cap} more — Show all` while collapsed, `Show fewer` while expanded — `onClick={() => setExpanded(e => !e)}`, styled `className="text-xs font-medium hover:underline pt-2 text-center w-full"` with its text color set from the same tier-key → text-color lookup already defined for `Section`'s `heading` (reuse that exact 4-entry object rather than defining a second copy — hoist it to a small module-level `const HEADING_COLOR = { danger: 'text-danger-700', warning: 'text-warning-700', ink: 'text-ink-700', accent: 'text-accent-700' }` that both `Section` and `RowCap` read from). This is new interaction behavior with no direct interactive precedent in this codebase — `CalendarTab.jsx`'s `FEED_LATER_CAP` hint is static text only — so build it as this one self-contained, reusable component; every array-backed section (from both Task 1 and this task) will be retrofitted to use it in Task 3, not in this task.

    Build `ApplicationRow({ app: a, showTriageChips, onOpen, onRefresh })` (UI-SPEC's Type 3 — used by both Stale Applications and Job Boards Needs-Review): visual shape ported from `ActionsTab.jsx`'s inert `ActionRow` (lines 279-293) — a `py-2.5` flex row, left block (`{a.company}` in `text-sm font-medium text-ink-900`, `{a.role || ''}` in `text-xs text-ink-500`, and if `a.jdLink` a `View JD ↗` link with `target="_blank"` and `onClick={e => e.stopPropagation()}`), right block (`<Badge label={a.stage} color={STAGE_COLOR[a.stage]} />` then a Mono-wrapped meta line: `<Mono>{a.daysInStage ?? daysSince(a.lastActivity)}d</Mono> in {a.stage}`, matching `PipelineTab.jsx:186`'s exact Mono-wrap scope). Wrap the whole row in `<div onClick={() => onOpen(a)} className="cursor-pointer">`, borrowing PipelineTab.jsx's click MECHANIC (not its bordered-card visual style — `ApplicationRow` keeps `ActionsTab.jsx`'s plain `py-2.5` row shape, matching Type 1/Type 2's "shape family" per UI-SPEC, not `PipelineTab.jsx`'s individually-bordered card style). When `showTriageChips` is true (the Job Boards Needs-Review variant only), render below the meta line a flex-wrap row of chips built from `BUCKET_CONFIG.filter(b => !['all', 'review'].includes(b.key))` (i.e. `applying`/`maybe`/`applied`/`pass`), each styled with `BUCKET_TAG[b.key]` matching `JobCard.jsx:83`'s exact chip class shape (`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-medium ${BUCKET_TAG[b.key]}`), `onClick={e => { e.stopPropagation(); changeAppTriage(a, b.key) }}`.

    Build `OaRow({ app: a, needsCheck, onOpen, onRefresh })` (Type 4): port `ActionsTab.jsx:236-277`'s `OaRow` verbatim (layout, the local `markCompleted` function + `marking` state, the `needsCheck` branch), adding the same row-click-to-modal wiring as `ApplicationRow` (`onClick={() => onOpen(a)}` on the outer row `<div>`), with `e.stopPropagation()` added to both the existing "Open assessment ↗" link's `onClick` and the "✓ Mark completed" button's `onClick` (neither currently stops propagation, since the row wasn't clickable before this phase). Mono-wrap the due-countdown date/day-count per the Typography table, matching `ActionsTab.jsx:266-269`'s two text branches exactly; the `needsCheck` branch's "No stated deadline found — check manually" text stays plain (no Mono — no numeral present).

    Add a shared `async function changeAppTriage(app, bucketKey)` at the top level of the `TodayTab` component (used both by the Needs-Review chips above and, in Task 3, by `ApplicationDetailModal`'s `onStatusChange`): `await updateApplicationTriage(app.id, BUCKET_TO_TRIAGE[bucketKey === null ? 'review' : bucketKey], app.stage); onRefresh?.()` — matching `PipelineTab.jsx`'s `changeTriage` exactly.

    Inside `TodayTab`, compute `const staleApps = staleApplications(apps)`, `const needsReview = needsReviewApps(apps)`, `const oaDueList = oaDue(apps)`, `const oaNeedsCheckList = oaNeedsCheck(apps)`. Add `const [selectedAppId, setSelectedAppId] = useState(null)`. Extend the returned JSX (still not in final D-02 order — Task 3 handles final ordering) with 4 more `.length > 0 &&`-guarded `Section`s: Stale Applications (`accent="danger"`, `icon={Hourglass}`, `title={`Stale Applications (${staleApps.length})`}`, `subtitle="No movement in 14+ days — follow up or update the stage."`, rows via `ApplicationRow` with `showTriageChips={false}`), Job Boards Needs-Review (`accent="warning"`, `icon={Inbox}`, `title={`Job Boards Needs-Review (${needsReview.length})`}`, rows via `ApplicationRow` with `showTriageChips`), OA-Due (`accent="warning"`, `icon={ClipboardCheck}`, `title={`OA-Due (${oaDueList.length})`}`, rows via `OaRow`), OA-Needs-Check (`accent="ink"`, `icon={Search}`, `title={`OA-Needs-Check (${oaNeedsCheckList.length})`}`, rows via `OaRow needsCheck`). Every row's `onOpen` prop should call `setSelectedAppId(x.id)`. Use plain `.map()` for these 4 sections' rows in this task — do not wire `RowCap` in yet, Task 3 does the final row-cap retrofit across all 8 array-backed sections at once. Do not mount `ApplicationDetailModal` in this task — Task 3 mounts it once `onFindPeople` threading and final section assembly are settled, avoiding two separate diffs touching the same modal-mount block.
  </action>
  <acceptance_criteria>
    - `ApplicationRow`, `OaRow`, `RowCap`, `changeAppTriage` are present as named identifiers in `app/src/components/TodayTab.jsx`.
    - `staleApplications, needsReviewApps, oaDue, oaNeedsCheck` are imported from `../lib/attention.js`.
    - `BUCKET_TAG` and `BUCKET_TO_TRIAGE` are imported from `./jobBoards/helpers.js`.
    - `e.stopPropagation()` (or `stopPropagation()`) appears at least 3 times in the file (JD link, triage chips, OA link/mark-completed).
    - `npx esbuild` parses the file with zero syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd app && F=src/components/TodayTab.jsx; for sym in "function ApplicationRow" "function OaRow" "function RowCap" "changeAppTriage"; do grep -q "$sym" "$F" || { echo "FAIL: missing $sym"; exit 1; }; done; grep -qF "from '"'"'./jobBoards/helpers.js'"'"'" "$F" || { echo "FAIL: helpers.js not imported"; exit 1; }; C=$(grep -c "stopPropagation" "$F"); [ "$C" -ge 3 ] || { echo "FAIL: expected >=3 stopPropagation call sites, found $C"; exit 1; }; npx esbuild "$F" --bundle=false --format=esm --outfile=/dev/null && echo "TASK2 TodayTab.jsx VERIFIED"'</automated>
  </verify>
  <done>TodayTab.jsx now also renders Stale Applications, Job Boards Needs-Review (with inline triage chips), OA-Due, and OA-Needs-Check sections, each opening ApplicationDetailModal on row click via a shared onOpen wiring point, with every nested interactive element correctly stopping propagation.</done>
</task>

<task type="auto">
  <name>Task 3: Final assembly — D-02 section order, ApplicationDetailModal mount with onFindPeople relay, Timeline Finds section, row-cap retrofit, demo-mode omission, page-level empty state</name>
  <files>app/src/components/TodayTab.jsx</files>
  <read_first>
    app/src/components/TimelineFindsPanel.jsx (Plan 02-01's post-refactor shape — the `onPendingChange` prop, the unchanged `apps/calls/interactions/contacts/onEventCreated` prop contract); app/src/components/PipelineTab.jsx lines 208-223 (the exact `ApplicationDetailModal` prop contract, including `onFindPeople`); .planning/phases/02-unified-attention-feed-today/02-CONTEXT.md D-02 (section order) and D-04/D-04b (the corrected deep-link target and Timeline Finds' deliberate scope-narrowing); .planning/phases/02-unified-attention-feed-today/02-UI-SPEC.md §"Color" tier table (the final section order with tier + icon per section), §"Navigation" `/demo` nav paragraph (Timeline Finds' demo-mode omission), §"Copywriting Contract" (the page-level and Timeline-Finds-section empty-state text), §"Feed Composition Contract" page-level structure paragraph.
  </read_first>
  <action>
    Extend `app/src/components/TodayTab.jsx`. Import `TimelineFindsPanel` from `./TimelineFindsPanel.jsx`.

    Reorder the JSX return so the 8 array-backed sections render in exactly this sequence — D-02's canonical order: 1. Overdue Follow-Ups, 2. Stale Applications, 3. High Urgency Contacts, 4. Keep in Touch Due, 5. Job Boards Needs-Review, 6. Want to Schedule, 7. OA-Due, 8. OA-Needs-Check — followed by section 9, Timeline Finds, always last. Do not re-sort by tier-color grouping — UI-SPEC is explicit that the danger/warning/ink/accent tiers intentionally repeat non-adjacently in this order (e.g. OA-Due at position 7 is `warning`, sandwiched between two `ink`-tier sections) and that D-02's order takes strict priority over color-clustering.

    Retrofit every one of the 8 array-backed sections to render its rows through the `RowCap` component built in Task 2 (`cap={5}`, `tier` set to that section's own `accent` key, `renderItem` returning the appropriate row component call with a `key` prop) instead of a bare `.map()` — replace each inline `.map()` call from Task 1/Task 2 with a `<RowCap items={...} tier="..." renderItem={x => <RowComponent key={x.id} .../>} />` placed inside that section's children.

    Add `const [selectedAppId, setSelectedAppId] = useState(null)` if it is not already present from Task 2. Mount `ApplicationDetailModal` once, at the bottom of the returned JSX alongside the `ContactDetailModal`/`LogInteractionModal` mounts from Task 1: `{selectedAppId && <ApplicationDetailModal app={apps.find(a => a.id === selectedAppId)} contacts={contacts} apps={apps} interactions={interactions} relationships={relationships} onStatusChange={s => changeAppTriage(apps.find(a => a.id === selectedAppId), s)} onClose={() => setSelectedAppId(null)} onDelete={async () => { const app = apps.find(a => a.id === selectedAppId); await archiveApplication(app.id); setSelectedAppId(null); onRefresh?.() }} onSaved={() => onRefresh?.()} onFindPeople={onFindPeople} onRefresh={onRefresh} onRefreshRelationships={onRefreshRelationships} />}`. Including `onFindPeople={onFindPeople}` here is mandatory (`onFindPeople` is a prop `TodayTab` itself receives from its caller) — omitting it silently drops the "Find people →" cross-tab relay per the carried-forward STATE.md blocker and the corrected D-04.

    Add Timeline Finds. At the top of the component, add `const [timelineFindsCount, setTimelineFindsCount] = useState(0)`. Render, as the 9th and final section, `{!isDemoMode && <TimelineFindsPanel apps={apps} calls={calls} interactions={interactions} contacts={contacts} onPendingChange={setTimelineFindsCount} />}`. This is deliberately NOT wrapped in `TodayTab`'s own `Section` component — `TimelineFindsPanel` now renders its own matching Section-style chrome internally (per Plan 02-01's refactor) — and is deliberately NOT gated by a `.length > 0` check: it always renders when not in demo mode, showing its own in-section idle/scanning empty state. This is the one deliberate exception to the "hide when empty" rule the other 8 sections follow, preserving Timeline Finds' pre-existing always-visible-with-its-own-empty-state behavior per UI-SPEC's Copywriting Contract ("Unchanged" for that in-section empty text) and D-04b (approve/dismiss-only, no full-record deep-link for this one category).

    Compute the page-level empty-state guard, near the top of the component's return: `const allEmpty = overdueContacts.length === 0 && staleApps.length === 0 && highUrgency.length === 0 && keepInTouch.length === 0 && needsReview.length === 0 && scheduleContacts.length === 0 && oaDueList.length === 0 && oaNeedsCheckList.length === 0 && (isDemoMode || timelineFindsCount === 0)`. Before the `<div className="space-y-4">` wrapper, add `if (allEmpty) return <EmptyState msg="✓ Nothing needs your attention. You're on top of it." />` — matching `ActionsTab.jsx:46-48`'s existing all-empty guard pattern, reworded per UI-SPEC's Copywriting Contract since Today's scope is broader than "overdue" alone.
  </action>
  <acceptance_criteria>
    - All 9 sections appear in `app/src/components/TodayTab.jsx`'s JSX in D-02's exact order (verified by ascending line-number order of each section's title string).
    - `TimelineFindsPanel` is imported and mounted with `onPendingChange={setTimelineFindsCount}`, guarded by `!isDemoMode`.
    - `ApplicationDetailModal` is mounted exactly once, with `onFindPeople={onFindPeople}` present verbatim.
    - The exact string `Nothing needs your attention` appears as the page-level empty-state message.
    - `RowCap` is used by all 8 array-backed sections (at least 8 `<RowCap` occurrences).
    - `npx esbuild` parses the file with zero syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd app && F=src/components/TodayTab.jsx; grep -qF "onFindPeople={onFindPeople}" "$F" || { echo "FAIL: onFindPeople relay missing"; exit 1; }; grep -qF "onPendingChange={setTimelineFindsCount}" "$F" || { echo "FAIL: TimelineFindsPanel onPendingChange wiring missing"; exit 1; }; grep -qF "!isDemoMode && <TimelineFindsPanel" "$F" || { echo "FAIL: demo-mode Timeline Finds guard missing"; exit 1; }; grep -qF "Nothing needs your attention" "$F" || { echo "FAIL: page-level empty copy missing"; exit 1; }; C=$(grep -c "<RowCap" "$F"); [ "$C" -ge 8 ] || { echo "FAIL: expected >=8 RowCap usages, found $C"; exit 1; }; ORDER=$(grep -n "Overdue Follow-Ups (\|Stale Applications (\|High Urgency Contacts (\|Keep in Touch Due (\|Job Boards Needs-Review (\|Want to Schedule (\|OA-Due (\|OA-Needs-Check (" "$F" | cut -d: -f1); SORTED=$(echo "$ORDER" | sort -n); [ "$ORDER" = "$SORTED" ] || { echo "FAIL: sections not in ascending D-02 order"; exit 1; }; npx esbuild "$F" --bundle=false --format=esm --outfile=/dev/null && echo "TASK3 TodayTab.jsx VERIFIED"'</automated>
  </verify>
  <done>TodayTab.jsx is complete: all 9 sections render in D-02's locked order with tier-correct coloring/icons, every contact/application row deep-links to its full record modal with a working Find-people relay, Job Boards Needs-Review carries inline triage chips, every array-backed section respects a 5-row cap with Show more/fewer, Timeline Finds is present and demo-safe, and the page shows one unified empty state only when literally everything (including Timeline Finds) is empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (React) → already-fetched Supabase data | TodayTab.jsx renders `contacts`/`apps`/`interactions`/`calls` props already fetched via the existing RLS-scoped `fetch*` calls in App.jsx — no new query, no new data path. |
| Client → existing per-record modals | ContactDetailModal/ApplicationDetailModal are reused exactly as-is, unmodified by this plan; both already write through db.js's RLS-scoped update/insert calls. |
| Client → TimelineFindsPanel's AI scan | This plan mounts TimelineFindsPanel (unmodified core logic, Plan 02-01 only changed its chrome) — the existing `requireUser()`-gated `/claude-api`/`/openai-api` proxy call is unchanged. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched — TodayTab reuses the existing authenticated session already established by App.jsx |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched — RLS scoping happens at the existing `fetch*`/proxy layer, unchanged |
| V5 Input Validation | No | No new input surface; every mutation (mark followed up, mark scheduled, mark completed, triage change) reuses existing `db.js` functions with their existing validation |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-03 | Information Disclosure | TodayTab.jsx + reused ContactDetailModal/ApplicationDetailModal | low | accept | Renders only props already passed down from App.jsx's RLS-scoped fetch* results — same trust boundary as every other existing tab; no new query surface introduced. |
| T-02-04 | Tampering (functional integrity, not a classic exploit) | ApplicationDetailModal mount inside TodayTab | medium | mitigate | onFindPeople must be threaded from the caller through to ApplicationDetailModal or the cross-tab "Find people →" relay silently breaks (fail-soft, no crash, no data exposure — a UX regression, not a security bypass); this plan's Task 3 acceptance criteria explicitly grep-verify onFindPeople is wired. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new package installs this plan — 100% reuse of already-installed lucide-react/react/existing app modules; Package Legitimacy Gate not applicable. |

</threat_model>

<verification>
Run all 3 tasks' `<verify><automated>` commands in order after each task's edits land (they build on the same file incrementally). `npx esbuild` must pass with zero syntax errors after every task. `TodayTab.jsx` is not yet imported anywhere in the app after this plan — Plan 02-03's `npm run build` is the first point the whole file becomes reachable from the entry point for a true end-to-end compile check.
</verification>

<success_criteria>
- `app/src/components/TodayTab.jsx` exists, is syntactically valid, and renders all 9 attention sections in D-02's locked order.
- Every contact/application row opens its correct full-record modal in one click; the Find-people cross-tab relay is wired.
- Every carried-forward inline action (mark followed up, mark scheduled, mark completed, triage chips, Met, Log, Timeline Finds' approve/dismiss) works identically to its source surface.
- Demo-mode correctly omits the Timeline Finds section via the `isDemoMode` prop.
</success_criteria>

<output>
Create `.planning/phases/02-unified-attention-feed-today/02-02-SUMMARY.md` when done
</output>
