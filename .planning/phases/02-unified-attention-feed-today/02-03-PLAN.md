---
phase: 02-unified-attention-feed-today
plan: 3
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/src/components/layout/Sidebar.jsx
  - app/src/lib/icons.js
  - app/src/App.jsx
  - app/src/components/ActionsTab.jsx
  - app/src/components/OverviewTab.jsx
  - app/src/components/CalendarTab.jsx
autonomous: true
requirements: [ATTN-01, ATTN-03]

must_haves:
  truths:
    - "Today is reachable as the first item in the persistent nav, and clicking it renders TodayTab with live data in both the authenticated app and /demo, per D-03 and D-03b."
    - "The standalone Actions tab, Overview's nudge section, and Keep in Touch's standalone Network view no longer exist anywhere in the codebase — not just App.jsx's own render branch — per ATTN-03, verified by a repo-wide zero-hit grep."
    - "TimelineFindsPanel no longer mounts inside CalendarTab.jsx — its only remaining mount point is TodayTab.jsx, per ATTN-03's 'TimelineFindsPanel's standalone presentation removed' requirement."
  artifacts: []
  key_links:
    - "Task 1's Sidebar.jsx nav-list edit and Task 2's App.jsx render-branch edit land together in this one plan (sequential tasks, same commit trajectory) — reachability and renderability of 'today' change atomically, so no wave boundary in this phase ever has 'today' in the nav with nothing to render, or 'actions' still rendering with no way left to reach it."
    - "Task 2's App.jsx import removal -> Task 3's ActionsTab.jsx deletion — the deletion must happen after the import is gone, or the build breaks; this plan's task ordering guarantees that sequence within one plan."
---

<objective>
Perform the phase's single atomic cutover: swap `App.jsx`'s and `Sidebar.jsx`'s nav/routing from `Actions`/`ActionsTab.jsx` to `Today`/`TodayTab.jsx` (per D-03, D-03b), remove `NetworkTab`'s Keep-in-Touch standalone view, then retire every surface `TodayTab.jsx` (Plan 02-02) now fully replaces — `ActionsTab.jsx` (deleted outright), `OverviewTab.jsx`'s nudge sections, and `CalendarTab.jsx`'s `TimelineFindsPanel` mount — per ATTN-03's explicit "removed... not left running in parallel as a 9th destination."

This plan is deliberately sequenced as its own wave, one full wave after `TodayTab.jsx` (Plan 02-02) is complete and self-verified — the orchestrator's explicit instruction for this phase is that no wave boundary may ever leave a capability temporarily unavailable, and this plan is where that constraint bites hardest: `Today` becoming reachable and `Actions`/the old nudges/the old Timeline Finds mount disappearing must happen together, not staggered across separate commits where one lands before the other.

Purpose: this is the plan that actually makes ATTN-03 true (the old surfaces are gone, not duplicated) while simultaneously making ATTN-01 fully observable for the first time (Today is reachable, not just built).
Output: `Sidebar.jsx`, `icons.js`, `App.jsx`, `OverviewTab.jsx`, `CalendarTab.jsx` each updated; `ActionsTab.jsx` deleted.
</objective>

## Artifacts This Phase Produces

New files:
- `app/src/lib/attention.js` — attention-derivation module (Plan 02-01)
- `app/src/components/TodayTab.jsx` — the unified attention feed component (Plan 02-02)

Modified files:
- `app/src/components/TimelineFindsPanel.jsx` — Section-style chrome refactor, new `onPendingChange` prop (Plan 02-01)
- `app/src/lib/icons.js` — `NAV_ICON.today` added (Plan 02-01), `NAV_ICON.actions` removed (this plan)
- `app/src/components/layout/Sidebar.jsx` — `NAV_ITEMS` gains `today` (first), loses `actions` (this plan)
- `app/src/App.jsx` — `AppInner`/`DemoApp` render `TodayTab` instead of `ActionsTab`; `NETWORK_VIEWS` drops `keepintouch`; `DEMO_NAV_ITEMS` swaps `actions` for `today` (this plan)
- `app/src/components/OverviewTab.jsx` — nudge sections removed (this plan)
- `app/src/components/CalendarTab.jsx` — `TimelineFindsPanel` mount removed (this plan)

Deleted files:
- `app/src/components/ActionsTab.jsx` (this plan)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unified-attention-feed-today/02-CONTEXT.md
@.planning/phases/02-unified-attention-feed-today/02-RESEARCH.md
@.planning/phases/02-unified-attention-feed-today/02-PATTERNS.md
@.planning/phases/02-unified-attention-feed-today/02-VALIDATION.md
@app/src/App.jsx
@app/src/components/layout/Sidebar.jsx
@app/src/lib/icons.js
@app/src/components/OverviewTab.jsx
@app/src/components/CalendarTab.jsx
@app/src/components/TodayTab.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav registry cutover — Sidebar.jsx NAV_ITEMS + icons.js NAV_ICON (add Today first, remove Actions), per D-03</name>
  <files>app/src/components/layout/Sidebar.jsx, app/src/lib/icons.js</files>
  <read_first>
    app/src/components/layout/Sidebar.jsx (full, current `NAV_ITEMS` array lines 3-12); app/src/lib/icons.js (current `NAV_ICON`, post-Plan-02-01 with `today: Gauge` already present); .planning/phases/02-unified-attention-feed-today/02-CONTEXT.md D-03 (Today is a new top-level nav item, positioned first); .planning/phases/02-unified-attention-feed-today/02-PATTERNS.md §"app/src/components/layout/Sidebar.jsx (NAV_ITEMS) — config edit" and §"app/src/lib/icons.js (NAV_ICON) — config edit".
  </read_first>
  <action>
    In `Sidebar.jsx`'s `NAV_ITEMS` array, insert `{ id: 'today', label: 'Today' }` as the first entry (before `{ id: 'overview', label: 'Overview' }`), and remove the `{ id: 'actions', label: 'Actions' }` entry entirely — this is the moment `today` becomes reachable and `actions` becomes unreachable via the nav, per D-03. This edit alone does not break the build (`App.jsx` still has its `tab === 'actions'` render branch until Task 2 of this same plan runs), but this plan's tasks must all complete together — do not treat Task 1 as a safe standalone stopping point.

    In `icons.js`, remove the `actions: ListChecks` entry from `NAV_ICON` (the `today: Gauge` entry, added in Plan 02-01, stays). Before touching the `lucide-react` import line, grep the rest of `app/src` for any other use of `ListChecks` — if none exists outside this one now-removed `NAV_ICON` reference, remove `ListChecks` from the import list too; if it is used elsewhere, leave the import in place rather than break another call site over an unrelated cleanup.
  </action>
  <acceptance_criteria>
    - `Sidebar.jsx`'s `NAV_ITEMS` array's first entry has `id: 'today'`; no entry with `id: 'actions'` remains anywhere in the array.
    - `icons.js`'s `NAV_ICON` object has no `actions:` key; `today: Gauge` is still present.
    - If `ListChecks` was removed from `icons.js`'s import line, a repo-wide grep for `ListChecks` returns zero hits; if it was left in place, at least one other real usage justifies keeping it.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd app && S=src/components/layout/Sidebar.jsx; I=src/lib/icons.js; grep -qF "{ id: '"'"'today'"'"', label: '"'"'Today'"'"' }" "$S" || { echo "FAIL: today nav item missing"; exit 1; }; grep -q "id: '"'"'actions'"'"'" "$S" && { echo "FAIL: actions nav item still present"; exit 1; }; grep -q "actions:" "$I" && { echo "FAIL: NAV_ICON still has actions key"; exit 1; }; grep -qF "today: Gauge" "$I" || { echo "FAIL: today icon missing"; exit 1; }; echo "TASK1 nav registry cutover VERIFIED"'</automated>
  </verify>
  <done>Sidebar.jsx's nav list leads with Today and no longer lists Actions; icons.js's NAV_ICON map matches.</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx cutover — AppInner + DemoApp render Today instead of Actions; NetworkTab drops the Keep-in-Touch standalone view, per D-03/D-03b</name>
  <files>app/src/App.jsx</files>
  <read_first>
    app/src/App.jsx (full — `NETWORK_VIEWS` lines 36-44, `NetworkTab` lines 53-229 including the `view === 'keepintouch'` branch at lines 123-125 and the `logContact` state at line 59 / render block at lines 211-218, `AppInner` lines 246-370 including lines 20, 304-314, 330-334, 347, `DemoApp` lines 372-437 including lines 383, 409-410, 422-425, 434); app/src/components/TodayTab.jsx (Plan 02-02's output — exact prop names `contacts, apps, interactions, calls, relationships, onFindPeople, onRefresh, onRefreshRelationships, isDemoMode`); app/src/lib/attention.js; .planning/phases/02-unified-attention-feed-today/02-CONTEXT.md D-03b (`/demo`'s `DEMO_NAV_ITEMS` must gain `today` in this same phase); .planning/phases/02-unified-attention-feed-today/02-PATTERNS.md §"app/src/App.jsx (AppInner) — nav/routing edit", §"app/src/App.jsx (DemoApp/DEMO_NAV_ITEMS) — demo nav edit", §"app/src/components/KeepInTouchTab.jsx standalone-view removal".
  </read_first>
  <action>
    Remove `{ key: 'keepintouch', label: 'Keep in Touch', icon: HeartHandshake }` from `NETWORK_VIEWS` (line 40). Grep the rest of the file for `HeartHandshake` — it is not used anywhere else in `App.jsx` per the full read cited above, so drop it from the `lucide-react` import at line 32 too. In `NetworkTab`, remove the `view === 'keepintouch' ? <KeepInTouchTab .../> :` ternary branch (lines 123-125), the now-unreachable `logContact`/`setLogContact` state (line 59), and its render block (`{logContact && <LogInteractionModal .../>}`, lines 211-218) — `TodayTab` now owns its own equivalent Keep-in-Touch "Log" wiring independently, so this dead state in `NetworkTab` must not linger. Remove the `import KeepInTouchTab from './components/KeepInTouchTab.jsx'` line (line 14) — the component file itself is NOT deleted, only this one import site goes away (its row markup lives on, reimplemented locally inside `TodayTab.jsx`).

    Replace `import ActionsTab from './components/ActionsTab.jsx'` (line 20) with `import TodayTab from './components/TodayTab.jsx'`. Add `import { overdueFollowUps, staleApplications, highUrgencyContacts, wantToSchedule, oaDue, oaNeedsCheck, keepInTouchDue, needsReviewApps } from './lib/attention.js'`.

    In `AppInner`, replace lines 305-314's inline count math with `lib/attention.js` calls: compute `const todayCount = overdueFollowUps(contacts).length + staleApplications(apps).length + highUrgencyContacts(contacts).length + wantToSchedule(contacts).length + oaDue(apps).length + oaNeedsCheck(apps).length + keepInTouchDue(contacts, interactions).length + needsReviewApps(apps).length`, and change the `counts` object's `actions: actionCount > 0 ? actionCount : null` entry to `today: todayCount > 0 ? todayCount : null`. Drop the now-unused intermediate consts `overdueCount`/`staleCount`/`scheduleCount`/`actionCount` — but keep computing the local `activeApps` const (still feeds `counts.pipeline` and `PipelineTab`'s props) and grep the rest of the file before removing the `./shared.jsx` import's `isOverdue`/`isStaleApplication` names — if nothing else in `App.jsx` still uses them after this edit, drop them from that import line; otherwise leave them.

    Change `onOpenActions={() => setTab('actions')}` (line 333, `OverviewTab`'s prop) to `onOpenActions={() => setTab('today')}`. Replace line 347 — `{!loading && tab === 'actions' && <ActionsTab contacts={contacts} apps={apps} interactions={interactions} onRefresh={load} />}` — with `{!loading && tab === 'today' && <TodayTab contacts={contacts} apps={apps} interactions={interactions} calls={calls} relationships={contactRelationships} onFindPeople={goFindPeople} onRefresh={load} onRefreshRelationships={refreshContactRelationships} />}`.

    In `DemoApp`, change `const DEMO_NAV_ITEMS = NAV_ITEMS.filter(item => ['overview', 'network', 'pipeline', 'actions'].includes(item.id))` (line 383) so the filter list is exactly `['today', 'overview', 'network', 'pipeline']`, per D-03b. Replace lines 409-410's `overdueCount`/`counts` computation with `const todayCount = overdueFollowUps(contacts).length + staleApplications(apps).length + highUrgencyContacts(contacts).length + wantToSchedule(contacts).length + oaDue(apps).length + oaNeedsCheck(apps).length + keepInTouchDue(contacts, interactions).length + needsReviewApps(apps).length` and `const counts = { network: contacts.length, pipeline: activeApps.length, today: todayCount > 0 ? todayCount : null }`. Change line 424's `onOpenActions={() => setTab('actions')}` to `onOpenActions={() => setTab('today')}`. Replace line 434 — `{!loading && tab === 'actions' && <ActionsTab contacts={contacts} apps={apps} interactions={interactions} onRefresh={load} />}` — with `{!loading && tab === 'today' && <TodayTab contacts={contacts} apps={apps} interactions={interactions} relationships={contactRelationships} onRefresh={load} onRefreshRelationships={refreshContactRelationships} isDemoMode />}` — deliberately omitting `calls` (DemoApp never fetches it, and Timeline Finds — the only section needing it — is excluded in demo mode by `TodayTab`'s own `isDemoMode` guard) and `onFindPeople` (DemoApp has no `goFindPeople` closure; `ApplicationDetailModal`'s "Find people →" button fails soft without it, exactly matching `DemoApp`'s existing `PipelineTab` call, which also omits `onFindPeople`).
  </action>
  <acceptance_criteria>
    - `grep -c "ActionsTab" app/src/App.jsx` and `grep -c "'actions'" app/src/App.jsx` both return `0`.
    - `grep -c "'keepintouch'" app/src/App.jsx` returns `0`.
    - `import TodayTab from './components/TodayTab.jsx'` is present.
    - Both `AppInner`'s and `DemoApp`'s render blocks contain `tab === 'today' && <TodayTab`.
    - `DEMO_NAV_ITEMS`'s filter list contains `today` and does not contain `actions`.
    - `AppInner`'s `TodayTab` call includes `onFindPeople={goFindPeople}`; `DemoApp`'s `TodayTab` call does not pass `onFindPeople`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd app && F=src/App.jsx; C1=$(grep -c "ActionsTab" "$F"); [ "$C1" = "0" ] || { echo "FAIL: ActionsTab still referenced ($C1)"; exit 1; }; C2=$(grep -c "'"'"'actions'"'"'" "$F"); [ "$C2" = "0" ] || { echo "FAIL: '"'"'actions'"'"' string still present ($C2)"; exit 1; }; C3=$(grep -c "'"'"'keepintouch'"'"'" "$F"); [ "$C3" = "0" ] || { echo "FAIL: keepintouch still present ($C3)"; exit 1; }; grep -qF "import TodayTab from '"'"'./components/TodayTab.jsx'"'"'" "$F" || { echo "FAIL: TodayTab import missing"; exit 1; }; grep -qF "onFindPeople={goFindPeople}" "$F" || { echo "FAIL: AppInner Find-people relay missing"; exit 1; }; grep -qF "'"'"'today'"'"', '"'"'overview'"'"', '"'"'network'"'"', '"'"'pipeline'"'"'" "$F" || { echo "FAIL: DEMO_NAV_ITEMS filter not updated"; exit 1; }; npx esbuild "$F" --bundle=false --format=esm --outfile=/dev/null && echo "TASK2 App.jsx cutover VERIFIED"'</automated>
  </verify>
  <done>App.jsx renders Today (not Actions) in both AppInner and DemoApp, with the sidebar badge count sourced from lib/attention.js, the Find-people relay wired for the real app, and NetworkTab's Keep-in-Touch standalone view fully removed.</done>
</task>

<task type="auto">
  <name>Task 3: Retire ActionsTab.jsx, Overview's nudge sections, and CalendarTab's TimelineFindsPanel mount — final ATTN-03 verification</name>
  <files>app/src/components/ActionsTab.jsx, app/src/components/OverviewTab.jsx, app/src/components/CalendarTab.jsx</files>
  <read_first>
    app/src/components/OverviewTab.jsx (full — the "want to schedule" nudge block lines 80-88, the "Needs Attention" block lines 127-156, the local consts `scheduleQueue`/`overdueContacts`/`staleApps` at lines 30/37-38/40); app/src/components/CalendarTab.jsx (full — `TimelineFindsPanel` import line 12, mount lines 154-155); .planning/phases/02-unified-attention-feed-today/02-PATTERNS.md §"app/src/components/OverviewTab.jsx — nudge removal", §"app/src/components/CalendarTab.jsx — TimelineFindsPanel unmount", §"app/src/components/ActionsTab.jsx — deletion"; .planning/phases/02-unified-attention-feed-today/02-VALIDATION.md's ATTN-03 grep command.
  </read_first>
  <action>
    Delete `app/src/components/ActionsTab.jsx` outright — by this point Task 2 has already removed `App.jsx`'s only import/render references to it, so nothing in the working tree still points at it.

    In `OverviewTab.jsx`: remove the indigo "want to schedule" nudge block (lines 80-88, the `{scheduleQueue.length > 0 && (...)}` div) and the danger-colored "Needs Attention" block (lines 127-156, the `{(overdueContacts.length > 0 || staleApps.length > 0) && (...)}` div) entirely. Remove the now-unused local consts `scheduleQueue` (line 30), `overdueContacts` (lines 37-38), and `staleApps` (line 40) — leave `triagedApps`, `activeApps`, `interviews`, `offers`, `warmContacts`, `stageCounts`, `funnelData`, `conversions`, `donutData`, `trendData`/`trendWeeks`, `hasInteractions`, `companyCount`, `hasRecruitingActivity`, and `daysSince` untouched — all still feed the KPI cards, funnel chart, donut chart, and trend chart, none of which are in this phase's scope. Remove `onOpenActions` from `OverviewTab`'s destructured props (`App.jsx`'s Task 2 edit already repoints its call site to `setTab('today')`, and `OverviewTab` no longer calls `onOpenActions` anywhere after this removal) — keeping an unused prop parameter would be dead code. Remove `daysUntil`/`isOverdue`/`isStaleApplication` from `OverviewTab.jsx`'s `../shared.jsx` import line if nothing else in the file still uses them after this edit (grep first).

    In `CalendarTab.jsx`: remove the `<TimelineFindsPanel apps={apps} calls={calls} interactions={interactions} contacts={contacts} onEventCreated={refetchMonth} />` mount (lines 154-155) and the `import TimelineFindsPanel from './TimelineFindsPanel.jsx'` line (line 12) — the component file itself is not touched further; it now mounts only from `TodayTab.jsx`.

    Run this phase's ATTN-03 removal grep from `02-VALIDATION.md`: search `app/src/` for `ActionsTab` or the literal quoted string `'actions'` — confirm zero hits anywhere in the tree, not just in `App.jsx`.
  </action>
  <acceptance_criteria>
    - `app/src/components/ActionsTab.jsx` no longer exists on disk.
    - A repo-wide search of `app/src/` for `ActionsTab` or the quoted string `'actions'` returns zero hits.
    - `OverviewTab.jsx` contains neither the "Needs Attention" heading text nor the `scheduleQueue` nudge block; `onOpenActions` is fully removed from its prop signature and body.
    - `CalendarTab.jsx` contains no reference to `TimelineFindsPanel`.
    - `cd app && npm run build` succeeds — this is the first point in the phase where `TodayTab.jsx` becomes reachable from the entry point via `App.jsx`, making this the real end-to-end compile check for the whole feature.
  </acceptance_criteria>
  <verify>
    <automated>bash -c "[ ! -f app/src/components/ActionsTab.jsx ] || { echo 'FAIL: ActionsTab.jsx still exists'; exit 1; }; C=\$(grep -rn \"ActionsTab\|'actions'\" app/src/ | wc -l | tr -d ' '); [ \"\$C\" = '0' ] || { echo \"FAIL: \$C lingering ActionsTab/actions references\"; grep -rn \"ActionsTab\|'actions'\" app/src/; exit 1; }; grep -q 'TimelineFindsPanel' app/src/components/CalendarTab.jsx && { echo 'FAIL: CalendarTab.jsx still references TimelineFindsPanel'; exit 1; }; grep -q 'Needs Attention' app/src/components/OverviewTab.jsx && { echo 'FAIL: OverviewTab.jsx still has the old nudge'; exit 1; }; cd app && npm run build && echo 'TASK3 RETIREMENT + FULL BUILD VERIFIED'"</automated>
  </verify>
  <done>ActionsTab.jsx is deleted, Overview's nudge sections and Calendar's TimelineFindsPanel mount are gone, the ATTN-03 grep is clean repo-wide, and the full app build succeeds end-to-end with TodayTab now live in the nav.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (React) → nav/routing state | This plan changes only which tab id maps to which component (`App.jsx`'s local `tab` state, no router, no URL param) — no new endpoint, no new auth/session code, no new data path. |
| Client → /demo anonymous route | `DEMO_NAV_ITEMS` now includes `today`; `TodayTab`'s `isDemoMode` prop (wired in Plan 02-02) must suppress the `TimelineFindsPanel` mount for anonymous visitors, since that mount is the only section that fires an authenticated AI proxy call. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched |
| V5 Input Validation | No | No new input surface — this plan only rewires which component renders for which tab id |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-05 | Denial of Service (resource/cost abuse, not unauthorized access — the proxy already fails closed via requireUser()) | `/demo` route reaching TimelineFindsPanel's mount-time AI scan | low | mitigate | `DemoApp`'s `TodayTab` call passes `isDemoMode` (Plan 02-02 threads this into the `!isDemoMode && <TimelineFindsPanel .../>` guard) — an anonymous demo visitor's browser never mounts the component that fires the scan; verified by this plan's Task 2/3 acceptance criteria and the phase's end-of-phase human `/demo` pass. |
| T-02-06 | (none identified) | Sidebar.jsx / icons.js NAV_ITEMS / NAV_ICON edits | low | accept | Static config-array edits; no props/behavior surface beyond which label+icon+id renders in the nav list. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new package installs this plan — Package Legitimacy Gate not applicable. |

</threat_model>

<verification>
Run all 3 tasks' `<verify><automated>` commands in order — Task 3's `npm run build` is the phase's real end-to-end compile gate, since `TodayTab.jsx` only becomes reachable from the entry point once this plan's App.jsx edit lands. After this plan, `git diff --stat` for the full phase changeset should show exactly: `app/src/lib/attention.js` (new), `app/src/components/TodayTab.jsx` (new), `app/src/components/TimelineFindsPanel.jsx`, `app/src/lib/icons.js`, `app/src/components/layout/Sidebar.jsx`, `app/src/App.jsx`, `app/src/components/OverviewTab.jsx`, `app/src/components/CalendarTab.jsx`, and `app/src/components/ActionsTab.jsx` deleted — no other file.
</verification>

<success_criteria>
- Today is reachable first in the nav in both the real app and /demo; clicking it renders the full unified feed.
- ActionsTab.jsx no longer exists; Overview's nudge sections and Calendar's TimelineFindsPanel mount are gone.
- The ATTN-03 repo-wide grep is clean and the full production build succeeds.
</success_criteria>

<output>
Create `.planning/phases/02-unified-attention-feed-today/02-03-SUMMARY.md` when done
</output>
