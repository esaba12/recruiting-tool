---
phase: 02-unified-attention-feed-today
plan: 4
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: true
requirements: [ATTN-01, ATTN-02, ATTN-03]

must_haves:
  truths:
    - "User visits Today and sees one feed combining overdue follow-ups, stale applications, the Keep in Touch queue, Job Boards' Needs-Review bucket, and Timeline Finds — Success Criterion 1."
    - "User clicks any attention item and lands directly on its full contact/application/job record in one click (Timeline Finds excepted, per D-04b's documented scope-narrowing) — Success Criterion 2."
    - "The standalone Actions tab, Overview's separate nudge section, Keep in Touch's standalone queue view, and TimelineFindsPanel's standalone presentation are gone — not left running in parallel as a 9th destination — Success Criterion 3."
    - "A human has visually confirmed all of the above across both the authenticated app and the public /demo route, per the project's standing render-and-screenshot verification directive."
  artifacts: []
  key_links:
    - "02-01/02-02/02-03's individual diffs -> this plan's combined regression sweep — the first point in the phase where all 3 plans' changes coexist in the working tree simultaneously, so this is the only place a cross-plan interaction would surface."
---

<objective>
Run the phase-closing verification pass: a combined automated regression sweep across every grep/build gate established in `02-01` through `02-03`, followed by the mandatory human render-and-screenshot check across Today's 9 sections, every deep-link target, Overview, Calendar, and `/demo`.

Purpose: this is the phase's goal-backward closing gate — ATTN-01/02/03 can only be meaningfully confirmed once all 3 code-editing plans have landed together and the app is running live; the aesthetic/functional checks require a live render, which no earlier plan's automated verify can substitute for.
Output: a recorded pass/fail for every regression gate plus a human-confirmed visual/functional pass, documented in this plan's SUMMARY.md — the artifact `/gsd-verify-work` reads to close out Phase 2.
</objective>

## Artifacts This Phase Produces

New files:
- `app/src/lib/attention.js` — attention-derivation module (Plan 02-01)
- `app/src/components/TodayTab.jsx` — the unified attention feed component (Plan 02-02)

Modified files:
- `app/src/components/TimelineFindsPanel.jsx` — Section-style chrome refactor, new `onPendingChange` prop (Plan 02-01)
- `app/src/lib/icons.js` — `NAV_ICON.today` added, `NAV_ICON.actions` removed (Plans 02-01/02-03)
- `app/src/components/layout/Sidebar.jsx` — `NAV_ITEMS` gains `today` (first), loses `actions` (Plan 02-03)
- `app/src/App.jsx` — `AppInner`/`DemoApp` render `TodayTab` instead of `ActionsTab`; `NETWORK_VIEWS` drops `keepintouch`; `DEMO_NAV_ITEMS` swaps `actions` for `today` (Plan 02-03)
- `app/src/components/OverviewTab.jsx` — nudge sections removed (Plan 02-03)
- `app/src/components/CalendarTab.jsx` — `TimelineFindsPanel` mount removed (Plan 02-03)

Deleted files:
- `app/src/components/ActionsTab.jsx` (Plan 02-03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unified-attention-feed-today/02-UI-SPEC.md
@.planning/phases/02-unified-attention-feed-today/02-VALIDATION.md
@.planning/phases/02-unified-attention-feed-today/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combined automated regression sweep</name>
  <files></files>
  <read_first>
    02-VALIDATION.md's "Phase Requirements → Test Map" and "Manual-Only Verifications" table — the exact commands and the exact rationale for why the functional/aesthetic checks are manual, not scripted; the SUMMARY.md files produced by 02-01 through 02-03 (confirm each plan's own verify commands were reported green before starting this sweep).
  </read_first>
  <action>
    Re-run, as one combined pass against the final working tree (all of 02-01 through 02-03 landed), the deterministic checks each earlier plan already established individually: (1) `app/src/lib/attention.js` exports all 8 named derivation functions plus the `keepInTouchDue` re-export (02-01 Task 1's check); (2) `app/src/lib/icons.js`'s `NAV_ICON` has `today: Gauge` and no `actions:` key (02-01 Task 2 / 02-03 Task 1's checks); (3) `app/src/components/TimelineFindsPanel.jsx` has no `bg-gradient-to-r`/accordion state, has `onPendingChange`, and still builds cleanly (02-01 Task 3's check); (4) `app/src/components/TodayTab.jsx` exists, exports `TodayTab` as default, contains all 9 section title patterns in D-02's ascending order, uses `RowCap` at least 8 times, and mounts both `ContactDetailModal`/`ApplicationDetailModal` with `onFindPeople={onFindPeople}` present (02-02's 3 tasks' checks); (5) `app/src/App.jsx` has zero `ActionsTab`/`'actions'`/`'keepintouch'` references and both `AppInner`/`DemoApp` render `tab === 'today' && <TodayTab` (02-03 Task 2's check); (6) `app/src/components/ActionsTab.jsx` does not exist and a repo-wide `app/src/` grep for `ActionsTab`/`'actions'` returns zero hits (02-03 Task 3's check); (7) `cd app && npm run build` succeeds cleanly against the fully-merged tree. Running this as one combined sweep (rather than trusting each plan's individually-passing verify in isolation) is the only way to catch a cross-plan interaction — e.g. a later plan's edit accidentally re-touching a file an earlier plan already finalized.
  </action>
  <acceptance_criteria>
    - All 7 categories of checks above pass with zero FAIL against the current working tree.
    - `git diff --stat` (or equivalent) for this phase's full changeset shows exactly these files touched: `app/src/lib/attention.js` (new), `app/src/components/TodayTab.jsx` (new), `app/src/components/TimelineFindsPanel.jsx`, `app/src/lib/icons.js`, `app/src/components/layout/Sidebar.jsx`, `app/src/App.jsx`, `app/src/components/OverviewTab.jsx`, `app/src/components/CalendarTab.jsx` — plus `app/src/components/ActionsTab.jsx` deleted. No other file in the repo has an unexplained diff.
    - `app/src/components/KeepInTouchTab.jsx`, `app/src/components/PipelineTab.jsx`, `app/src/components/ApplicationDetailModal.jsx`, `app/src/components/ContactDetailModal.jsx` show zero diff (confirming these reused-as-is components were genuinely not touched by this phase).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; ok=1; cd app; F1=src/lib/attention.js; F2=src/lib/icons.js; F3=src/components/TimelineFindsPanel.jsx; F4=src/components/TodayTab.jsx; F5=src/App.jsx; for sym in "export function overdueFollowUps" "export function staleApplications" "export function highUrgencyContacts" "export function needsReviewApps" "keepInTouchQueue as keepInTouchDue"; do grep -q "$sym" "$F1" || { echo "FAIL attention.js: $sym"; ok=0; }; done; grep -qF "today: Gauge" "$F2" || { echo "FAIL icons.js: today missing"; ok=0; }; grep -q "actions:" "$F2" && { echo "FAIL icons.js: actions key still present"; ok=0; }; grep -q "bg-gradient-to-r" "$F3" && { echo "FAIL TimelineFindsPanel.jsx: old gradient chrome present"; ok=0; }; grep -q "onPendingChange" "$F3" || { echo "FAIL TimelineFindsPanel.jsx: onPendingChange missing"; ok=0; }; grep -q "export default function TodayTab" "$F4" || { echo "FAIL TodayTab.jsx: default export missing"; ok=0; }; C=$(grep -c "<RowCap" "$F4"); [ "$C" -ge 8 ] || { echo "FAIL TodayTab.jsx: RowCap count $C < 8"; ok=0; }; grep -qF "onFindPeople={onFindPeople}" "$F4" || { echo "FAIL TodayTab.jsx: onFindPeople relay missing"; ok=0; }; grep -qF "onFindPeople={goFindPeople}" "$F5" || { echo "FAIL App.jsx: AppInner Find-people relay missing"; ok=0; }; C2=$(grep -c "ActionsTab" "$F5"); [ "$C2" = "0" ] || { echo "FAIL App.jsx: ActionsTab still referenced"; ok=0; }; [ ! -f src/components/ActionsTab.jsx ] || { echo "FAIL: ActionsTab.jsx still exists"; ok=0; }; C3=$(grep -rn "ActionsTab\|'"'"'actions'"'"'" src/ | wc -l | tr -d " "); [ "$C3" = "0" ] || { echo "FAIL: $C3 lingering ActionsTab/actions references repo-wide"; ok=0; }; npm run build > /tmp/recruiter-phase2-build.log 2>&1 || { echo "FAIL: npm run build failed, see /tmp/recruiter-phase2-build.log"; ok=0; }; [ "$ok" = "1" ] && echo "COMBINED REGRESSION SWEEP: ALL GREEN" || { echo "COMBINED REGRESSION SWEEP: FAILURES ABOVE"; exit 1; }'</automated>
  </verify>
  <done>All 7 categories of deterministic checks pass against the fully-merged phase diff; no file outside the expected changeset was touched; the production build succeeds end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Start the dev server and stage the visual/functional verification for end-of-phase review</name>
  <files></files>
  <read_first>
    02-UI-SPEC.md's "Feed Composition Contract" and "Navigation" sections; the global frontend-aesthetics directive (`~/.claude/CLAUDE.md`) requiring a render-and-screenshot pass before declaring UI work done; this project's `workflow.human_verify_mode` config (defaults to `end-of-phase` — no mid-flight `checkpoint:human-verify` task is used here; the verification steps below are embedded in this task's `<verify><human-check>` block per that mode, harvested into this phase's UAT.md for one batched review rather than a blocking mid-flight pause).
  </read_first>
  <action>
    Start `cd app && npm run dev` in the background, wait for Vite's "ready in" signal, and confirm the local dev server responds (fetch returns 200) before considering this task's automated portion complete. Leave the server running — the human-check verification below depends on it.
  </action>
  <acceptance_criteria>
    - The dev server is running and reachable at its local URL (default http://localhost:3001 per this project's documented dev port).
    - No stray port-conflict or startup error in the server's output.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd app && (npm run dev &> /tmp/recruiter-dev-server.log &) ; for i in $(seq 1 30); do node -e "fetch(\"http://localhost:3001\").then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))" 2>/dev/null && { echo "DEV SERVER READY"; exit 0; }; sleep 1; done; echo "DEV SERVER DID NOT BECOME READY — see /tmp/recruiter-dev-server.log"; exit 1'</automated>
    <human-check>
      With the dev server running at http://localhost:3001 (sign in with your own account, or use its Sign-in-with-API-key fast path):
      1. Confirm the sidebar's first nav item is "Today" (with a gauge icon) and that "Actions" no longer appears anywhere in the nav (desktop sidebar and mobile bottom bar).
      2. Click Today. Confirm you see a vertical stack of section cards in this order: Overdue Follow-Ups, Stale Applications, High Urgency Contacts, Keep in Touch Due, Job Boards Needs-Review, Want to Schedule, OA-Due, OA-Needs-Check, Timeline Finds — each section only appears if it has items (except Timeline Finds, which always shows). If your account has few or no items in some categories, that's expected — confirm at minimum that the sections your data does populate render with the correct data and in the correct relative order.
      3. Click a contact row in Overdue Follow-Ups (or High Urgency, or Want to Schedule, or Keep in Touch Due) — confirm it opens the full ContactDetailModal (not just an inline expand). Separately, confirm the small chevron/expand toggle (Overdue Follow-Ups / Want to Schedule only) still opens the inline Draft panel without also opening the modal.
      4. Click an application row in Stale Applications or Job Boards Needs-Review — confirm it opens the full ApplicationDetailModal. If the application's company has any contacts you know there, confirm the "Find people →"/"Find more people →" button is present and clicking it navigates to Network → Discover pre-filtered to that company (the cross-tab relay). On a Needs-Review row, confirm the inline triage chips (Applying/Maybe/Applied/Pass) change the item's bucket without opening the modal.
      5. If any Timeline Finds items are pending, confirm the section shows a Section-style card (icon, title, count, Rescan button, Mono-wrapped "Last scan" text) — not the old gradient/accordion look — and that "+ Add to Calendar"/"Dismiss" still work. If none are pending, confirm it shows "Nothing pending — you're caught up." rather than being hidden.
      6. Visit Overview — confirm the KPI cards, funnel chart, donut chart, and trend chart all still render correctly, and that the old indigo "want to schedule" nudge and the danger-red "Needs Attention" block are both gone.
      7. Visit Calendar — confirm it no longer shows the Timeline Finds panel above the calendar grid (that panel now lives only on Today).
      8. Visit Network → the segmented view control — confirm "Keep in Touch" is no longer one of the options (Table/Cards/Graph/Coverage/Outbox/Discover should remain).
      9. Visit `https://<your-dev-host>/demo` (or `http://localhost:3001/demo`) in a private/incognito window (no sign-in). Confirm the trimmed nav shows Today, Overview, Network, Pipeline (no Actions, no Settings/Explore/Calendar/Job Boards). Click Today — confirm Overdue Follow-Ups, Stale Applications, High Urgency, Keep in Touch Due, and Job Boards Needs-Review (Airbnb + Discord seed data) all render with real seeded data, and that there is NO Timeline Finds section and no AI-call error banner anywhere on the page.
    </human-check>
  </verify>
  <done>Dev server running and reachable; the 9-step visual/functional verification is staged in this task's human-check block for end-of-phase batch review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev server (client browser ↔ Vite dev server) | Standard local development loop, already covered by this repo's existing auth/RLS/BYOK model (unchanged by this phase). This task only renders the app locally for inspection — no new endpoint, no new data path. |
| Public /demo route | Verified in this plan's human-check to confirm zero AI-proxy calls fire for an anonymous visitor (Timeline Finds correctly omitted) — the last confirmation that the demo-mode mitigation from Plans 02-01/02-02/02-03 actually holds end-to-end. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched — verification uses the existing signed-in session or `/demo`'s no-auth path, both unchanged by this phase |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched |
| V5 Input Validation | No | No new input surface — this task is read-only visual/functional inspection |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-07 | (none identified) | Local dev server used for visual/functional verification | low | accept | Standard `npm run dev` local loop already in daily use for this project; no new exposure introduced by running it for this pass. |
| T-02-08 | Denial of Service (cost/abuse — proxy already fails closed) | `/demo` route, final end-to-end confirmation | low | accept | Human-check step 9 is the phase's final confirmation that no AI-proxy call fires for an anonymous demo visitor — if this step surfaces a 401 error banner, that is a finding to fix before closing the phase, not an accepted risk. |

</threat_model>

<verification>
Task 1's automated sweep must be green before Task 2 starts the dev server. Task 2's `<verify><human-check>` block is harvested by the verifier at end-of-phase (per `workflow.human_verify_mode=end-of-phase`) into this phase's UAT.md for one batched human review — do not mark Phase 2 complete until that review confirms all 9 steps pass, including the /demo AI-call-free confirmation.
</verification>

<success_criteria>
- Combined regression sweep across all 3 plans' deterministic checks passes with zero FAIL, and the production build succeeds.
- Dev server starts cleanly and the 9-step visual/functional-verification human-check is staged for end-of-phase UAT review.
- ATTN-01, ATTN-02, and ATTN-03 are all confirmed true by that review — one unified feed, working deep-links (Timeline Finds' documented exception aside), and the 4 retired surfaces genuinely gone, not duplicated.
</success_criteria>

<output>
Create `.planning/phases/02-unified-attention-feed-today/02-04-SUMMARY.md` when done
</output>
