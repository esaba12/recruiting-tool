---
phase: 04-shared-record-side-panel
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - app/src/App.jsx
  - app/src/components/ReferralCoverageTab.jsx
  - app/src/components/TodayTab.jsx
  - app/src/components/CalendarTab.jsx
  - app/src/components/PipelineTab.jsx
  - app/src/components/jobBoards/RepoJobsView.jsx
  - app/src/components/ContactDetailModal.jsx
  - app/src/components/ApplicationDetailModal.jsx
  - app/src/components/jobBoards/JobDetailModal.jsx
autonomous: true
requirements: [PANEL-01, PANEL-02]

must_haves:
  truths:
    - "Opening a contact from Network, Today, Calendar, or Coverage renders the same shared side-panel component."
    - "Opening an application from Today, Calendar, or Pipeline renders that same shared side-panel component."
    - "Opening a job from Job Boards renders that same shared side-panel component."
    - "Each of the 8 non-nested render sites passes exactly the prop set it passes today — every existing omission is carried forward unchanged, and no site gains a capability it did not have."
    - "The three legacy record modal files no longer exist and nothing imports or renders them."
  artifacts:
    - "All 6 call-site files re-pointed to SidePanel plus the matching panel body"
    - "Three legacy record modal files deleted"
  key_links:
    - "Every re-pointed site imports SidePanel from the ui/ directory and its body from the panels/ directory"
    - "Each SidePanel's onClose is the same handler the body's onClose already used at that site"
    - "app/src/components/ui/Modal.jsx stays untouched and still serves the out-of-scope dialogs"
---

<!-- planner-discipline-allow: onRefreshRelationships -->
<!-- planner-discipline-allow: contactRelationships -->
<!-- planner-discipline-allow: onFindPeople -->
<!-- planner-discipline-allow: ContactDetailModal -->
<!-- planner-discipline-allow: ApplicationDetailModal -->
<!-- planner-discipline-allow: JobDetailModal -->

<objective>
Re-point all 9 record render sites to `<SidePanel>` plus the matching `*PanelBody`, then delete the three legacy record modal files.

Purpose: This is the plan that actually delivers PANEL-01 — until every site is re-pointed, the app still has divergent record surfaces. It is also where PANEL-02 is most easily violated by accident, because the three modals are called with materially different prop subsets at different sites.

Output: 6 modified call-site files, 3 deleted legacy files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-record-side-panel/04-CONTEXT.md
@.planning/phases/04-shared-record-side-panel/04-PATTERNS.md
@.planning/phases/04-shared-record-side-panel/04-01-SUMMARY.md
@.planning/phases/04-shared-record-side-panel/04-02-SUMMARY.md
@.planning/phases/04-shared-record-side-panel/04-03-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase-level symbol list. This plan produces the rows marked ★.

| Symbol | File | Kind | Plan |
|--------|------|------|------|
| `useMediaQuery(query)` | `app/src/lib/useMediaQuery.js` | default-export hook | 04-01 |
| `SidePanel` | `app/src/components/ui/SidePanel.jsx` | default-export component — props `{ open = true, onClose, children, className }` | 04-01 |
| `JobPanelBody` | `app/src/components/panels/JobPanelBody.jsx` | default-export component | 04-01 |
| `ContactPanelBody` | `app/src/components/panels/ContactPanelBody.jsx` | default-export component with the new optional `onBack` prop | 04-02 |
| `ApplicationPanelBody` | `app/src/components/panels/ApplicationPanelBody.jsx` | default-export component; owns D-05's in-place swap | 04-03 |
| ★ 6 re-pointed call-site files | `App.jsx`, `TodayTab.jsx`, `CalendarTab.jsx`, `ReferralCoverageTab.jsx`, `PipelineTab.jsx`, `jobBoards/RepoJobsView.jsx` | 8 non-nested render sites migrated; zero new props threaded | 04-04 |
| ★ 3 legacy record modal files deleted | `app/src/components/`, `app/src/components/jobBoards/` | file deletions | 04-04 |

## The 9 render sites (verified against current source)

| # | File | Record | Current line | Notes |
|---|------|--------|--------------|-------|
| 1 | `App.jsx` (NetworkTab) | contact | ~177 | full prop set; drives both edit and create via `editing === 'new'` |
| 2 | `TodayTab.jsx` | contact | ~422 | full prop set |
| 3 | `TodayTab.jsx` | application | ~441 | full prop set |
| 4 | `CalendarTab.jsx` | contact | ~270 | **omits** `interactions`, `contactRelationships`, `onRefreshRelationships` |
| 5 | `CalendarTab.jsx` | application | ~279 | **omits** `contacts`, `apps`, `interactions`, `relationships`, `onSaved`, `onFindPeople`, `onRefresh`, `onRefreshRelationships` |
| 6 | `ReferralCoverageTab.jsx` | contact | ~178 | create mode (`contact={null}` + `initial`); **omits** `onRefreshRelationships` |
| 7 | `PipelineTab.jsx` | application | ~209 | full prop set; exercises create mode via `addingNew` |
| 8 | `jobBoards/RepoJobsView.jsx` | job | ~328 | full prop set |
| 9 | `ApplicationDetailModal.jsx` | nested contact | ~487 | already migrated in 04-03 as the in-place swap; deleted with its host file here |

**Prop-omission rule (04-RESEARCH.md Pitfall 4 — hard constraint):** sites 4, 5, and 6 omit props that other sites pass. Carry every omission forward exactly. Do not thread missing props through "while you are in there". Doing so would newly surface the relationships editor, the warm-path dossier, and the fit analysis on Calendar for the first time — behavior the user never asked for and never verified. If a resulting gap looks like a bug, record it in the SUMMARY as a pre-existing finding; do not fix it in this phase.

<tasks>

<task type="auto">
  <name>Task 1: Re-point the contact-only call sites (App.jsx, ReferralCoverageTab.jsx)</name>
  <files>app/src/App.jsx, app/src/components/ReferralCoverageTab.jsx</files>
  <read_first>
    - `app/src/App.jsx` — read the import block (lines 1-30), the NetworkTab render site (lines 165-190), and the two comment blocks at lines ~272-276 and ~375-379. Do not read the whole file; use those ranges.
    - `app/src/components/ReferralCoverageTab.jsx` — read lines 1-12 (imports) and lines 170-190 (the render site).
    - `app/src/components/panels/ContactPanelBody.jsx` — confirm the exact prop names before wiring.
    - `app/src/components/ui/SidePanel.jsx` — confirm the shell's prop names (`open`, `onClose`, `children`, `className`).
  </read_first>
  <action>
**`app/src/App.jsx`:**
1. In the import block, replace the legacy contact modal import line with two imports: `import SidePanel from './components/ui/SidePanel.jsx'` and `import ContactPanelBody from './components/panels/ContactPanelBody.jsx'`.
2. At the NetworkTab render site (currently `{editing && (<ContactDetailModal ... />)}`), wrap the body in the shell:
   `{editing && (<SidePanel open onClose={() => setEditing(null)}><ContactPanelBody contact={editing === 'new' ? null : editing} contacts={contacts} interactions={interactions} contactRelationships={contactRelationships} onClose={() => setEditing(null)} onSaved={() => { setEditing(null); onRefresh() }} onRefreshRelationships={onRefreshRelationships} /></SidePanel>)}`
   Every body prop is byte-identical to what the legacy render passed. The shell's `onClose` is the same `() => setEditing(null)` handler. Do not pass `onBack` — nothing at a top-level site is a swapped-in record.
3. Rewrite the two prose comments that name the legacy modal so they describe the current mechanism instead: the `refreshContactRelationships` comment inside `AppInner` (around line 272) and the matching one inside `DemoApp` (around line 375). Both explain that the helper deliberately avoids toggling `loading`, because toggling it unmounts the Network tab and would silently close whatever record surface is open mid-edit. Keep that meaning; just stop naming a file that no longer exists — refer to "the open record panel". These are the only two comment edits in this file.

**`app/src/components/ReferralCoverageTab.jsx`:**
4. Replace the legacy contact modal import with `import SidePanel from './ui/SidePanel.jsx'` and `import ContactPanelBody from './panels/ContactPanelBody.jsx'`.
5. Wrap the `addingFor` render site:
   `{addingFor && (<SidePanel open onClose={() => setAddingFor(null)}><ContactPanelBody contact={null} initial={{ company: addingFor }} contacts={contacts} interactions={interactions} contactRelationships={contactRelationships} onClose={() => setAddingFor(null)} onSaved={() => { setAddingFor(null); onRefresh() }} /></SidePanel>)}`
   This site deliberately does not pass `onRefreshRelationships` today — keep it omitted. It also exercises the create branch (`contact={null}` plus `initial`), which must still render the reduced field set and the add-contact button copy.

Leave every other line of both files alone. Do not add any comment in either file that names a legacy modal file.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && test $(grep -c "ContactDetailModal" app/src/App.jsx) -eq 0 && test $(grep -c "ContactDetailModal" app/src/components/ReferralCoverageTab.jsx) -eq 0 && test $(grep -c "<SidePanel" app/src/App.jsx) -eq 1 && test $(grep -c "<ContactPanelBody" app/src/App.jsx) -eq 1 && test $(grep -c "<SidePanel" app/src/components/ReferralCoverageTab.jsx) -eq 1 && test $(grep -c "<ContactPanelBody" app/src/components/ReferralCoverageTab.jsx) -eq 1 && test $(grep -c "onRefreshRelationships" app/src/components/ReferralCoverageTab.jsx) -eq 0 && test $(grep -c "initial={{ company: addingFor }}" app/src/components/ReferralCoverageTab.jsx) -eq 1 && test $(grep -c "editing === 'new' ? null : editing" app/src/App.jsx) -eq 1 && cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ContactDetailModal" app/src/App.jsx` returns 0 and the same for `app/src/components/ReferralCoverageTab.jsx` — no imports, no JSX, and no leftover prose references.
    - Each file contains exactly one `<SidePanel` and one `<ContactPanelBody` render.
    - `App.jsx` still passes all 7 body props it passed before (`contact`, `contacts`, `interactions`, `contactRelationships`, `onClose`, `onSaved`, `onRefreshRelationships`) and still derives `contact` from `editing === 'new' ? null : editing` so the create path is intact.
    - `ReferralCoverageTab.jsx` still passes exactly 7 body props (`contact`, `initial`, `contacts`, `interactions`, `contactRelationships`, `onClose`, `onSaved`) and `onRefreshRelationships` remains absent from the whole file — the pre-existing omission is preserved, not fixed.
    - Neither file passes `onBack`: `grep -c "onBack" app/src/App.jsx app/src/components/ReferralCoverageTab.jsx` returns 0 for both.
    - The two `refreshContactRelationships` comments in `App.jsx` still explain the loading-flag hazard, now without naming a deleted file.
    - `cd app && npm run build` exits 0.
  </acceptance_criteria>
  <done>Both contact-only sites render the shared shell with byte-identical prop sets, including the preserved omission at Coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the dual-record call sites (TodayTab.jsx, CalendarTab.jsx)</name>
  <files>app/src/components/TodayTab.jsx, app/src/components/CalendarTab.jsx</files>
  <read_first>
    - `app/src/components/TodayTab.jsx` — read lines 1-20 (imports), lines 325-335 (the `changeAppTriage` comment block), and lines 415-458 (both render sites). Do not read the whole file.
    - `app/src/components/CalendarTab.jsx` — read lines 1-20 (imports) and lines 262-290 (both render sites).
    - `app/src/components/panels/ContactPanelBody.jsx` and `app/src/components/panels/ApplicationPanelBody.jsx` — confirm exact prop names.
  </read_first>
  <action>
**`app/src/components/TodayTab.jsx`:**
1. Replace the two legacy modal imports with `import SidePanel from './ui/SidePanel.jsx'`, `import ContactPanelBody from './panels/ContactPanelBody.jsx'`, `import ApplicationPanelBody from './panels/ApplicationPanelBody.jsx'`.
2. Wrap the `selectedContactId` site: `<SidePanel open onClose={() => setSelectedContactId(null)}>` around `<ContactPanelBody ... />` carrying its existing 7 props unchanged (`contact={contacts.find(c => c.id === selectedContactId)}`, `contacts`, `interactions`, `contactRelationships={relationships}`, `onClose`, `onSaved`, `onRefreshRelationships`).
3. Wrap the `selectedAppId` site: `<SidePanel open onClose={() => setSelectedAppId(null)}>` around `<ApplicationPanelBody ... />` carrying its existing 12 props unchanged (`app={selectedApp}`, `contacts`, `apps`, `interactions`, `relationships`, `onStatusChange`, `onClose`, `onDelete`, `onSaved`, `onFindPeople`, `onRefresh`, `onRefreshRelationships`). The `onDelete` async body (archive, clear the id, refresh) is copied across untouched.
4. Rewrite the comment above `changeAppTriage` (around line 329) so it no longer names a deleted file: it currently says the shared triage mutation is used both by the inline chips via `ApplicationRow` and by the application modal's status-change handler, and matches `PipelineTab.jsx`'s `changeTriage` exactly. Keep all three of those facts; refer to "the application panel" instead of the deleted file. This is the only comment edit in this file.

**`app/src/components/CalendarTab.jsx`:**
5. Replace the two legacy modal imports with the same three imports (shell plus both bodies), using `./ui/SidePanel.jsx` and `./panels/...` paths.
6. Wrap the `selectedContact` site: `<SidePanel open onClose={() => setSelectedContact(null)}>` around `<ContactPanelBody contact={selectedContact} contacts={contacts} onClose={() => setSelectedContact(null)} onSaved={() => { setSelectedContact(null); onRefresh() }} />`. Exactly 4 body props — this site does not pass `interactions`, `contactRelationships`, or `onRefreshRelationships` today, and must still not pass them. That means the history block renders empty and the relationships add button no-ops when a contact is opened from Calendar, exactly as it does today.
7. Wrap the `selectedApp` site: `<SidePanel open onClose={() => setSelectedApp(null)}>` around `<ApplicationPanelBody app={selectedApp} onStatusChange={s => changeAppTriage(selectedApp, s)} onClose={() => setSelectedApp(null)} onDelete={async () => { await archiveApplication(selectedApp.id); setSelectedApp(null); onRefresh() }} />`. Exactly 4 body props — this site does not pass `contacts`, `apps`, `interactions`, `relationships`, `onSaved`, `onFindPeople`, `onRefresh`, or `onRefreshRelationships` today, and must still not pass them.
8. Do not add defensive guards, default values, or optional-call operators to work around those omissions. If you notice that a code path in the body would throw because a callback is absent at this site, that is a pre-existing condition — record it in the SUMMARY under a "pre-existing findings, not fixed this phase" heading and move on. 04-RESEARCH.md Pitfall 4 is explicit that quietly changing behavior at these sites during the port is out of scope.

Leave every other line of both files alone. Do not add any comment naming a legacy modal file.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && for f in app/src/components/TodayTab.jsx app/src/components/CalendarTab.jsx; do test $(grep -c "ContactDetailModal\|ApplicationDetailModal" $f) -eq 0 || { echo "STALE REF in $f"; exit 1; }; test $(grep -c "<SidePanel" $f) -eq 2 || { echo "BAD SHELL COUNT in $f"; exit 1; }; test $(grep -c "<ContactPanelBody" $f) -eq 1 || { echo "BAD CONTACT COUNT in $f"; exit 1; }; test $(grep -c "<ApplicationPanelBody" $f) -eq 1 || { echo "BAD APP COUNT in $f"; exit 1; }; done && test $(grep -c "contactRelationships" app/src/components/CalendarTab.jsx) -eq 0 && test $(grep -c "onRefreshRelationships" app/src/components/CalendarTab.jsx) -eq 0 && test $(grep -c "onFindPeople" app/src/components/CalendarTab.jsx) -eq 0 && test $(grep -c "onRefreshRelationships" app/src/components/TodayTab.jsx) -ge 2 && test $(grep -c "contactRelationships={relationships}" app/src/components/TodayTab.jsx) -eq 1 && cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Neither file contains any reference to a legacy record modal name (0 matches in each), including in comments.
    - Each file contains exactly two `<SidePanel` renders, one `<ContactPanelBody`, and one `<ApplicationPanelBody`.
    - `TodayTab.jsx`'s contact site passes all 7 props and its application site all 12, unchanged from before.
    - `CalendarTab.jsx`'s contact site passes exactly 4 props and its application site exactly 4 props; `contactRelationships`, `onRefreshRelationships`, and `onFindPeople` appear 0 times in the entire file — the three pre-existing omissions are preserved.
    - No optional-call operators, defaults, or guards were added to either body or to `CalendarTab.jsx` to paper over those omissions.
    - The `changeAppTriage` comment in `TodayTab.jsx` still records all three original facts (used by the inline chips via `ApplicationRow`, used by the application panel's status-change handler, matches `PipelineTab.jsx`'s `changeTriage`) without naming a deleted file.
    - `cd app && npm run build` exits 0.
  </acceptance_criteria>
  <done>Both dual-record sites render the shared shell for contacts and applications, with Calendar's two reduced prop sets carried forward exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point Pipeline and Job Boards, then delete the three legacy modal files</name>
  <files>app/src/components/PipelineTab.jsx, app/src/components/jobBoards/RepoJobsView.jsx, app/src/components/ContactDetailModal.jsx, app/src/components/ApplicationDetailModal.jsx, app/src/components/jobBoards/JobDetailModal.jsx</files>
  <read_first>
    - `app/src/components/PipelineTab.jsx` — read lines 1-15 (imports) and lines 200-227 (the render site).
    - `app/src/components/jobBoards/RepoJobsView.jsx` — read lines 1-20 (imports) and lines 320-342 (the render site).
    - `app/src/components/panels/ApplicationPanelBody.jsx` and `app/src/components/panels/JobPanelBody.jsx` — confirm exact prop names.
  </read_first>
  <action>
**`app/src/components/PipelineTab.jsx`:**
1. Replace the legacy application modal import with `import SidePanel from './ui/SidePanel.jsx'` and `import ApplicationPanelBody from './panels/ApplicationPanelBody.jsx'`.
2. Wrap the render site, preserving its combined open condition and all 12 props:
   `{(selectedApp || addingNew) && (<SidePanel open onClose={() => { setSelectedAppId(null); setAddingNew(false) }}><ApplicationPanelBody app={selectedApp} contacts={contacts} apps={apps} interactions={interactions} relationships={relationships} onStatusChange={s => changeTriage(selectedApp, s)} onClose={() => { setSelectedAppId(null); setAddingNew(false) }} onDelete={async () => { await archiveApplication(selectedApp.id); setSelectedAppId(null); onRefresh() }} onSaved={() => { setAddingNew(false); onRefresh() }} onFindPeople={onFindPeople} onRefresh={onRefresh} onRefreshRelationships={onRefreshRelationships} /></SidePanel>)}`
   The `addingNew` branch is what exercises the create-an-application path (`app` is null), including the URL auto-fill box and the duplicate warning — it must keep working.

**`app/src/components/jobBoards/RepoJobsView.jsx`:**
3. Replace the legacy job modal import with `import SidePanel from '../ui/SidePanel.jsx'` and `import JobPanelBody from '../panels/JobPanelBody.jsx'`.
4. Wrap the render site, preserving all 8 props:
   `{selectedJob && (<SidePanel open onClose={() => setSelectedJob(null)}><JobPanelBody job={selectedJob} status={statusFor(selectedJob)} blurb={blurbs[jobId(selectedJob)]} deadline={deadlines[jobId(selectedJob)]} onRecheckDeadline={() => recheckDeadline(selectedJob)} onStatusChange={s => { updateStatus(selectedJob, s) }} onClose={() => setSelectedJob(null)} prefs={prefs} /></SidePanel>)}`

**Deletions (do these last, only after the two edits above build clean):**
5. Delete `app/src/components/ContactDetailModal.jsx`, `app/src/components/ApplicationDetailModal.jsx`, and `app/src/components/jobBoards/JobDetailModal.jsx` with `git rm`.
6. Confirm nothing still imports or renders them anywhere under `app/src`. Three files retain prose comments that reference the old names as historical provenance for extracted code (`shared.jsx`, `ui/ChipToggleGroup.jsx`, `QuickScheduleModal.jsx`). Leave those alone — they are accurate statements about where that code came from, and rewriting comments in files this phase otherwise does not touch is the kind of churn Phase 2 already learned to avoid. The gate below therefore checks imports and JSX usage, not the bare names.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && test $(grep -c "ApplicationDetailModal" app/src/components/PipelineTab.jsx) -eq 0 && test $(grep -c "JobDetailModal" app/src/components/jobBoards/RepoJobsView.jsx) -eq 0 && test $(grep -c "<SidePanel" app/src/components/PipelineTab.jsx) -eq 1 && test $(grep -c "<ApplicationPanelBody" app/src/components/PipelineTab.jsx) -eq 1 && test $(grep -c "(selectedApp || addingNew)" app/src/components/PipelineTab.jsx) -eq 1 && test $(grep -c "<SidePanel" app/src/components/jobBoards/RepoJobsView.jsx) -eq 1 && test $(grep -c "<JobPanelBody" app/src/components/jobBoards/RepoJobsView.jsx) -eq 1 && test ! -f app/src/components/ContactDetailModal.jsx && test ! -f app/src/components/ApplicationDetailModal.jsx && test ! -f app/src/components/jobBoards/JobDetailModal.jsx && test $(grep -rEn "<(Contact|Application|Job)DetailModal" app/src --include='*.jsx' | wc -l) -eq 0 && test $(grep -rEn "from '.*(Contact|Application|Job)DetailModal\.jsx'" app/src --include='*.jsx' | wc -l) -eq 0 && cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `PipelineTab.jsx` has zero references to the legacy application modal, exactly one `<SidePanel`, exactly one `<ApplicationPanelBody`, and still gates on `(selectedApp || addingNew)` so the create path survives.
    - `RepoJobsView.jsx` has zero references to the legacy job modal, exactly one `<SidePanel`, and exactly one `<JobPanelBody` carrying all 8 props.
    - All three legacy files are gone from the working tree and from git (`git status` shows them as deleted/staged deletions, not untracked leftovers).
    - Repo-wide, zero JSX renders of the three legacy component names remain: `grep -rEn "<(Contact|Application|Job)DetailModal" app/src --include='*.jsx' | wc -l` returns 0.
    - Repo-wide, zero imports of the three legacy files remain: `grep -rEn "from '.*(Contact|Application|Job)DetailModal\.jsx'" app/src --include='*.jsx' | wc -l` returns 0. Note `EventDetailModal` is a different, still-live component and must not be touched — confirm it still exists and is still imported by `CalendarTab.jsx`.
    - `cd app && npm run build` exits 0.
  </acceptance_criteria>
  <done>All 8 non-nested render sites go through the shared shell, the three legacy record modals are deleted, and nothing references them.</done>
</task>

</tasks>

<verification>
1. `cd app && npm run build` exits 0.
2. All 8 non-nested render sites are migrated: `grep -rn "<SidePanel" app/src --include='*.jsx' | wc -l` returns 8.
3. Body render counts across the app: 4 `<ContactPanelBody` at top-level sites plus 1 inside `ApplicationPanelBody` (the D-05 swap) = 5 total; 3 `<ApplicationPanelBody`; 1 `<JobPanelBody`.
4. `git diff --exit-code app/src/components/ui/Modal.jsx` succeeds — the out-of-scope dialog primitive is still untouched (D-04), and `LogInteractionModal`, `QuickAddContactModal`, and `AddToCalendarModal` still import it.
5. `git status --porcelain` shows exactly the 6 modified files and the 3 deletions listed in `files_modified`.
</verification>

<success_criteria>
- Contact, application, and job records all open through one shared side-panel component (PANEL-01 satisfied).
- Every call site's prop set is byte-identical to before, including the three pre-existing omissions at Calendar and the one at Coverage (PANEL-02's no-silent-behavior-change side).
- The three legacy record modal files are deleted with zero dangling imports or renders.
- Production build passes.
</success_criteria>

<output>
Create `.planning/phases/04-shared-record-side-panel/04-04-SUMMARY.md` when done, including a table of all 9 render sites with their before/after prop counts, and a "pre-existing findings, not fixed this phase" section for anything noticed at the reduced-prop Calendar sites.
</output>
