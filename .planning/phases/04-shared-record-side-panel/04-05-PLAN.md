---
phase: 04-shared-record-side-panel
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified: []
autonomous: true
requirements: [PANEL-01, PANEL-02]

must_haves:
  truths:
    - "Every deterministic gate from plans 04-01 through 04-04 is green against the fully merged tree, not just against each plan's own branch state."
    - "The phase's changeset is exactly the expected 12-file set (3 new components, 1 new hook, 6 re-pointed call sites, 3 deletions) with no unexplained diffs."
    - "A dev server is running with a written, ordered manual checklist covering all three record types, both create modes, the in-place swap, the responsive axis flip, dialog stacking, and the public demo route."
  artifacts:
    - ".planning/phases/04-shared-record-side-panel/04-05-SUMMARY.md — sweep results plus the staged manual checklist"
  key_links:
    - "The sweep re-runs gates against the merged tree because wave-parallel plans each verified only their own slice"
    - "The manual checklist covers the two demo-reachable record types (contact, application) per 04-RESEARCH.md Pitfall 1"
---

<!-- planner-discipline-allow: contactRelationships -->
<!-- planner-discipline-allow: onRefreshRelationships -->
<objective>
Re-verify every deterministic gate from this phase against the fully merged tree, audit the changeset, and stage the human verification pass.

Purpose: Plans 04-01 through 04-03 ran in parallel waves and each verified only its own files. This plan is the only place the whole phase is checked as one integrated tree, matching the end-of-phase sweep pattern Phases 1, 2, and 3 all used. `workflow.human_verify_mode` is `end-of-phase`, so the manual checklist is staged here rather than blocking mid-phase.

Output: `.planning/phases/04-shared-record-side-panel/04-05-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-record-side-panel/04-CONTEXT.md
@.planning/phases/04-shared-record-side-panel/04-01-SUMMARY.md
@.planning/phases/04-shared-record-side-panel/04-02-SUMMARY.md
@.planning/phases/04-shared-record-side-panel/04-03-SUMMARY.md
@.planning/phases/04-shared-record-side-panel/04-04-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase-level symbol list, for the record. This plan adds no source symbols.

| Symbol | File | Kind | Plan |
|--------|------|------|------|
| `useMediaQuery(query)` | `app/src/lib/useMediaQuery.js` | default-export hook | 04-01 |
| `SidePanel` | `app/src/components/ui/SidePanel.jsx` | default-export component — props `{ open = true, onClose, children, className }` | 04-01 |
| `JobPanelBody` | `app/src/components/panels/JobPanelBody.jsx` | default-export component — 8 props | 04-01 |
| `ContactPanelBody` | `app/src/components/panels/ContactPanelBody.jsx` | default-export component — 9 props, `onBack` new this phase | 04-02 |
| `ApplicationPanelBody` | `app/src/components/panels/ApplicationPanelBody.jsx` | default-export component — 12 props; owns the D-05 in-place swap | 04-03 |
| `NetworkAtCompany` | `app/src/components/panels/ApplicationPanelBody.jsx` | file-local, unexported | 04-03 |
| 6 re-pointed call-site files, 3 deleted legacy modals | — | — | 04-04 |

<tasks>

<task type="auto">
  <name>Task 1: Deterministic gate re-run, changeset audit, and production build</name>
  <files>(read-only — this task modifies no source files)</files>
  <read_first>
    - `.planning/phases/04-shared-record-side-panel/04-01-SUMMARY.md` through `04-04-SUMMARY.md` — collect every gate each plan reported green, plus any deviations or pre-existing findings recorded along the way.
    - `.planning/ROADMAP.md` §"Phase 4" — the two success criteria this sweep is evidence for.
  </read_first>
  <action>
Run the integrated sweep against the merged tree. Fix nothing silently: if a gate fails, record the failure, fix it, and note the fix in the SUMMARY.

1. **Determine the pre-phase base commit.** Find the last commit before this phase's first source change (the commit `.planning/phases/04-shared-record-side-panel/04-01-PLAN.md` was created at, or its parent if plan creation and execution share a commit). Phase 2's sweep learned this matters: diffing against the wrong base silently folds an unrelated commit into the "expected changeset" and hides real drift. Record the resolved base SHA in the SUMMARY.

2. **Changeset audit.** Run `git diff --stat <base>..HEAD -- app/` and confirm it lists exactly these 12 paths and nothing else:
   - added: `app/src/lib/useMediaQuery.js`, `app/src/components/ui/SidePanel.jsx`, `app/src/components/panels/JobPanelBody.jsx`, `app/src/components/panels/ContactPanelBody.jsx`, `app/src/components/panels/ApplicationPanelBody.jsx`
   - modified: `app/src/App.jsx`, `app/src/components/TodayTab.jsx`, `app/src/components/CalendarTab.jsx`, `app/src/components/ReferralCoverageTab.jsx`, `app/src/components/PipelineTab.jsx`, `app/src/components/jobBoards/RepoJobsView.jsx`
   - deleted: `app/src/components/ContactDetailModal.jsx`, `app/src/components/ApplicationDetailModal.jsx`, `app/src/components/jobBoards/JobDetailModal.jsx`
   (12 paths of change across 14 entries counting the 3 deletions and 5 additions.) Any additional path is unexplained drift — investigate and report it, do not quietly accept it.

3. **Re-run every deterministic gate** from the four prior plans against the merged tree, in one pass. At minimum:
   - shell is type-agnostic: exactly 4 imports in `SidePanel.jsx`, one `{children}`, one `z-50`, one `x: '100%'`, one `y: '100%'`
   - animation library import is still `framer-motion` repo-wide, with zero bare-`motion` package imports introduced this phase
   - `git diff <base>..HEAD -- app/src/components/ui/Modal.jsx` is empty (D-04)
   - 8 `<SidePanel` renders across `app/src`
   - 5 `<ContactPanelBody` renders (4 top-level sites plus the one D-05 swap), 3 `<ApplicationPanelBody`, 1 `<JobPanelBody`
   - zero legacy-modal imports and zero legacy-modal JSX renders repo-wide
   - Calendar's preserved omissions: `contactRelationships`, `onRefreshRelationships`, and `onFindPeople` each appear 0 times in `CalendarTab.jsx`
   - Coverage's preserved omission: `onRefreshRelationships` appears 0 times in `ReferralCoverageTab.jsx`
   - the interaction-logging dialog is portaled from `ContactPanelBody` (`createPortal` twice, `document.body` once)

4. **Production build:** `cd app && npm run build` must exit 0.

5. **Demo-route reachability check (static, not visual):** confirm `DEMO_NAV_ITEMS` in `App.jsx` still contains the today, network, and pipeline destinations, and that none of the three panel bodies or the shell added an auth gate that would block them for an anonymous visitor — `grep -rn "useAuth\|requireUser\|session" app/src/components/panels/ app/src/components/ui/SidePanel.jsx` should surface only `ContactPanelBody`'s pre-existing `useAuth` usage for `profile?.school`, which already ran in demo mode before this phase. Any new auth dependency is a demo regression (04-RESEARCH.md Pitfall 1).

Record every gate with its actual command output in the SUMMARY, one row per gate, PASS or FAIL.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && test $(grep -rn "<SidePanel" app/src --include='*.jsx' | wc -l) -eq 8 && test $(grep -rn "<ContactPanelBody" app/src --include='*.jsx' | wc -l) -eq 5 && test $(grep -rn "<ApplicationPanelBody" app/src --include='*.jsx' | wc -l) -eq 3 && test $(grep -rn "<JobPanelBody" app/src --include='*.jsx' | wc -l) -eq 1 && test $(grep -rEn "<(Contact|Application|Job)DetailModal" app/src --include='*.jsx' | wc -l) -eq 0 && test $(grep -rEn "from '.*(Contact|Application|Job)DetailModal\.jsx'" app/src --include='*.jsx' | wc -l) -eq 0 && test $(grep -c "^import" app/src/components/ui/SidePanel.jsx) -eq 4 && test $(grep -c "from 'framer-motion'" app/src/components/ui/SidePanel.jsx) -eq 1 && test $(grep -rc "contactRelationships" app/src/components/CalendarTab.jsx) -eq 0 && test $(grep -rc "onRefreshRelationships" app/src/components/CalendarTab.jsx) -eq 0 && test $(grep -rc "onRefreshRelationships" app/src/components/ReferralCoverageTab.jsx) -eq 0 && test $(grep -c "createPortal" app/src/components/panels/ContactPanelBody.jsx) -eq 2 && cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The resolved pre-phase base SHA is recorded in the SUMMARY, along with the reasoning for choosing it.
    - `git diff --stat <base>..HEAD -- app/` lists exactly the 14 expected entries (5 added, 6 modified, 3 deleted) and nothing more; any extra path is reported explicitly.
    - Render counts across `app/src`: 8 shells, 5 contact bodies, 3 application bodies, 1 job body.
    - Zero legacy-modal imports and zero legacy-modal JSX renders repo-wide.
    - `git diff <base>..HEAD -- app/src/components/ui/Modal.jsx` produces no output (D-04 held for the whole phase).
    - `SidePanel.jsx` still has exactly 4 imports and still imports from `framer-motion`.
    - All four preserved prop omissions still hold (three in `CalendarTab.jsx`, one in `ReferralCoverageTab.jsx`).
    - `cd app && npm run build` exits 0.
    - No new auth dependency exists in the panels directory or the shell beyond `ContactPanelBody`'s pre-existing profile lookup.
    - Every gate is recorded in the SUMMARY as a row with its command and PASS/FAIL.
  </acceptance_criteria>
  <done>Every deterministic gate is green against the merged tree, and the changeset is exactly the expected 14 entries.</done>
</task>

<task type="auto">
  <name>Task 2: Stage the end-of-phase human verification pass</name>
  <files>(no source files — writes the checklist into the SUMMARY)</files>
  <read_first>
    - `.planning/phases/04-shared-record-side-panel/04-01-SUMMARY.md` through `04-04-SUMMARY.md` — the three capability-parity checklists (13 job rows, 18 contact rows, 18 application rows) that the manual pass spot-checks rather than re-enumerates.
    - `.planning/ROADMAP.md` §"Phase 4" — the two success criteria the checklist must produce evidence for.
  </read_first>
  <action>
Start the dev server (`cd app && npm run dev`, serving on localhost:3001) and leave it running, then write the ordered manual checklist into the SUMMARY. Do not attempt to perform the visual judgment steps automatically — `workflow.human_verify_mode` is `end-of-phase`, so this task stages the pass for a human, exactly as Phases 1, 2, and 3 did.

The checklist must be ordered, numbered, and each step must state the exact navigation path and the expected result. Cover at minimum:

1. **Desktop slide-over (PANEL-01, D-01/D-02):** at a window width ≥ 768px, open a contact from Network. Expect the panel to slide in from the right edge over a dimmed backdrop, not to appear centered and not to jump in without motion. Repeat for an application from Pipeline and a job from Job Boards, confirming the same motion and the same panel geometry each time — that identical-pattern-across-three-record-types observation is Phase 4 success criterion 1.
2. **Mobile bottom sheet (D-01):** narrow the window below 768px (or use devtools device mode) and reopen a contact. Expect a bottom sheet sliding up from the bottom. Then, with the panel closed, drag the window back across 768px and reopen — expect the axis to have flipped without a reload, which is the `matchMedia` change listener working.
3. **Close semantics (D-02):** Escape closes the panel; clicking the dimmed backdrop closes it; clicking inside the panel does not.
4. **Contact capabilities (PANEL-02):** on a contact opened from Network, spot-check editing status, urgency, referred-by, follow-up date, the alum checkbox and its two-way sync with the affinity chips, a life-domain chip, the schedule block, and notes — then save and confirm the change persisted after reopening. Add and then remove a relationship, confirming the panel stays open through both. Confirm the history list and its count render.
5. **Contact create mode:** the add-a-contact entry point from Network shows the reduced field set and the add-contact button copy, and actually creates a contact. Repeat from Grow → Coverage's add-contact-for-a-company affordance, confirming the company field is pre-seeded.
6. **Application capabilities:** on an application opened from Pipeline, change the stage to a terminal one and confirm Closed Date auto-fills with today, then change back out and confirm it clears. Save stage/dates and confirm persistence. Confirm the warm-path dossier renders with its count and per-person rows, the triage bucket row appears for an untriaged application and toggles off when the active bucket is clicked, and the fit analysis runs.
7. **Application create mode:** the add-an-application entry point in Pipeline opens the create form with the paste-a-link auto-fill box; paste a real posting URL, confirm auto-fill populates fields, and confirm a duplicate company/role produces the duplicate warning.
8. **In-place record swap (D-05):** from an application panel with at least one known person at the company, click a person in the dossier. Expect the panel's content to swap in place to that contact with a back affordance in the header — not a second stacked panel. Click back and confirm the application returns with its previously entered form state intact (including any unsaved stage/date edits). Confirm the close button from the swapped-in contact dismisses the whole panel rather than revealing the application.
9. **Dialog stacking (04-RESEARCH.md Pitfall 5):** from inside a contact panel, open the interaction-logging dialog. Expect it to render centered over the viewport and above the panel — not clipped to the panel's box and not behind it. Close it and confirm the panel is still open with its state intact.
10. **Job panel:** open a job from Job Boards and confirm the deadline badge, the recheck affordance, the apply link, the triage buckets, and the fit analysis all behave as before.
11. **Reduced-prop Calendar sites (deliberate, not a bug):** open a contact and an application from Calendar. Expect them to render in the shared panel with the same reduced surface they had before this phase — empty history, non-functional relationship add, no dossier, no fit analysis. This is a preserved pre-existing condition; note it, do not file it as a regression.
12. **Public demo route (04-RESEARCH.md Pitfall 1):** load `/demo` and open a contact from Network (Table and Cards views) and an application from Today and from Pipeline. Expect the shared panel to open and edits to apply against the in-memory demo data with no errors. Confirm Job Boards is still absent from the demo nav, so no job panel is reachable there.

Also record, in the SUMMARY, any pre-existing findings surfaced during the phase (particularly at the reduced-prop Calendar sites) under a clearly separated "pre-existing, not fixed this phase" heading, so verification does not mistake them for new regressions.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && sleep 3 && curl -sf -o /dev/null -w "%{http_code}\n" http://localhost:3001/ && curl -sf -o /dev/null -w "%{http_code}\n" http://localhost:3001/demo && grep -c "^[0-9]\+\." .planning/phases/04-shared-record-side-panel/04-05-SUMMARY.md</automated>
    <human-check>
Work through the 12-step checklist written into `.planning/phases/04-shared-record-side-panel/04-05-SUMMARY.md` against the running dev server at http://localhost:3001. Reply `approved` if every step matches its expected result, or describe the first step that does not.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The dev server responds 200 on both `/` and `/demo`.
    - `04-05-SUMMARY.md` contains a numbered checklist of at least 12 steps, each naming an exact navigation path and an expected result — not a vague "check that it works".
    - The checklist explicitly covers: the desktop right-edge slide for all three record types, the mobile bottom-sheet axis and the live flip across the breakpoint, Escape and backdrop close, contact edit and create, application edit and create with the terminal-stage date sync, the D-05 in-place swap including form-state survival across back, dialog stacking above the panel, the job panel, the deliberately reduced Calendar sites, and the `/demo` route for contact and application.
    - A separate "pre-existing, not fixed this phase" section exists (even if empty, stated explicitly).
    - The checklist is staged for a human, not marked complete by the executor.
  </acceptance_criteria>
  <done>The dev server is live and a specific, ordered 12-step verification checklist is staged in the SUMMARY for the end-of-phase human pass.</done>
</task>

</tasks>

<verification>
1. All deterministic gates from plans 04-01 through 04-04 re-run green against the merged tree.
2. `cd app && npm run build` exits 0.
3. The phase changeset is exactly 5 added, 6 modified, 3 deleted paths under `app/`.
4. `app/src/components/ui/Modal.jsx` is unchanged across the entire phase.
5. The dev server serves both `/` and `/demo` with a staged manual checklist awaiting human sign-off.
</verification>

<success_criteria>
- Phase 4 success criterion 1 (one consistent panel component for contact, application, and job) has both structural evidence (8 shells, 1 shell component, 3 bodies, 0 legacy modals) and a staged visual check.
- Phase 4 success criterion 2 (no feature regression) has the three capability-parity checklists from 04-01/02/03 plus a manual spot-check plan covering both create modes and the swap.
- No unexplained file appears in the phase changeset.
</success_criteria>

<output>
Create `.planning/phases/04-shared-record-side-panel/04-05-SUMMARY.md` when done, containing: the resolved base SHA, the changeset audit table, one row per deterministic gate with its command and result, the numbered 12-step manual checklist, and the "pre-existing, not fixed this phase" section.
</output>
