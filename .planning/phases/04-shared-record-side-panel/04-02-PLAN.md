---
phase: 04-shared-record-side-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/panels/ContactPanelBody.jsx
autonomous: true
requirements: [PANEL-01, PANEL-02]

must_haves:
  truths:
    - "Every field the contact modal could edit is still editable from the new contact body — name, company, role, email, linkedin, source, status, urgency, referredById, referralStatus, followUpDate, isUMichAlum, affinity, lifeDomain, wantsToSchedule/scheduleBy/scheduleNote, whatTheyDid, notes."
    - "The create-a-contact (isNew) branch still exists with its reduced field set and its '+ Add Contact' button copy."
    - "Relationship add/remove, the Met quick-log, interaction history, the outreach draft affordance, and the interaction-logging dialog all still work from inside the body."
    - "When the body is handed an onBack callback it renders a back affordance in its own sticky header (D-05); when it is not, the header looks exactly as it does today."
  artifacts:
    - "app/src/components/panels/ContactPanelBody.jsx — contact body ported from the legacy contact modal with one new optional onBack prop"
  key_links:
    - "ContactPanelBody imports addContact/updateContact/archiveContact/addContactRelationship/deleteContactRelationship from ../../db.js — the same demo-mode-aware data layer, untouched"
    - "ContactPanelBody renders the interaction-logging dialog through createPortal(document.body) so it escapes the panel's animated transform containing block"
    - "ContactPanelBody keeps ChipToggleGroup, DraftPanel, useAuth, quickLog.logMetWithContact and the shared.jsx option constants"
---

<!-- planner-discipline-allow: fixed inset-0 -->
<!-- planner-discipline-allow: md:max-w-lg -->
<objective>
Port the contact record body out of its hand-rolled modal into `panels/ContactPanelBody.jsx`, adding the one new capability D-05 requires (an optional back affordance) and nothing else.

Purpose: This is the richest of the three record surfaces — 19 form keys, relationship management, embedded history, two embedded sub-surfaces. PANEL-02's zero-regression bar lives or dies here, so the port is field-for-field, not a rewrite.

Output: `app/src/components/panels/ContactPanelBody.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-record-side-panel/04-CONTEXT.md
@.planning/phases/04-shared-record-side-panel/04-PATTERNS.md
</context>

## Artifacts this phase produces

Full phase-level symbol list. This plan produces the row marked ★.

| Symbol | File | Kind | Plan |
|--------|------|------|------|
| `useMediaQuery(query)` | `app/src/lib/useMediaQuery.js` | default-export hook | 04-01 |
| `SidePanel` | `app/src/components/ui/SidePanel.jsx` | default-export component — props `{ open = true, onClose, children, className }` | 04-01 |
| `JobPanelBody` | `app/src/components/panels/JobPanelBody.jsx` | default-export component | 04-01 |
| ★ `ContactPanelBody` | `app/src/components/panels/ContactPanelBody.jsx` | default-export component — props `{ contact, contacts, interactions, contactRelationships = [], onClose, onSaved, onRefreshRelationships, initial = {}, onBack }` — `onBack` is the one NEW prop this phase adds | 04-02 |
| `ApplicationPanelBody` | `app/src/components/panels/ApplicationPanelBody.jsx` | default-export component | 04-03 |
| `NetworkAtCompany` | `app/src/components/panels/ApplicationPanelBody.jsx` | file-local component, moves with its parent | 04-03 |
| *(no shared navigation hook)* | — | D-05's swap is one `useState` inside `ApplicationPanelBody`; 04-03 documents why | 04-03 |
| removed: the three legacy record modal files | — | deletions after all 9 render sites are re-pointed | 04-04 |

<tasks>

<task type="auto">
  <name>Task 1: Port the contact body into panels/ContactPanelBody.jsx</name>
  <files>app/src/components/panels/ContactPanelBody.jsx</files>
  <read_first>
    - `app/src/components/ContactDetailModal.jsx` (all 376 lines) — the source being ported. This file IS the spec. 04-RESEARCH.md's first anti-pattern is "rewriting form field logic from memory instead of porting verbatim"; the two-way alum/affinity sync in particular is non-obvious and must move across character-for-character.
    - `.planning/phases/04-shared-record-side-panel/04-PATTERNS.md` section for the contact body — the seven numbered patterns to preserve, with exact excerpts for the state initializer, the affinity sync pair, and the save handler.
    - `app/src/components/ui/ChipToggleGroup.jsx` — used for both the life-domain and affinity chip groups; confirm the prop names `options`/`value`/`onToggle` at the new import depth.
    - `app/src/components/ui/SidePanel.jsx` — created in 04-01; this body becomes its children in 04-04.
  </read_first>
  <action>
Create `app/src/components/panels/ContactPanelBody.jsx`, ported from `app/src/components/ContactDetailModal.jsx`.

Do not delete or edit the source file in this plan — Plan 04-04 removes it after all four of its render sites are re-pointed. Both files coexist until then, which is expected and must not be "cleaned up" early.

Port rules:
1. Rename the function to `ContactPanelBody` and keep the prop contract identical apart from one addition: `export default function ContactPanelBody({ contact, contacts, interactions, contactRelationships = [], onClose, onSaved, onRefreshRelationships, initial = {}, onBack })`. `onBack` is optional and is the only new prop. Do not reorder or rename the existing eight.
2. Delete exactly two wrapper elements: the outer `div` carrying `fixed inset-0 bg-ink-900/40 z-50 ...` and the inner `div` carrying `bg-white w-full md:max-w-lg rounded-t-2xl ...`. The shell owns both now. Everything between them moves up into a fragment (`<>...</>`) root.
3. Fix import depth only — the file moves one directory deeper. `'../db.js'` becomes `'../../db.js'`; `'../lib/quickLog.js'` becomes `'../../lib/quickLog.js'`; `'../shared.jsx'` becomes `'../../shared.jsx'`; `'../lib/AuthContext.jsx'` becomes `'../../lib/AuthContext.jsx'`; `'./ui/ChipToggleGroup.jsx'` becomes `'../ui/ChipToggleGroup.jsx'`; `'./LogInteractionModal.jsx'` becomes `'../LogInteractionModal.jsx'`; `'./DraftPanel.jsx'` becomes `'../DraftPanel.jsx'`. Nothing else about the imports changes.
4. Every `useState`, every handler (`set`, `toggleUMichAlum`, `toggleAffinity`, `toggleLifeDomain`, `addRelationship`, `removeRelationship`, `save`, `met`, `del`), every derived local (`schoolLabel`, `isNew`, `history`, `referralOptions`, `nameOf`, `myRelationships`), and the two local render helpers (`field`, `select`) move over unchanged. Keep the existing explanatory comments above `toggleUMichAlum` and above `addRelationship` — both document real, non-obvious behavior.
5. Do NOT swap the hand-rolled `field`/`select` helpers for `ui/Input.jsx`/`ui/Select.jsx`. 04-RESEARCH.md flags that as a discretionary improvement; it is explicitly declined for this phase because the two carry different border tokens (`border-ink-200` here versus `border-ink-100` in the primitives) and PANEL-02 is a zero-regression consolidation, not a restyle. Phase 7 owns visual convergence.
6. Back affordance (D-05): in the sticky header, wrap the existing `h2` in a `div` with `className="flex items-center gap-2 min-w-0"` and, immediately before the `h2`, render — only when `onBack` is truthy — a button with `onClick={onBack}`, `aria-label="Back"`, `className="shrink-0 w-7 h-7 rounded-full bg-ink-100 hover:bg-ink-200 flex items-center justify-center text-ink-500"`, containing `ArrowLeft` at `size={14}` imported from `lucide-react` (add `ArrowLeft` to the existing `lucide-react` import line that already brings in `Handshake`). Add `truncate` to the `h2` className. When `onBack` is undefined the header must render exactly as it does today. The close button stays on the right, wired to `onClose`, unchanged.
7. Nested dialog containing-block fix: the interaction-logging dialog is rendered from inside this body, and this body now lives inside a `motion.div` that carries a transform during the panel's open and close animation. A transformed ancestor becomes the containing block for `position: fixed` descendants, so that dialog could end up positioned relative to the panel rather than the viewport. Render it through a portal to sidestep this entirely: add `import { createPortal } from 'react-dom'` and change the trailing conditional render at the end of the component to `{logOpen && createPortal(<LogInteractionModal ... />, document.body)}`, keeping all four of its props (`contacts`, `contact`, `onClose`, `onSaved`) byte-identical. Leave `DraftPanel` alone — it renders inline in normal flow with no fixed positioning.

Add a 2-line provenance head comment naming the source file it was ported from, and stating that the only behavioral additions are the optional back affordance and the portal.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && F=app/src/components/panels/ContactPanelBody.jsx && test -f $F && test $(grep -c "fixed inset-0" $F) -eq 0 && test $(grep -c "md:max-w-lg" $F) -eq 0 && test $(grep -c "export default function ContactPanelBody" $F) -eq 1 && test $(grep -c "createPortal" $F) -eq 2 && test $(grep -c "document.body" $F) -eq 1 && test $(grep -c "ArrowLeft" $F) -eq 2 && for t in toggleUMichAlum toggleAffinity toggleLifeDomain addRelationship removeRelationship logMetWithContact archiveContact addContactRelationship deleteContactRelationship DraftPanel ChipToggleGroup referralOptions myRelationships wantsToSchedule scheduleBy scheduleNote whatTheyDid followUpDate referralStatus lifeDomain isUMichAlum affinityOptionsFor LIFE_DOMAIN_OPTIONS RELATIONSHIP_TYPES REFERRAL_STATUS_OPTIONS; do test $(grep -c "$t" $F) -ge 1 || { echo "MISSING $t"; exit 1; }; done && git diff --exit-code app/src/components/ContactDetailModal.jsx && cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    Structural:
    - `app/src/components/panels/ContactPanelBody.jsx` exists; zero `fixed inset-0` matches and zero `md:max-w-lg` matches.
    - `git diff --exit-code app/src/components/ContactDetailModal.jsx` succeeds — the source is untouched this plan.
    - `createPortal` appears exactly twice (import plus the single call site) and `document.body` exactly once.
    - `ArrowLeft` appears exactly twice (the icon import plus the back button).
    - `cd app && npm run build` exits 0.

    Capability parity (PANEL-02 — each item exists in the source modal and must be present in the port; verify by reading both files side by side and record the checklist in the SUMMARY):
    1. Form state initializer covers all 19 keys — `name`, `company`, `role`, `email`, `linkedin`, `source`, `status`, `urgency`, `referredById`, `referralStatus`, `whatTheyDid`, `notes`, `followUpDate`, `isUMichAlum`, `affinity`, `lifeDomain`, `wantsToSchedule`, `scheduleBy`, `scheduleNote` — with the same defaults, including `initial.name`/`initial.company` seeding and the `.slice(0, 10)` date truncation on `followUpDate` and `scheduleBy`.
    2. The `isNew = !contact` branch: the six always-visible fields render in both modes, and the Status/Urgency/Referred By/Referral Status/Follow-Up Date grid, Relationships block, alum-plus-affinity block, schedule block, whatTheyDid/notes block, History block, draft affordance, and Delete button all stay gated behind `!isNew` exactly as before.
    3. Primary button copy switches between `+ Add Contact` (isNew) and `Save Changes`, with a `Saving...` in-flight state.
    4. `save()` calls `addContact` with exactly `{ name, company, role, email }` on the isNew path, and `updateContact(contact.id, {...})` with all mapped fields on the edit path, including the `|| null` coercions on `role`, `source`, `referredById`, `followUpDate`, `scheduleBy`.
    5. The name-required validation message still blocks save when the name is blank.
    6. `toggleUMichAlum` and `toggleAffinity` still keep the checkbox and the affinity chip array in two-way sync through `schoolLabel`, which is still derived from `profile?.school` and never hardcoded.
    7. `toggleLifeDomain` still toggles values into and out of `form.lifeDomain`.
    8. Relationships block lists `myRelationships` with the correct outgoing/incoming phrasing via `nameOf`, a per-row remove calling `deleteContactRelationship` then `onRefreshRelationships`, and the type-select plus contact-select plus add row calling `addContactRelationship` then `onRefreshRelationships` — never `onSaved`, because the panel must stay open for batch edits.
    9. The Referred By dropdown is populated from `referralOptions` (all contacts except this one) with the `Name @ Company` label format; the Relationships target select uses the same list.
    10. Alum checkbox plus `ChipToggleGroup` over `affinityOptionsFor(profile)`; life-domain `ChipToggleGroup` over `LIFE_DOMAIN_OPTIONS`.
    11. Schedule block: the `wantsToSchedule` checkbox reveals the `scheduleBy` date input and the `scheduleNote` text input.
    12. History block: `interactions` filtered to `contact?.id`, sorted newest-first, each row showing the type Badge, direction, formatted date, and summary, with the count in the section header.
    13. The Met quick-log button calls `logMetWithContact(contact)` then `onSaved()`, with its in-flight state.
    14. The log button opens the interaction-logging dialog with `contacts`, `contact`, an `onClose` that clears the flag, and an `onSaved` that clears the flag and calls `onSaved()`.
    15. The draft-outreach affordance renders when `history.length <= 1` and toggles in `DraftPanel` with `kind="cold_open"`.
    16. The delete action calls `archiveContact(contact.id)` behind the existing `confirm()` gate, then `onSaved()`, with a `Deleting...` in-flight state.
    17. The error banner surfaces thrown messages from every handler.
    18. The back affordance renders only when `onBack` is supplied; close-button behavior is unchanged in both cases.
  </acceptance_criteria>
  <done>`ContactPanelBody` renders the full contact record surface as plain children with all 18 capabilities intact plus an optional back affordance, and the app builds.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical port audit against the source modal</name>
  <files>app/src/components/panels/ContactPanelBody.jsx</files>
  <read_first>
    - `app/src/components/ContactDetailModal.jsx` — re-examined here specifically as a diff target, not re-read line by line.
    - `app/src/components/panels/ContactPanelBody.jsx` — the port produced by Task 1.
  </read_first>
  <action>
Audit the port mechanically rather than impressionistically, and repair any drift found.

1. Produce a normalized diff so only real divergences surface. Normalize the component name out of the comparison first:
   `diff <(sed 's/ContactDetailModal/ContactPanelBody/g' app/src/components/ContactDetailModal.jsx) app/src/components/panels/ContactPanelBody.jsx`
2. Walk every hunk and classify it as exactly one of the five sanctioned change categories: (a) the two removed wrapper elements plus the fragment root, (b) import-depth rewrites, (c) the `onBack` prop with its header button, its icon import, and the header wrapper div plus `truncate`, (d) the portal import and its single call site, (e) the provenance head comment.
3. Any hunk that falls outside those five categories is unsanctioned drift — restore the source behavior for it.
4. Record the per-category hunk counts in the SUMMARY.

Do not fix unrelated things while in here. Pre-existing quirks in the source — for example the confirm-dialog copy that still names the legacy workspace, or the emoji-prefixed status option values — are carried forward verbatim. 04-RESEARCH.md Pitfall 4 applies: behavior expansion is out of scope for a zero-regression consolidation, and any such change would be an unreviewed surprise at four different call sites.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && diff <(sed 's/ContactDetailModal/ContactPanelBody/g' app/src/components/ContactDetailModal.jsx) app/src/components/panels/ContactPanelBody.jsx > /tmp/gsd-contact-port.diff; echo "changed lines: $(grep -c '^[<>]' /tmp/gsd-contact-port.diff)"; test $(grep -c '^[<>]' /tmp/gsd-contact-port.diff) -le 60 && cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The normalized diff between source and port contains at most 60 changed lines (`grep -c '^[<>]'` on the diff output), bounding the port to the five sanctioned categories — a larger diff means the body was rewritten rather than moved.
    - Every hunk in that diff is classified into one of the five sanctioned categories in the SUMMARY, with counts.
    - Zero hunks touch the bodies of `save`, `met`, `del`, `addRelationship`, `removeRelationship`, `toggleUMichAlum`, `toggleAffinity`, or `toggleLifeDomain`: `grep -A2 -B2 '^[<>].*\(function save\|function met\|function del\|toggleUMichAlum\|toggleAffinity\|toggleLifeDomain\|addRelationship\|removeRelationship\)' /tmp/gsd-contact-port.diff` returns no output.
    - `cd app && npm run build` exits 0.
  </acceptance_criteria>
  <done>The port is provably a move, not a rewrite: the normalized diff is bounded and every hunk is accounted for by a sanctioned change category.</done>
</task>

</tasks>

<verification>
1. `cd app && npm run build` exits 0.
2. `git diff --exit-code app/src/components/ContactDetailModal.jsx` succeeds — the legacy modal is untouched until 04-04.
3. `git diff --name-only` lists only `app/src/components/panels/ContactPanelBody.jsx`.
4. No call site changed in this plan.
</verification>

<success_criteria>
- `ContactPanelBody` exists with the exact 9-prop signature from the artifacts table.
- All 18 contact capabilities survive, verified item by item and recorded in the SUMMARY.
- The normalized source-to-port diff is bounded to at most 60 changed lines, all classified.
- Production build passes.
</success_criteria>

<output>
Create `.planning/phases/04-shared-record-side-panel/04-02-SUMMARY.md` when done, including the 18-row contact capability-parity checklist and the per-category diff-hunk counts.
</output>
