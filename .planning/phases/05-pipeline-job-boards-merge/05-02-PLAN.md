---
phase: 05-pipeline-job-boards-merge
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03]

must_haves:
  truths:
    - "Every deterministic gate from plan 05-01's three tasks is green against the fully merged tree, not just against the state each task committed on."
    - "The phase changeset is exactly the expected seven paths — two renames, one rewrite, three edits — with no unexplained diffs and, critically, no diff anywhere under the Job Boards subtree beyond the one renamed body."
    - "A dev server is running with a written, ordered manual checklist covering the view switch, every preserved Job Boards capability, the auto-import to attention-feed round trip, the shared side-panel from both views, and the public demo route."
  artifacts:
    - ".planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md — sweep results plus the staged manual checklist"
  key_links:
    - "The sweep proves PIPE-02 and PIPE-03 structurally (an empty diff under the untouched subtrees) before asking a human to prove them behaviourally"
    - "The manual checklist is the end-of-phase human verification pass required by workflow.human_verify_mode"
---

<!-- planner-discipline-allow: PipelineTab -->
<!-- planner-discipline-allow: GitHubTab -->
<!-- planner-discipline-allow: github -->
<!-- planner-discipline-allow: JobBoardsView -->

<objective>
Re-verify every deterministic gate from this phase against the fully merged tree, audit the changeset for unrequested drift, and stage the human verification pass.

Purpose: Plan 05-01's three tasks each verified only the slice of the tree that existed when they ran, and two of them passed through deliberate intermediate states (a doubly-reachable Job Boards, a shell rendering before its call site was updated). This plan is the only place the phase is checked as one finished tree. `workflow.human_verify_mode` is `end-of-phase`, so the manual checklist is written and staged here rather than blocking mid-phase, matching the sweep plans Phases 1 through 4 all ended on.

Output: `.planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-job-boards-merge/05-CONTEXT.md
@.planning/phases/05-pipeline-job-boards-merge/05-UI-SPEC.md
@.planning/phases/05-pipeline-job-boards-merge/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase-level symbol list, for the record. This plan adds no source symbols.

| Symbol | File | Kind | Plan |
|--------|------|------|------|
| `ApplicationsView` | `app/src/components/ApplicationsView.jsx` | default-export component, content-identical to the body it renames | 05-01 |
| `JobBoardsView` | `app/src/components/jobBoards/JobBoardsView.jsx` | default-export component, content-identical to the body it renames | 05-01 |
| `PipelineTab` (rewritten) | `app/src/components/PipelineTab.jsx` | default-export view-switch shell | 05-01 |
| `PIPELINE_VIEWS` | `app/src/components/PipelineTab.jsx` | module-local const | 05-01 |
| `DEMO_PIPELINE_VIEWS` | `app/src/components/PipelineTab.jsx` | named export consumed by the demo app root | 05-01 |
| deleted path `app/src/components/jobBoards/GitHubTab.jsx`, removed nav entry | — | rename-away, config deletion | 05-01 |
| `.planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md` | — | sweep record + staged manual checklist | 05-02 |

<tasks>

<task type="auto">
  <name>Task 1: Integrated gate re-run, changeset audit, and production build</name>
  <files>(read-only — this task modifies no source files)</files>
  <read_first>
    - `.planning/phases/05-pipeline-job-boards-merge/05-01-SUMMARY.md` — the gates each task reported green, the measured rename diff-line counts, the resolved pre-phase base SHA, and any deviation or spacing recommendation recorded there.
    - `.planning/ROADMAP.md` §"Phase 5" — the four success criteria this sweep is the evidence for.
    - `.planning/REQUIREMENTS.md` §"Pipeline + Job Boards Merge (PIPE)" — the three requirement statements the checklist in Task 2 must map onto.
  </read_first>
  <action>
Run the integrated sweep against the merged tree. Fix nothing silently: if a gate fails, record the failure, fix it, and record the fix in the SUMMARY.

1. **Resolve the pre-phase base commit.** Find the last commit before this phase's first source change — the commit that created `.planning/phases/05-pipeline-job-boards-merge/05-01-PLAN.md`, or its parent if plan creation and execution landed together. Phase 2's sweep learned this the hard way: diffing against the wrong base folds an unrelated commit into the expected changeset and hides real drift. Cross-check against the SHA recorded in `05-01-SUMMARY.md` and record the resolved SHA in this plan's SUMMARY.

2. **Changeset audit.** Run `git diff --stat -M <base>..HEAD -- app/` and confirm it lists exactly these paths and nothing else:
   - renamed: `app/src/components/PipelineTab.jsx` to `app/src/components/ApplicationsView.jsx`; `app/src/components/jobBoards/GitHubTab.jsx` to `app/src/components/jobBoards/JobBoardsView.jsx`
   - added: `app/src/components/PipelineTab.jsx` (the shell — git may pair this with the rename above; use `--no-renames` as a cross-check the way Phase 4's sweep did, and reconcile the two views in the SUMMARY rather than trusting either alone)
   - modified: `app/src/App.jsx`, `app/src/components/layout/Sidebar.jsx`, `app/src/components/TodayTab.jsx`
   Anything else in the list is drift and must be explained or reverted.

3. **Preservation proof for PIPE-02 and PIPE-03.** Confirm each of these diffs is empty against the base — this is the structural half of both requirements and is stronger evidence than any click-through:
   - `git diff <base>..HEAD -- app/src/components/jobBoards/` restricted to everything except the renamed body: `RepoJobsView.jsx`, `TrackedBoardsPanel.jsx`, `CalendarView.jsx`, `RepoStats.jsx`, `UserProfileView.jsx`, `PreferencesPanel.jsx`, `JobCard.jsx`, `helpers.js`, `boardsRegistry.js`, `useJobDeadlines.js`, `useJobBlurbs.js`
   - `git diff <base>..HEAD -- app/src/lib/attention.js app/src/db.js app/src/github.js`
   - `git diff <base>..HEAD -- app/src/components/ui/ app/src/components/panels/`
   - `git diff <base>..HEAD -- app/src/lib/icons.js app/src/components/SettingsTab.jsx app/src/index.css`

4. **Rename fidelity re-measurement.** Against the base, confirm both renamed bodies differ from their originals by exactly the declared number of changed lines — 2 diff markers for the Job Boards body, 4 for the Applications body. Use `git show <base>:<old path>` piped into `diff` against the current file, counting lines starting with an angle bracket.

5. **Re-run every deterministic gate from plan 05-01's three tasks** against the merged tree, in one pass. At minimum: the shell's line count, its single state hook, its `views.length > 1` condition, its verbatim switcher class strings, the absence of the amber CTA colour family in the shell, the exported demo views constant, the two-and-only-two references to each body from the shell, the seven-entry nav array, the two Applications-body references in the Today tab, the surviving BYOK provider entry in Settings, and the repo-wide absence of the retired component identifier.

6. **Dangling-route check** (05-RESEARCH.md Pitfall 3, which notes this failure mode is silent — the build passes with a dead nav id). Confirm every id in the nav array has a matching render branch in `AppInner`, and that every `tab ===` comparison in `App.jsx` names an id that still exists in the array. Do this as an explicit id-by-id comparison and record the resulting table in the SUMMARY, not as a single grep.

7. **Production build.** `cd app && npm run build` must exit 0. Record the exit status and any new warnings that were not present before the phase.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && test $(wc -l < app/src/components/PipelineTab.jsx) -lt 65 && test $(grep -c "useState" app/src/components/PipelineTab.jsx) -eq 2 && test $(grep -c "views.length > 1" app/src/components/PipelineTab.jsx) -eq 1 && test $(grep -c "flex border border-ink-200 rounded-full overflow-hidden text-xs font-medium" app/src/components/PipelineTab.jsx) -eq 1 && test $(grep -c "bg-ink-900 text-white" app/src/components/PipelineTab.jsx) -eq 1 && test $(grep -c "accent" app/src/components/PipelineTab.jsx) -eq 0 && test $(grep -c "export const DEMO_PIPELINE_VIEWS" app/src/components/PipelineTab.jsx) -eq 1 && test $(grep -c "ApplicationsView" app/src/components/PipelineTab.jsx) -eq 2 && test $(grep -c "JobBoardsView" app/src/components/PipelineTab.jsx) -eq 2 && test $(wc -l < app/src/components/ApplicationsView.jsx) -eq 230 && test $(wc -l < app/src/components/jobBoards/JobBoardsView.jsx) -eq 98 && test $(grep -c "onImported" app/src/components/ApplicationsView.jsx) -eq 0 && test $(grep -c "JobBoardsView" app/src/App.jsx) -eq 0 && test $(grep -c "DEMO_PIPELINE_VIEWS" app/src/App.jsx) -eq 2 && test $(grep -c "<PipelineTab" app/src/App.jsx) -eq 2 && test $(grep -c "pipeline: activeApps.length" app/src/App.jsx) -eq 2 && test $(grep -c "id: '" app/src/components/layout/Sidebar.jsx) -eq 7 && test $(grep -c "github" app/src/components/layout/Sidebar.jsx) -eq 0 && test $(grep -c "id: 'github'" app/src/components/SettingsTab.jsx) -eq 1 && test $(grep -c "ApplicationsView.jsx" app/src/components/TodayTab.jsx) -eq 2 && test $(grep -rn "GitHubTab" app/src --include='*.jsx' --include='*.js' | wc -l) -eq 0 && test $(grep -rn "tab === 'github'" app/src --include='*.jsx' | wc -l) -eq 0 && cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The resolved pre-phase base SHA is recorded in the SUMMARY, cross-checked against the one 05-01 recorded, with any discrepancy explained.
    - The changeset against that base lists exactly the seven expected paths under `app/`, reconciled between the rename-detecting and rename-suppressing views of the diff, with zero unexplained entries.
    - All four preservation diffs in step 3 are empty. In particular the Job Boards subtree shows changes to the renamed body only — this is the structural proof that multi-board tracking, auto-import, deadline extraction, triage buckets and the calendar and stats views were preserved rather than re-implemented.
    - Both renames measure exactly their declared changed-line counts (2 and 4 diff markers respectively).
    - Every deterministic gate listed in step 5 passes against the merged tree; the combined command above exits 0.
    - The nav-id to render-branch table is recorded in the SUMMARY with every id matched in both directions and no orphan on either side.
    - `cd app && npm run build` exits 0 with no warnings that are new relative to the base.
  </acceptance_criteria>
  <done>The finished tree passes every gate the phase declared, and the untouched-subtree diffs prove the preservation requirements structurally.</done>
</task>

<task type="auto">
  <name>Task 2: Stage the end-of-phase human verification pass</name>
  <files>(read-only — writes only the plan SUMMARY)</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 5" success criteria — the four statements the checklist must produce evidence for.
    - `.planning/phases/05-pipeline-job-boards-merge/05-UI-SPEC.md` §Spacing Scale — the switcher-row spacing note and its explicit verify-not-assume instruction, and §Copywriting Contract's demo-mode note.
    - `.planning/phases/05-pipeline-job-boards-merge/05-01-SUMMARY.md` — any spacing recommendation or deviation raised during implementation that the checklist should call out for a decision.
  </read_first>
  <action>
Stage the manual pass. Do not attempt to self-approve any item that needs a human eye.

1. Start the dev server (`cd app && npm run dev`, expected on port 3001) and leave it running for the human pass. Confirm it compiled and record the URL.

2. Render the merged Pipeline destination and capture a screenshot, then check it against the industrial direction and the UI-SPEC before declaring the visual work done — this is the project-level render-and-screenshot requirement, and 05-RESEARCH.md Open Question 2 explicitly defers the switcher-row spacing decision to this step. Specifically judge: whether the segmented control plus each body's own first element (the duplicates panel on Applications, the tracked-boards panel on Job Boards) read with correct spacing and no doubled chrome; whether the two segment labels and icons are legible at the small switcher scale; and whether the active segment's solid dark fill reads as the current view. If a spacing adjustment is warranted, do not apply it silently — record the exact proposed change in the SUMMARY as a recommendation for a follow-up, since the phase's locked scope is a rename plus wrap with zero restyling of either body.

3. Write an ordered manual checklist into the SUMMARY, each row naming the requirement or success criterion it produces evidence for. Cover at minimum:
   - Pipeline opens on the Applications view every time, including after navigating away to another destination and back (PIPE-01, and the D-01 always-Applications default).
   - The switcher moves between the two views and the previously-active body unmounts rather than hiding (PIPE-01).
   - Job Boards is gone from the sidebar rail and from the mobile bottom bar, with no dead nav button anywhere (the silent failure mode in 05-RESEARCH.md Pitfall 3).
   - Inside the Job Boards view: pulling all tracked boards works, the collapsed single-repo and profile lookup still works, deadline badges still resolve on job cards, triage bucket changes still stick, and the calendar and stats views still render (PIPE-02 — one row per capability, so a partial failure is attributable).
   - Auto-import round trip: pull a board with an unimported listing, then switch to the Applications view without reloading the page and confirm the new row is present with Needs Review triage, then open Today and confirm it appears in the attention feed (PIPE-03, and the shell's no-local-copy discipline from 05-RESEARCH.md Pitfall 1 — a stale snapshot fails exactly here).
   - Clicking an application row on the Applications view and a job card on the Job Boards view each opens the shared side-panel, and each panel's edit controls still save (ROADMAP success criterion 4).
   - Adding a new application from the Applications view still works — that path exercises the panel's create branch.
   - The public demo route: Pipeline is reachable, renders the Applications view, and shows no switcher chrome at all — not a disabled control, not a single-segment pill (D-03).
   - The Pipeline nav badge count is unchanged in meaning from before the merge.
   - Responsive check at mobile width: the switcher row and the bottom bar both render sanely with the reduced nav.

4. Record any pre-existing issue noticed during the pass under a clearly separated "pre-existing findings, not fixed this phase" heading, so it lands in the phase record without expanding scope. Two known carried-forward items are already on the books and should be referenced rather than rediscovered: the stale-day-count and duplicates-panel colour literals flagged in Phase 1, and the calendar feed's missing post-create refresh flagged in Phase 2.
  </action>
  <verify>
    <human-check>
      Dev server reachable on the recorded URL; the merged Pipeline destination renders with the two-segment switcher; the checklist below is present in the SUMMARY, ordered, and each row is annotated with the requirement it evidences.
    </human-check>
    <automated>cd /Users/ethansaba/code/recruiter && test -f .planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md && test $(grep -c "PIPE-01" .planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md) -ge 1 && test $(grep -c "PIPE-02" .planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md) -ge 1 && test $(grep -c "PIPE-03" .planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md) -ge 1 && test $(grep -c "demo" .planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - A dev server is running and its URL is recorded in the SUMMARY.
    - A screenshot of the merged Pipeline destination was taken and judged against the UI-SPEC, with an explicit written verdict on switcher-row spacing (05-RESEARCH.md Open Question 2) — either "no adjustment needed" or a concrete proposed change deferred as a recommendation, never applied silently.
    - The SUMMARY contains an ordered manual checklist covering all ten areas listed in step 3, each row annotated with the requirement or success criterion it evidences, and each Job Boards capability given its own row.
    - The checklist explicitly includes the without-a-page-reload wording on the auto-import round trip, since that is the only step that would catch a stale-props regression in the shell.
    - No checklist item is marked passed on the strength of a build or a grep alone; behavioural items are left for the human pass.
    - Pre-existing findings are recorded under their own heading and separated from this phase's results, with the two known carried-forward items referenced rather than re-raised as new.
  </acceptance_criteria>
  <done>The human verification pass is staged with a running server, a screenshot-backed visual verdict, and a requirement-annotated checklist.</done>
</task>

</tasks>

<verification>
1. Task 1's combined gate command exits 0 against the merged tree.
2. `cd app && npm run build` exits 0.
3. The four preservation diffs in Task 1 step 3 are empty, recorded verbatim in the SUMMARY.
4. The changeset against the pre-phase base is exactly the seven expected paths.
5. `.planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md` exists and contains the sweep results, the nav-id table, the visual verdict, and the staged manual checklist.
</verification>

<success_criteria>
- Every deterministic gate from this phase is green against one integrated tree.
- The preservation requirements are proven structurally by empty diffs before any human clicks anything.
- The changeset contains no drift beyond the seven declared paths.
- The end-of-phase human verification pass is fully staged and ready to run.
</success_criteria>

<output>
Create `.planning/phases/05-pipeline-job-boards-merge/05-02-SUMMARY.md` when done, including: the resolved pre-phase base SHA, the full changeset listing reconciled across both diff views, the four preservation-diff results, the rename fidelity measurements, the nav-id to render-branch table, the build result, the screenshot-backed visual verdict on switcher spacing, the ordered manual checklist annotated by requirement, and a separated pre-existing-findings section.
</output>
