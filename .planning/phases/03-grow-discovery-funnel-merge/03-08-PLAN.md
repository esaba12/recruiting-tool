---
phase: 03-grow-discovery-funnel-merge
plan: 08
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07"]
files_modified: []
autonomous: true
requirements: [GROW-01, GROW-02]

must_haves:
  truths:
    - "The full 9-file Phase 3 changeset builds cleanly with npm run build"
    - "No stale reference to the old Explore tab, or to Network's Coverage/Discover sub-views, remains anywhere in app/src"
    - "A staged, documented manual-verification checklist exists covering GROW-01 and GROW-02's 4 behavior rows from 03-VALIDATION.md, ready for human review"
  artifacts: []
  key_links:
    - "This plan re-verifies every prior plan's own automated gate against the fully-merged tree, not just each plan's own isolated diff"
---

<objective>
Combined end-of-phase regression sweep for Phase 3, mirroring the precedent set by Phase 1's `01-05-PLAN.md` and Phase 2's `02-04-PLAN.md`: re-run every prior plan's automated check against the fully-merged tree (not each plan's isolated diff), confirm the exact expected changeset landed with no unexpected extra edits, and stage the manual human-verification checklist from `03-VALIDATION.md`'s Phase Requirements → Test Map for a human to walk through. This repo has no automated test framework (confirmed by `03-RESEARCH.md`/`03-VALIDATION.md`); per `workflow.human_verify_mode=end-of-phase`, this plan does not block on a live checkpoint — it stages the verification procedure in this plan's own `SUMMARY.md` for a post-hoc human pass.

Purpose: confirm GROW-01 and GROW-02 are genuinely met before the phase is marked complete — that Grow reads as one connected flow, that nothing regressed, and that the old Explore/Coverage/Discover destinations are fully gone (not just individually-verified in isolation).
Output: a re-verified, build-clean merged tree; `.planning/phases/03-grow-discovery-funnel-merge/03-08-SUMMARY.md` documenting the staged manual-verification checklist and dev-server status for human review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grow-discovery-funnel-merge/03-VALIDATION.md
@.planning/phases/03-grow-discovery-funnel-merge/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated regression sweep — build, old-destination absence, changeset confirmation</name>
  <files>app/src/App.jsx, app/src/components/layout/Sidebar.jsx, app/src/lib/icons.js, app/src/components/GrowTab.jsx, app/src/components/ExploreTab.jsx, app/src/components/ReferralCoverageTab.jsx, app/src/components/DiscoverTab.jsx, app/src/components/TodayTab.jsx, app/src/components/ui/Section.jsx</files>
  <read_first>
    .planning/phases/03-grow-discovery-funnel-merge/03-VALIDATION.md §"Phase Requirements → Test Map" row 4 (the old-destinations-gone check this task automates precisely)
    .planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md §"File Classification" table (the exact 9-file expected changeset)
  </read_first>
  <action>
    Run `cd app && npm run build` and confirm it succeeds with zero errors — this is the single strongest automated signal that all 7 prior plans' edits compose correctly (every import resolves, no syntax errors introduced by any plan). Re-run each prior plan's own key structural grep check against the now fully-merged tree (not in isolation): confirm `app/src/components/layout/Sidebar.jsx` has no `id: 'explore'`; confirm `app/src/lib/icons.js` has no `explore:` key and does have `grow: Sprout`; confirm `app/src/App.jsx` has no `tab === 'explore'` and no `key: 'coverage'`/`key: 'discover'` inside `NETWORK_VIEWS`; confirm `app/src/components/ExploreTab.jsx`/`ReferralCoverageTab.jsx`/`DiscoverTab.jsx` each contain their respective `RowCap` application (`<RowCap items={shown}`, `<RowCap items={rows}`, `<RowCap items={recommended}`); confirm no file under `app/src` contains the literal stale phrase "Network → Discover" or "Network → Coverage" (a recursive repo-wide check, catching any stray reference beyond the ones already fixed plan-by-plan). Determine the Phase 3 planning commit (the commit that first added `.planning/phases/03-grow-discovery-funnel-merge/03-01-PLAN.md`) via `git log --diff-filter=A --format=%H -- .planning/phases/03-grow-discovery-funnel-merge/03-01-PLAN.md`, take its parent commit as the pre-phase base, and run `git diff --stat <base>..HEAD -- app/src` to confirm the changeset touches exactly the 9 expected files (`app/src/components/ui/Section.jsx` new, `app/src/components/GrowTab.jsx` new, plus edits to `TodayTab.jsx`, `ExploreTab.jsx`, `ReferralCoverageTab.jsx`, `DiscoverTab.jsx`, `App.jsx`, `layout/Sidebar.jsx`, `lib/icons.js`) with no unexpected extra file changed.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Every one of the 6 structural greps listed above returns the expected result (absence checks return 0 matches, presence checks return ≥1 match).
    - The `git diff --stat` changeset against the pre-phase base touches exactly the 9 expected `app/src` files — no more, no fewer.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ethansaba/code/recruiter && cd app && npm run build >/tmp/gsd-build-0308.log 2>&1 && cd .. && grep -c "id: 'explore'" app/src/components/layout/Sidebar.jsx | grep -qx 0 && grep -c "explore:" app/src/lib/icons.js | grep -qx 0 && grep -c "grow: Sprout" app/src/lib/icons.js | grep -qx 1 && grep -c "tab === 'explore'" app/src/App.jsx | grep -qx 0 && grep -c "key: 'coverage'\|key: 'discover'" app/src/App.jsx | grep -qx 0 && ! grep -rl "Network → Discover\|Network → Coverage" app/src && echo PASS || (tail -40 /tmp/gsd-build-0308.log; exit 1)</automated>
  </verify>
  <done>The fully-merged tree builds cleanly, every old-destination reference is confirmed gone repo-wide, and the changeset matches the expected 9-file footprint.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stage the manual-verification checklist and dev server for human review</name>
  <files>none (verification staging only, no source edits)</files>
  <read_first>
    .planning/phases/03-grow-discovery-funnel-merge/03-VALIDATION.md §"Phase Requirements → Test Map" (all 4 rows — the exact manual steps to stage)
    .planning/phases/03-grow-discovery-funnel-merge/03-UI-SPEC.md §"Interaction Contract — Cross-Section Linking" (the exact expected scroll/highlight behavior a human should confirm)
  </read_first>
  <action>
    Start the dev server (`cd app && npm run dev`, backgrounded) so a human reviewer can immediately click through the checklist without a separate setup step — this mirrors Phase 1's `01-05` and Phase 2's `02-04` precedent of leaving the dev server running for the staged human-verification pass, per `workflow.human_verify_mode=end-of-phase`. In this plan's `SUMMARY.md`, write out the 4-row manual checklist verbatim from `03-VALIDATION.md`'s Phase Requirements → Test Map: (1) land on Grow with ≥1 target company, click "+ Add to targets" on a new company card, confirm smooth-scroll to Coverage + row highlight, then click "🔍 Find people" on a Coverage row, confirm smooth-scroll to People + row highlight/pre-search — all without a tab switch; (2) from Pipeline, open an application and click "who could I meet here," confirm it lands on Grow/People (not a blank Network tab); repeat identically from Today's equivalent panel; (3) run one full pass of each of the 4 preserved capabilities — refresh Companies ranking, view a Coverage gap row, run/re-run a People search, generate + copy a cold-outreach draft; (4) confirm Sidebar has no "Explore" item and Network's segmented control has no Coverage/Discover chips (already automated in Task 1, restate here as a quick visual double-check). Note the dev server URL (`http://localhost:3001`) and that the checklist is pending human execution — this plan does not itself click through the UI (no browser automation is in scope for this repo per `03-VALIDATION.md`'s "no automated test framework" precedent).
  </action>
  <acceptance_criteria>
    - The dev server is running and reachable at `http://localhost:3001`.
    - `03-08-SUMMARY.md` contains the full 4-row manual checklist, verbatim from `03-VALIDATION.md`, marked pending human review.
  </acceptance_criteria>
  <verify>
    <human-check>
      Visit http://localhost:3001, sign in, and click "Grow" in the sidebar. Confirm all 4 checklist rows staged in 03-08-SUMMARY.md pass: (1) the Companies→Coverage→People scroll-and-highlight flow, (2) both external deep-link entry points (Pipeline and Today), (3) all 4 preserved AI capabilities (ranking/gap-detection/discovery/drafting), (4) old Explore/Coverage/Discover destinations are visually absent. Reply "approved" or describe any issue found.
    </human-check>
  </verify>
  <done>Dev server staged, 4-row manual checklist documented in SUMMARY.md, ready for a human to walk through and confirm GROW-01/GROW-02.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

- No new source artifacts — this plan is verification-only. It re-confirms the complete Phase 3 artifact set already produced by Plans 03-01 through 03-07: `app/src/components/ui/Section.jsx` (new), `app/src/components/GrowTab.jsx` (new), and edits to `TodayTab.jsx`, `ExploreTab.jsx`, `ReferralCoverageTab.jsx`, `DiscoverTab.jsx`, `App.jsx`, `layout/Sidebar.jsx`, `lib/icons.js`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan runs a local build, greps, and a dev server for human review — no new code path, no new trust boundary beyond what Plans 03-01 through 03-07 already introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08-1 | Information Disclosure | Local dev server left running | low | accept | Standard local-dev-loopback exposure only (`localhost:3001`), identical in kind to every prior phase's end-of-phase staging step (`01-05`, `02-04`) — not a new risk pattern for this repo. |

</threat_model>

<verification>
1. `cd app && npm run build` succeeds.
2. All 6 structural greps in Task 1 pass against the fully-merged tree.
3. `git diff --stat` against the Phase 3 pre-planning base commit touches exactly the 9 expected `app/src` files.
4. `03-08-SUMMARY.md` stages the full 4-row manual checklist for human execution.
</verification>

<success_criteria>
- Phase 3's complete changeset builds cleanly, every prior plan's own gate re-verifies green against the merged tree, no old-destination reference survives anywhere in `app/src`, and a human reviewer has everything needed to confirm GROW-01/GROW-02 in one sitting.
</success_criteria>

<output>
Create `.planning/phases/03-grow-discovery-funnel-merge/03-08-SUMMARY.md` when done
</output>
