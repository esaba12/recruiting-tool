---
phase: 03-grow-discovery-funnel-merge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/icons.js
  - app/src/components/layout/Sidebar.jsx
autonomous: true
requirements: [GROW-01]

must_haves:
  truths:
    - "The primary nav shows a 'Grow' item with an icon, in the exact array position 'Explore' used to occupy"
    - "No nav item labeled 'Explore' exists anywhere in Sidebar.jsx after this plan"
  artifacts:
    - "NAV_ICON.grow exported from app/src/lib/icons.js"
  key_links:
    - "Sidebar.jsx's NAV_ITEMS 'grow' id resolves to NAV_ICON.grow via icons.js's lookup map (Sidebar.jsx:30/90's existing `NAV_ICON[item.id]` pattern, unchanged)"
---

<objective>
Rename the Sidebar's `explore` nav entry to `grow` (label "Grow"), and add a matching `grow` key to `lib/icons.js`'s `NAV_ICON` lookup map so the nav item renders with an icon instead of silently falling back to no glyph (RESEARCH.md Pitfall 6). Per this phase's resolved autonomous-run decision, use lucide-react's `Sprout` icon (not `TrendingUp`, which 03-RESEARCH.md's Assumption A2 had tentatively suggested before a human confirmed `Sprout` for this run) — confirmed present in the installed `lucide-react` package.

This plan is purely nav-chrome and has zero dependency on `GrowTab.jsx` existing yet (that lands in a later wave) — `App.jsx`'s tab-routing catches up to this rename in Plan 03-07. Until then, the nav item will exist but its destination isn't wired; that's an expected, non-shipped intermediate state within this phase's execution, not a regression.

Purpose: deliver the nav-visible half of GROW-01 success criterion 3 ("the standalone Explore top-level tab... no longer exists as a separate destination") and prepare the icon lookup Sidebar.jsx already depends on.
Output: `app/src/lib/icons.js` (edited — `NAV_ICON.explore` replaced by `NAV_ICON.grow`, `Compass` import dropped since it becomes unused in this file, `Sprout` import added), `app/src/components/layout/Sidebar.jsx` (edited — `NAV_ITEMS`' `explore` entry replaced by `grow`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/lib/icons.js
@app/src/components/layout/Sidebar.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add NAV_ICON.grow (Sprout) to lib/icons.js, remove the now-unused Compass import</name>
  <files>app/src/lib/icons.js</files>
  <read_first>
    app/src/lib/icons.js (full file — confirm the `NAV_ICON` object's exact key order and the single `lucide-react` import block at the top)
  </read_first>
  <action>
    In `app/src/lib/icons.js`, replace the `NAV_ICON` object's `explore: Compass,` entry with `grow: Sprout,`, keeping it in the same array/object position (between `network` and `pipeline`) so the nav ordering is unaffected. Add `Sprout` to the existing `lucide-react` named-import block at the top of the file (the one currently importing `LayoutDashboard, Users, Kanban, GitFork, Compass, Gauge`). Remove `Compass` from that same import block — it is used nowhere else in `icons.js` after this edit (confirm via read: `Compass` appears only once in this file, as the old `explore` value). Note: `Compass` is still imported separately and used by `app/src/components/NotFoundPage.jsx` — that is a different file with its own independent import statement and is not affected by this change.
  </action>
  <acceptance_criteria>
    - `icons.js`'s `NAV_ICON` object has a `grow: Sprout` entry and no `explore` key.
    - `icons.js`'s `lucide-react` import line includes `Sprout` and does not include `Compass`.
    - `NotFoundPage.jsx` is untouched and still imports/uses `Compass` independently.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "grow: Sprout" app/src/lib/icons.js | grep -qx 1 && grep -c "explore:" app/src/lib/icons.js | grep -qx 0 && grep -c "Compass" app/src/lib/icons.js | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>NAV_ICON.grow = Sprout exists, NAV_ICON.explore is gone, and the Compass import is removed from icons.js only.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename Sidebar's NAV_ITEMS 'explore' entry to 'grow'</name>
  <files>app/src/components/layout/Sidebar.jsx</files>
  <read_first>
    app/src/components/layout/Sidebar.jsx (full file, 122 lines — confirm the `NAV_ITEMS` array at lines 3-12 and that `Icon = NAV_ICON[item.id]` at both desktop, line 30, and mobile, line 90, resolve dynamically with no other explore-specific code)
  </read_first>
  <action>
    In `app/src/components/layout/Sidebar.jsx`'s `NAV_ITEMS` array, replace the `{ id: 'explore', label: 'Explore' }` entry with `{ id: 'grow', label: 'Grow' }`, in the exact same array position (3rd entry, between `network` and `pipeline`). No other line in this file needs to change — `NAV_ICON[item.id]` (used at both the desktop `<aside>` nav and the mobile bottom bar) already resolves the icon dynamically by id, and its existing `{Icon && <Icon .../>}` guard means it will pick up `NAV_ICON.grow` from Plan 03-02's Task 1 automatically once both edits land.
  </action>
  <acceptance_criteria>
    - `Sidebar.jsx`'s `NAV_ITEMS` array contains `{ id: 'grow', label: 'Grow' }` at the same position `explore` previously occupied.
    - No `id: 'explore'` or `label: 'Explore'` string remains anywhere in `Sidebar.jsx`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "id: 'grow'" app/src/components/layout/Sidebar.jsx | grep -qx 1 && grep -c "id: 'explore'" app/src/components/layout/Sidebar.jsx | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>Sidebar.jsx's NAV_ITEMS has a 'grow'/'Grow' entry in place of 'explore'/'Explore'; no other file content changed.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

- EDITED: `app/src/lib/icons.js` — `NAV_ICON.grow` (value: `Sprout` from `lucide-react`) added; `NAV_ICON.explore` removed; `Compass` import removed from this file only.
- EDITED: `app/src/components/layout/Sidebar.jsx` — `NAV_ITEMS` array's `explore` entry replaced by `{ id: 'grow', label: 'Grow' }`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Static nav-label/icon-lookup rename — no user input, no network call, no data rendering change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-1 | Information Disclosure | `Sidebar.jsx` nav labels | low | accept | Nav item labels are static UI copy, not user- or account-specific data; no disclosure risk from a rename. |

</threat_model>

<verification>
1. `cd app && npm run build` succeeds with no import-resolution errors (confirms `Sprout` resolves from the installed `lucide-react` version and `Compass`'s removal from `icons.js` doesn't break `NotFoundPage.jsx`, which has its own independent import).
2. `grep -c "grow: Sprout" app/src/lib/icons.js` returns `1`.
3. `grep -c "id: 'grow'" app/src/components/layout/Sidebar.jsx` returns `1`.
4. Manual smoke check (folded into Phase 3's end-of-phase regression sweep, plan 03-08): Sidebar renders a "Grow" nav item with a sprout icon in the same list position "Explore" used to occupy, both desktop and mobile.
</verification>

<success_criteria>
- Sidebar's primary nav shows "Grow" (with icon) instead of "Explore", in the same position.
- `NAV_ICON` map has no dangling reference to the removed `explore` key.
</success_criteria>

<output>
Create `.planning/phases/03-grow-discovery-funnel-merge/03-02-SUMMARY.md` when done
</output>
