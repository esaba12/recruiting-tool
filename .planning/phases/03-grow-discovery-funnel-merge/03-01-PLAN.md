---
phase: 03-grow-discovery-funnel-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ui/Section.jsx
  - app/src/components/TodayTab.jsx
autonomous: true
requirements: [GROW-01]

must_haves:
  truths:
    - "Section/RowCap render byte-identical to their pre-extraction TodayTab.jsx behavior — zero visual regression on the Today tab"
    - "Section supports an optional Mono step-index badge (needed by Grow's 3 sections, unused by Today's calls)"
  artifacts:
    - "app/src/components/ui/Section.jsx exports Section, RowCap, HEADING_COLOR"
  key_links:
    - "TodayTab.jsx imports Section/RowCap/HEADING_COLOR from ./ui/Section.jsx instead of declaring them as local module-scope functions"
---

<objective>
Extract the `Section`/`RowCap`/`HEADING_COLOR` trio out of `TodayTab.jsx` (where they are private, unexported module-scope functions) into a new shared `app/src/components/ui/Section.jsx`, and update `TodayTab.jsx` to import them back. This is the mandatory infrastructure prerequisite for Phase 3's D-01 "mirror TodayTab's proven Section pattern" — `GrowTab.jsx` (planned in a later wave) cannot cleanly reuse an unexported component living inside a page-level file, and duplicating the code would violate this project's own Phase 2 D-06 extraction precedent (`attention.js`).

While extracting, add one new capability `Section` doesn't have today: an optional `step` prop that renders a small `Mono`-styled step-index badge (`01`/`02`/`03`) ahead of the icon, per 03-UI-SPEC.md's "New this phase — a small Mono step index in each section header" requirement. `TodayTab.jsx`'s own 8 existing `<Section>` call sites never pass `step`, so this addition is invisible there — a pure additive, backward-compatible change.

Purpose: unblock every later Phase 3 plan that needs `Section`/`RowCap` (Grow's 3 sections, and the RowCap application inside `ExploreTab.jsx`/`ReferralCoverageTab.jsx`/`DiscoverTab.jsx`), without breaking Today's existing 8 call sites (RESEARCH.md Pitfall 1).
Output: `app/src/components/ui/Section.jsx` (new, exporting `Section`, `RowCap`, `HEADING_COLOR`), `app/src/components/TodayTab.jsx` (edited — local defs replaced with an import, all 8 existing `<Section>`/`<RowCap>` JSX call sites at lines ~392-450 otherwise untouched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md
@app/src/components/TodayTab.jsx
@app/src/components/ui/Mono.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create ui/Section.jsx with the extracted Section/RowCap/HEADING_COLOR trio plus a new step prop</name>
  <files>app/src/components/ui/Section.jsx</files>
  <read_first>
    app/src/components/TodayTab.jsx (lines 1-61 — the exact source being extracted, including the doc comments above `HEADING_COLOR` and `Section`)
    app/src/components/ui/Mono.jsx (the primitive the new `step` badge reuses — default export, takes `className`/`children`)
    .planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md §"app/src/components/ui/Section.jsx (NEW — extraction)" (the exact target code block and required extension)
  </read_first>
  <action>
    Create `app/src/components/ui/Section.jsx`. Import `useState` from `react` and the default export `Mono` from `./Mono.jsx` (same-directory import, `ui/`). Move `HEADING_COLOR`, `Section`, and `RowCap` out of `TodayTab.jsx` verbatim (identical Tailwind classes, identical `border`/`heading` lookup objects, identical JSX structure) — do not alter any existing visual output for calls that omit the new prop. Extend `Section`'s destructured props to accept an optional `step` (in addition to the existing `title, subtitle, accent, icon: Icon, children`). Inside the `<h2>` heading row, render `step` first — before the `Icon` glyph — wrapped in `<Mono className="text-ink-500">{step}</Mono>`, only when `step` is truthy (no badge renders when `step` is omitted, matching Today's existing call sites exactly). Keep `RowCap`'s signature (`items, cap = 5, tier, renderItem`) and body completely unchanged from the source. End the file with `export { Section, RowCap, HEADING_COLOR }` (named exports, matching how `TodayTab.jsx` will import them — this is not a default export).
  </action>
  <acceptance_criteria>
    - `app/src/components/ui/Section.jsx` exists and exports exactly three named symbols: `Section`, `RowCap`, `HEADING_COLOR`.
    - `Section`'s heading `<h2>` renders the `step` Mono badge only when `step` is passed; omitting `step` produces the exact same DOM/classes as the pre-extraction version.
    - `RowCap`'s cap/expand-toggle logic and copy (`+N more — Show all` / `Show fewer`) are unchanged from the source.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "export { Section, RowCap, HEADING_COLOR }" app/src/components/ui/Section.jsx</automated>
  </verify>
  <done>ui/Section.jsx exists, exports Section/RowCap/HEADING_COLOR, and Section accepts a new optional `step` prop rendered via Mono ahead of the icon.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update TodayTab.jsx to import Section/RowCap/HEADING_COLOR instead of declaring them locally</name>
  <files>app/src/components/TodayTab.jsx</files>
  <read_first>
    app/src/components/TodayTab.jsx (full file — confirm the exact lines to delete and the 8 call sites at lines ~392-450 that must remain untouched)
    app/src/components/ui/Section.jsx (the new file from Task 1 — confirm the exact export names to import)
  </read_first>
  <action>
    In `app/src/components/TodayTab.jsx`, delete the local `HEADING_COLOR` constant declaration and the local `Section`/`RowCap` function declarations (the block currently at the top of the file, immediately after the existing imports and the `TIE_LABEL` constant) — including their doc comments, since those comments now describe code that no longer lives in this file. Add a new import line: `import { Section, RowCap, HEADING_COLOR } from './ui/Section.jsx'`, placed alongside this file's other same-directory component imports (near the existing `import Mono from './ui/Mono.jsx'` line). Every one of `TodayTab.jsx`'s 8 existing `<Section .../>`/`<RowCap .../>` JSX usages (the Overdue Follow-Ups, Stale Applications, High Urgency Contacts, Keep in Touch Due, Job Boards Needs-Review, Want to Schedule, OA-Due, and OA-Needs-Check sections) must remain exactly as they are today — this task only removes the local declarations and adds the import, it does not touch any JSX in the component body. `HEADING_COLOR` is not directly referenced elsewhere in `TodayTab.jsx` outside the removed block, so no other line needs to change.
  </action>
  <acceptance_criteria>
    - `TodayTab.jsx` no longer contains a local `function Section(` or `function RowCap(` declaration.
    - `TodayTab.jsx` imports `Section`, `RowCap`, `HEADING_COLOR` from `./ui/Section.jsx`.
    - All 8 pre-existing `<Section` / `<RowCap` JSX call sites in `TodayTab.jsx`'s return statement are present and unmodified.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^function Section(\|^function RowCap(" app/src/components/TodayTab.jsx | grep -qx 0 && grep -c "import { Section, RowCap, HEADING_COLOR } from './ui/Section.jsx'" app/src/components/TodayTab.jsx | grep -qx 1 && grep -c "<Section" app/src/components/TodayTab.jsx | grep -qx 8 && echo PASS</automated>
  </verify>
  <done>TodayTab.jsx imports Section/RowCap/HEADING_COLOR from ./ui/Section.jsx, has zero local declarations of them, and all 8 pre-existing call sites are untouched.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

- NEW FILE: `app/src/components/ui/Section.jsx` — exports `Section` (component, props: `title, subtitle, accent, icon, step, children`), `RowCap` (component, props: `items, cap, tier, renderItem`), `HEADING_COLOR` (plain object, keys `danger|warning|ink|accent`).
- EDITED: `app/src/components/TodayTab.jsx` — local `Section`/`RowCap`/`HEADING_COLOR` declarations removed, replaced with an import from `./ui/Section.jsx`. No new exports, no prop-shape changes to `TodayTab` itself.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | This plan is a pure client-side, same-origin component extraction — no new network call, no new user input, no new data ever rendered that wasn't already rendered by `TodayTab.jsx` before the extraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-1 | Tampering | `ui/Section.jsx` (new module) | low | accept | Pure presentational component, no state mutation beyond `RowCap`'s local `expanded` toggle (already existed pre-extraction); no external input reaches this file. |
| T-03-01-2 | Information Disclosure | `TodayTab.jsx` import swap | low | accept | Import-path change only; the data flowing through `Section`/`RowCap` (contact/application fields) is unchanged and was already scoped server-side via RLS before this plan. |

</threat_model>

<verification>
1. `cd app && npm run build` succeeds with no import-resolution errors.
2. `grep -c "^function Section(\|^function RowCap(" app/src/components/TodayTab.jsx` returns `0`.
3. `grep -n "export { Section, RowCap, HEADING_COLOR }" app/src/components/ui/Section.jsx` returns one match.
4. Manual smoke check (folded into Phase 3's end-of-phase regression sweep, plan 03-08): Today tab still renders all 8 of its attention sections with unchanged styling.
</verification>

<success_criteria>
- `ui/Section.jsx` exists, is imported by `TodayTab.jsx`, and Today's 8 existing sections render with zero visual regression.
- `Section` accepts an unused-but-functional optional `step` prop, ready for Grow's later sections to consume.
</success_criteria>

<output>
Create `.planning/phases/03-grow-discovery-funnel-merge/03-01-SUMMARY.md` when done
</output>
