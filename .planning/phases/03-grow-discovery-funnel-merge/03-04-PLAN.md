---
phase: 03-grow-discovery-funnel-merge
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/components/ReferralCoverageTab.jsx
autonomous: true
requirements: [GROW-01, GROW-02]

must_haves:
  truths:
    - "With zero target companies, the manual-entry textarea no longer auto-opens — a new EmptyState pointing up to Companies renders instead, with the textarea still one click away behind its existing ▼ edit toggle (D-03)"
    - "A new focus prop drives a scroll-to-and-highlight ring on the matching company row, mirroring DiscoverTab.jsx's existing, unchanged mechanic (D-04, RESEARCH.md Pattern 4)"
    - "The gap/weak/strong row list is capped to 5 with a 'show all' expander (D-02)"
  artifacts:
    - "ReferralCoverageTab.jsx accepts a new optional focus prop shaped { company, ts }"
  key_links:
    - "GrowTab.jsx (Plan 03-06) will pass focus={coverageFocus} into this component to drive the new ring-highlight effect"
---

<objective>
Edit `ReferralCoverageTab.jsx` to prepare it for Grow's Coverage section: fix the auto-expanding textarea (RESEARCH.md Pitfall 2 — today it silently pops open its freeform edit box whenever the target list is empty, which would visually collide with the new D-03 EmptyState), swap the zero-targets `EmptyState` copy to the new "point up to Companies" message per 03-UI-SPEC.md's Copywriting Contract, port `DiscoverTab.jsx`'s existing `rowRefs`/`useEffect`/ring-highlight mechanic onto this component's rows via a new `focus` prop (RESEARCH.md Pattern 4 — this file currently has zero focus/highlight machinery, verified by direct read), and cap the row list to 5 via `RowCap` (D-02).

This is a byte-level port of an already-shipped pattern, not new invention — `DiscoverTab.jsx`'s `byCompany` rows already do exactly this; Plan 03-04 replicates that mechanic here unchanged in shape (`rowRefs = useRef(new Map())`, effect keyed on `focus?.ts`, `ring-2 ring-accent-300` conditional class).

Purpose: this is one of three parallel "strip header + wire new prop + apply RowCap" edits (siblings: `ExploreTab.jsx` in Plan 03-03, `DiscoverTab.jsx` in Plan 03-05) preparing all three source components for `GrowTab.jsx`'s Section-wrapped composition.
Output: `app/src/components/ReferralCoverageTab.jsx` (edited — auto-expand removed, new EmptyState copy, new `focus` prop + ring-highlight, `RowCap` applied to `rows`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grow-discovery-funnel-merge/03-CONTEXT.md
@.planning/phases/03-grow-discovery-funnel-merge/03-UI-SPEC.md
@.planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md
@app/src/components/ReferralCoverageTab.jsx
@app/src/components/DiscoverTab.jsx
@app/src/components/ui/Section.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix the auto-expanding textarea and replace the zero-targets EmptyState copy</name>
  <files>app/src/components/ReferralCoverageTab.jsx</files>
  <read_first>
    app/src/components/ReferralCoverageTab.jsx (full file, 159 lines — confirm the `useEffect` at the top of the component body and the zero-targets `EmptyState` render)
    .planning/phases/03-grow-discovery-funnel-merge/03-UI-SPEC.md §"Coverage's manual target-list textarea — preserved, not promoted" and §"Copywriting Contract" (exact required new copy)
  </read_first>
  <action>
    In `app/src/components/ReferralCoverageTab.jsx`, edit the `useEffect(() => { if (!loaded) return; setDraft(targets.join('\n')); setEditingList(targets.length === 0); ... }, [loaded])` block: remove the `setEditingList(targets.length === 0)` line entirely (keep `setDraft(targets.join('\n'))` and the `if (!loaded) return` guard unchanged) — this stops the freeform textarea from auto-opening when the target list is empty, per D-03. The manual toggle button (`🎯 Target companies ... ▼ edit`) that flips `editingList` on click remains completely unchanged — a user can still open the textarea manually, this only removes the automatic open-on-empty behavior. Separately, replace the zero-targets branch's `<EmptyState msg="Add a target-company list above to see where you have — and don't have — a way in." />` with `<EmptyState msg="Add target companies above to see referral gaps." />` — the exact copy from 03-UI-SPEC.md's Copywriting Contract table.
  </action>
  <acceptance_criteria>
    - The `useEffect` gated on `[loaded]` no longer calls `setEditingList`.
    - The zero-targets `EmptyState`'s `msg` prop reads exactly "Add target companies above to see referral gaps."
    - The manual `▼ edit` toggle button and its `onClick={() => { setDraft(targets.join('\n')); setEditingList(o => !o) }}` handler are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "setEditingList(targets.length === 0)" app/src/components/ReferralCoverageTab.jsx | grep -qx 0 && grep -c "Add target companies above to see referral gaps." app/src/components/ReferralCoverageTab.jsx | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>Auto-expanding textarea removed; zero-targets EmptyState shows the new copy; manual edit toggle unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port the focus/rowRefs/ring-highlight mechanic from DiscoverTab.jsx and apply RowCap</name>
  <files>app/src/components/ReferralCoverageTab.jsx</files>
  <read_first>
    app/src/components/DiscoverTab.jsx lines 39-60 and 166-174 and 282-283 (the exact donor pattern: `rowRefs = useRef(new Map())`, the `useEffect` keyed on `focus?.ts`, and the row-level ref-registration + `ring-2 ring-accent-300` conditional class)
    app/src/components/ReferralCoverageTab.jsx (the `rows.map(r => (...))` block rendering each company row — this is the target of the port)
    app/src/components/ui/Section.jsx (from Plan 03-01 — confirm `RowCap`'s exact prop signature)
    .planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md §"app/src/components/ReferralCoverageTab.jsx (EDITED)" item 3 (the exact port code)
  </read_first>
  <action>
    In `app/src/components/ReferralCoverageTab.jsx`: change the top import line from `import { useState, useEffect } from 'react'` to `import { useState, useEffect, useRef } from 'react'`. Add `import { RowCap } from './ui/Section.jsx'` alongside the other same-directory imports. Add `focus` to the component's prop destructure: `export default function ReferralCoverageTab({ contacts, apps, interactions, contactRelationships = [], onRefresh, onFindPeople, focus })`. Inside the component body, add `const rowRefs = useRef(new Map())` and a new `useEffect` that — mirroring `DiscoverTab.jsx`'s donor pattern exactly — early-returns when `focus` is falsy, otherwise looks up `rowRefs.current.get(normalizeCompanyName(focus.company))` and, if found, calls `el.scrollIntoView({ behavior: 'smooth', block: 'center' })` inside a `setTimeout(..., 100)`; the effect's dependency array must be `[focus?.ts]` (not `[focus]` or `[focus?.company]`) so a repeat click on the same company re-triggers it, matching the NAV-03 repeat-click requirement. Wrap the row-list render in `<RowCap items={rows} cap={5} tier="ink" renderItem={r => (...)} />` (importing `RowCap` as above), moving the existing per-row JSX unchanged into the `renderItem` callback except for two additions to the outer row `<div>`: (1) a `ref={el => { if (el) rowRefs.current.set(normalizeCompanyName(r.company), el) }}` attribute, and (2) extend the row's existing conditional `className` template string to prepend `${focus && normalizeCompanyName(focus.company) === normalizeCompanyName(r.company) ? 'ring-2 ring-accent-300' : ''}` ahead of the existing gap/weak/strong border-color ternary — `normalizeCompanyName` is already imported at the top of this file (from `../lib/networkGraph.js`), no new import needed for it.
  </action>
  <acceptance_criteria>
    - `ReferralCoverageTab.jsx` imports `useRef` from `react` and `RowCap` from `./ui/Section.jsx`.
    - The component accepts a new `focus` prop.
    - A `rowRefs` ref-Map and a `useEffect` keyed on `[focus?.ts]` exist, performing the scroll-into-view exactly as `DiscoverTab.jsx`'s donor pattern does (same 100ms `setTimeout`, same `block: 'center'`).
    - Each row registers itself in `rowRefs` and conditionally applies `ring-2 ring-accent-300` when it matches `focus.company`.
    - The row list is wrapped in `RowCap` with `cap={5}` and `tier="ink"`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "useRef(new Map())" app/src/components/ReferralCoverageTab.jsx | grep -qx 1 && grep -c "\[focus?.ts\]" app/src/components/ReferralCoverageTab.jsx | grep -qx 1 && grep -c "ring-2 ring-accent-300" app/src/components/ReferralCoverageTab.jsx | grep -qx 1 && grep -c "<RowCap items={rows}" app/src/components/ReferralCoverageTab.jsx | grep -qx 1 && cd app && npm run build >/tmp/gsd-build-0304.log 2>&1 && echo PASS || (tail -40 /tmp/gsd-build-0304.log; exit 1)</automated>
  </verify>
  <done>ReferralCoverageTab.jsx has a working focus/rowRefs/ring-highlight mechanic identical in shape to DiscoverTab.jsx's, and its row list is RowCap-capped at 5; the app builds cleanly.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

- EDITED: `app/src/components/ReferralCoverageTab.jsx` — new optional prop `focus` (shape `{ company, ts }`); new internal `rowRefs` ref-Map + scroll/highlight `useEffect`; auto-expanding textarea behavior removed; new zero-targets `EmptyState` copy; row list now rendered via `RowCap` (cap=5).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure client-side derivation over already-fetched, already-RLS-scoped `contacts`/`apps`/`interactions` props (RESEARCH.md confirms Coverage makes zero network calls of its own) — this plan adds only a scroll/highlight DOM effect and a copy/behavior tweak, no new data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-1 | Tampering | New `focus` prop / ring-highlight effect | low | accept | `focus.company` is only ever set by trusted in-app callers (Plan 03-06's `GrowTab.jsx`, Plan 03-07's `App.jsx`) with a string already sourced from this same session's own already-authorized company data — never raw, unsanitized external input. |
| T-03-04-2 | Denial of Service | Textarea auto-expand removal | low | accept | Behavior-only change (no longer auto-opens on empty); does not affect any resource-bounded operation. |

</threat_model>

<verification>
1. `cd app && npm run build` succeeds.
2. `grep -c "setEditingList(targets.length === 0)" app/src/components/ReferralCoverageTab.jsx` returns `0`.
3. `grep -c "ring-2 ring-accent-300" app/src/components/ReferralCoverageTab.jsx` returns `1`.
4. `grep -c "<RowCap items={rows}" app/src/components/ReferralCoverageTab.jsx` returns `1`.
5. Manual smoke check (folded into Plan 03-08's end-of-phase sweep): once wired into `GrowTab.jsx` (Plan 03-06/03-07), confirm a company row highlights with a visible ring when `focus` targets it, and that gap/weak/strong sorting and the "+ Add contact"/"✎ Draft outreach"/"🔍 Find people" buttons all still work unchanged.
</verification>

<success_criteria>
- ReferralCoverageTab.jsx no longer auto-opens its manual editor on empty, shows the new pointed-up EmptyState copy, supports scroll-to-and-highlight via a new `focus` prop, and caps its row list to 5 — with zero regression to gap detection, contact-adding, or draft-outreach capabilities (GROW-02).
</success_criteria>

<output>
Create `.planning/phases/03-grow-discovery-funnel-merge/03-04-SUMMARY.md` when done
</output>
