---
phase: 03-grow-discovery-funnel-merge
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/components/DiscoverTab.jsx
autonomous: true
requirements: [GROW-01, GROW-02]

must_haves:
  truths:
    - "The zero-targets EmptyState no longer references a dead 'Network → Coverage' nav path (D-03/Copywriting Contract)"
    - "The Recommended candidate list is capped to 5 with a 'show all' expander (D-02) — the By-company nested lists are explicitly left uncapped"
  artifacts: []
  key_links:
    - "DiscoverTab.jsx's existing focus/rowRefs/ring-highlight mechanic is left completely unchanged — it is the donor pattern Plan 03-04 ported into ReferralCoverageTab.jsx, not something this plan touches"
---

<objective>
Make DiscoverTab.jsx's two required edits for the Grow merge: replace its stale zero-targets `EmptyState` copy (which today hardcodes a "Network → Coverage" nav path that will no longer exist after this phase — 03-UI-SPEC.md's Copywriting Contract calls this a required fix, not optional polish), and cap the "✨ Recommended" candidate list to 5 via `RowCap` per D-02. This is deliberately the smallest of the three source-component edits in this wave: `DiscoverTab.jsx` already owns the `focus`/`rowRefs`/ring-highlight mechanic that `ReferralCoverageTab.jsx` (Plan 03-04) ports a copy of — nothing about that existing mechanic changes here. This file also has no page-level `<h2>` title to strip (verified by direct read — unlike `ExploreTab.jsx`, its own header row is per-section controls: view switch, refresh, settings gear — all of which stay exactly as-is).

Purpose: complete the third of three parallel "prepare for Section-wrapped composition" edits (siblings: `ExploreTab.jsx` in Plan 03-03, `ReferralCoverageTab.jsx` in Plan 03-04) that `GrowTab.jsx` (Plan 03-06) will wrap.
Output: `app/src/components/DiscoverTab.jsx` (edited — stale `EmptyState` copy replaced, `RowCap` applied to the `recommended` list only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grow-discovery-funnel-merge/03-UI-SPEC.md
@.planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md
@app/src/components/DiscoverTab.jsx
@app/src/components/ui/Section.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace the stale zero-targets EmptyState copy</name>
  <files>app/src/components/DiscoverTab.jsx</files>
  <read_first>
    app/src/components/DiscoverTab.jsx (the `targets.length === 0` branch inside the `!targetsLoaded ? ... : targets.length === 0 ? ... : (...)` ternary, currently rendering the stale-copy EmptyState)
    .planning/phases/03-grow-discovery-funnel-merge/03-UI-SPEC.md §"Copywriting Contract" (exact required new copy)
  </read_first>
  <action>
    In `app/src/components/DiscoverTab.jsx`, replace the `<EmptyState msg="Add a target-company list in Network → Coverage first — Discover uses the same list." />` element with `<EmptyState msg="Add target companies above to see people to reach out to — People uses the same list." />` — the exact copy from 03-UI-SPEC.md's Copywriting Contract table. This is the only change this task makes; do not alter the surrounding ternary structure, the `!targetsLoaded` branch above it, or anything inside the non-empty `(...)` branch.
  </action>
  <acceptance_criteria>
    - No literal string "Network → Coverage" remains anywhere in `DiscoverTab.jsx`.
    - The zero-targets `EmptyState`'s `msg` prop reads exactly "Add target companies above to see people to reach out to — People uses the same list."
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Network → Coverage" app/src/components/DiscoverTab.jsx | grep -qx 0 && grep -c "Add target companies above to see people to reach out to" app/src/components/DiscoverTab.jsx | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>DiscoverTab.jsx's zero-targets EmptyState no longer points at a dead nav path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply RowCap (cap=5) to the Recommended candidate list only</name>
  <files>app/src/components/DiscoverTab.jsx</files>
  <read_first>
    app/src/components/DiscoverTab.jsx (the `view === 'recommended'` branch's `recommended.map(c => <CandidateCard .../>)` render, and the separate `view === 'byCompany'` branch's nested `live.map(c => <CandidateCard .../>)` inside each company row — confirm these are two distinct render sites, only the first is in scope)
    app/src/components/ui/Section.jsx (from Plan 03-01 — confirm `RowCap`'s exact prop signature)
    .planning/phases/03-grow-discovery-funnel-merge/03-UI-SPEC.md §"Out of cap scope" (confirms the By-company nested lists must NOT be capped)
  </read_first>
  <action>
    In `app/src/components/DiscoverTab.jsx`, add `import { RowCap } from './ui/Section.jsx'` alongside this file's other same-directory imports. In the `view === 'recommended'` branch only, replace `recommended.map(c => <CandidateCard key={candKey(c.person.company, c.person.name)} showCompany {...cardProps(c)} />)` with `<RowCap items={recommended} cap={5} tier="ink" renderItem={c => <CandidateCard key={candKey(c.person.company, c.person.name)} showCompany {...cardProps(c)} />} />`. Do NOT touch the `view === 'byCompany'` branch's nested `live.map(c => <CandidateCard key={...} {...cardProps(c)} />)` render (inside each company row) — per 03-UI-SPEC.md, that nested list is explicitly out of this phase's cap scope and must remain an uncapped `.map()`.
  </action>
  <acceptance_criteria>
    - `DiscoverTab.jsx` imports `RowCap` from `./ui/Section.jsx`.
    - The `view === 'recommended'` list render uses `<RowCap items={recommended} cap={5} .../>` instead of a raw `.map()`.
    - The `view === 'byCompany'` branch's nested per-company candidate list is unchanged (still a raw `.map()`, not wrapped in `RowCap`).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "import { RowCap } from './ui/Section.jsx'" app/src/components/DiscoverTab.jsx | grep -qx 1 && grep -c "<RowCap items={recommended}" app/src/components/DiscoverTab.jsx | grep -qx 1 && grep -c "live.map(c =>" app/src/components/DiscoverTab.jsx | grep -qx 1 && cd app && npm run build >/tmp/gsd-build-0305.log 2>&1 && echo PASS || (tail -40 /tmp/gsd-build-0305.log; exit 1)</automated>
  </verify>
  <done>The Recommended list is RowCap-capped at 5; the By-company nested candidate lists remain uncapped exactly as before; the app builds cleanly.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

- EDITED: `app/src/components/DiscoverTab.jsx` — zero-targets `EmptyState` copy corrected (no longer references a dead nav path); Recommended list rendered via `RowCap` (cap=5); By-company nested lists explicitly left uncapped. No new props, no changes to the existing `focus`/`rowRefs`/ring-highlight mechanic.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/exa`, `/claude-api`/`/openai-api` proxies | Unchanged — `DiscoverTab.jsx`'s `discoverPeople`/`rankCandidates` calls still go through the same `requireUser()`-gated, BYOK-keyed proxies this phase does not touch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-1 | Information Disclosure | Capped Recommended list | low | accept | Capping only affects which already-fetched, already-authorized candidates are shown at once — no new data fetched, no data hidden that a "show all" click can't reveal. |
| T-03-05-2 | Tampering | Copy string change | low | accept | Static UI text change only; no code path or data flow altered. |

</threat_model>

<verification>
1. `cd app && npm run build` succeeds.
2. `grep -c "Network → Coverage" app/src/components/DiscoverTab.jsx` returns `0`.
3. `grep -c "<RowCap items={recommended}" app/src/components/DiscoverTab.jsx` returns `1`.
4. Manual smoke check (folded into Plan 03-08's end-of-phase sweep): once wired into `GrowTab.jsx` (Plan 03-06/03-07), confirm the existing focus/scroll/highlight behavior into a specific company's By-company row is unaffected, and the Recommended list's "+N more — Show all" expander works.
</verification>

<success_criteria>
- DiscoverTab.jsx's stale copy is fixed and its Recommended list is capped, with zero regression to its people-discovery, add-to-contacts, or draft-intro capabilities (GROW-02) and zero change to its existing focus/highlight deep-link mechanic (the donor pattern Plan 03-04 depends on).
</success_criteria>

<output>
Create `.planning/phases/03-grow-discovery-funnel-merge/03-05-SUMMARY.md` when done
</output>
