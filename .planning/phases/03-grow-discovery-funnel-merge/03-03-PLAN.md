---
phase: 03-grow-discovery-funnel-merge
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/components/ExploreTab.jsx
autonomous: true
requirements: [GROW-01, GROW-02]

must_haves:
  truths:
    - "ExploreTab no longer renders its own <h2> page-title row — Section (its future wrapper) owns that"
    - "Adding a company to targets notifies the parent via a new onTargetAdded callback, without changing any existing localStorage/Supabase write behavior"
    - "The post-add 'Find people →' button keeps jumping straight to People (unchanged), per the resolved Open Question 1"
    - "The ranked company list is capped to 5 with a 'show all' expander (D-02)"
  artifacts:
    - "ExploreTab.jsx accepts a new optional onTargetAdded prop"
  key_links:
    - "addToTargets(name) calls onTargetAdded?.(name) as its last statement, after the existing setTargetCompanies/setAdded/lsSet calls"
    - "shown.map(...) is replaced by <RowCap items={shown} cap={5} tier=\"ink\" renderItem={...} /> imported from ./ui/Section.jsx"
---

<objective>
Edit `ExploreTab.jsx` so it can be wrapped by Grow's `Section` component (planned in Plan 03-06) without producing a duplicate header: strip its own `<h2>Companies for you</h2>` title row (Section will own that), add a new `onTargetAdded` callback prop fired at the end of `addToTargets()` so the parent can trigger D-04's "scroll to Coverage" behavior, and cap the ranked company list to 5 with a "+N more — Show all" expander per D-02, reusing the `RowCap` component extracted in Plan 03-01.

Per this phase's resolved Open Question 1 (autonomous-run decision, not re-litigated here): `CompanyCard`'s post-add "Find people →" button keeps calling `onFindPeople(c.name)` directly — it is NOT routed through the new `onTargetAdded` callback or through Coverage. D-04's "scroll to Coverage" instruction applies only to the initial `+ Add to targets` click.

Purpose: this is one of three parallel "strip header + wire new prop + apply RowCap" edits (siblings: `ReferralCoverageTab.jsx` in Plan 03-04, `DiscoverTab.jsx` in Plan 03-05) that together prepare all three source components for `GrowTab.jsx`'s Section-wrapped composition, per RESEARCH.md Pattern 2 and Pattern 6.
Output: `app/src/components/ExploreTab.jsx` (edited — header JSX removed, `onTargetAdded` prop added and threaded, `RowCap` applied to the `shown` list render).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grow-discovery-funnel-merge/03-CONTEXT.md
@.planning/phases/03-grow-discovery-funnel-merge/03-UI-SPEC.md
@.planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md
@app/src/components/ExploreTab.jsx
@app/src/components/ui/Section.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip ExploreTab's own header row and add the onTargetAdded callback prop</name>
  <files>app/src/components/ExploreTab.jsx</files>
  <read_first>
    app/src/components/ExploreTab.jsx (full file, 296 lines — confirm the exact header block at the top of the returned JSX and the exact body of `addToTargets`)
    .planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md §"app/src/components/ExploreTab.jsx (EDITED)" (exact before/after for both edits)
  </read_first>
  <action>
    In `app/src/components/ExploreTab.jsx`, delete the `<h2 className="font-heading text-lg font-semibold text-ink-900">Companies for you</h2>` element and its immediately-following `<p className="text-[11px] text-ink-400">...</p>` subtitle paragraph (the "From YC's directory + Exa's public-web search..." text) from inside the `flex items-center gap-2 flex-wrap` header row — `Section title="Companies"` will own this title once `GrowTab.jsx` wraps this component (Plan 03-06). Keep the sibling `<div className="ml-auto flex items-center gap-2">` block (the "Updated Xh ago"/↻ Refresh/Edit interests controls) completely unchanged — those are per-section action controls, not page chrome. Update the component's prop destructure from `{ apps = [], onFindPeople }` to `{ apps = [], onFindPeople, onTargetAdded }`. Inside `addToTargets(name)`, add `onTargetAdded?.(name)` as the new last statement, after the existing `setAdded(next); lsSet(ADDED_KEY, [...next])` line — do not change the existing `setTargetCompanies`/`setAdded`/`lsSet` logic in any way, this is a pure addition. Do NOT wire `onTargetAdded` into `CompanyCard`'s "Find people →" button or `onFindPeople` — that button keeps calling `onFindPeople(c.name)` directly, unchanged, per the resolved Open Question 1.
  </action>
  <acceptance_criteria>
    - No literal string "Companies for you" remains in `ExploreTab.jsx`.
    - `ExploreTab`'s prop signature includes `onTargetAdded`.
    - `addToTargets` calls `onTargetAdded?.(name)` as its final line, after the pre-existing target/added-state updates.
    - `CompanyCard`'s "Find people →" button still calls `onFindPeople(c.name)` directly, with no new intermediate handler.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Companies for you" app/src/components/ExploreTab.jsx | grep -qx 0 && grep -c "onTargetAdded" app/src/components/ExploreTab.jsx | grep -qx 2 && echo PASS</automated>
  </verify>
  <done>ExploreTab.jsx has no duplicate header markup, accepts and fires an onTargetAdded callback from addToTargets, and the existing post-add Find-people shortcut is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply RowCap (cap=5) to the ranked company list</name>
  <files>app/src/components/ExploreTab.jsx</files>
  <read_first>
    app/src/components/ExploreTab.jsx (the `shown.map((c, i) => ...)` render block, currently inside a `<div className="space-y-2">` wrapper)
    app/src/components/ui/Section.jsx (from Plan 03-01 — confirm `RowCap`'s exact prop signature: `items, cap, tier, renderItem`)
  </read_first>
  <action>
    In `app/src/components/ExploreTab.jsx`, add `import { RowCap } from './ui/Section.jsx'` to the top of the file, alongside the existing `import CompanyOnboarding from './CompanyOnboarding.jsx'` line. Replace the `shown.map((c, i) => (<CompanyCard key={c.name} company={c} index={i} .../>))` block (currently rendered inside `<div className="space-y-2">...</div>`) with `<RowCap items={shown} cap={5} tier="ink" renderItem={(c, i) => <CompanyCard key={c.name} company={c} index={i} isAdded={added.has(c.name)} onAdd={() => addToTargets(c.name)} onDismiss={() => dismiss(c.name)} onExpand={() => expand(c)} expanding={expanding === c.name} onFindPeople={onFindPeople} />} />` — preserving every existing prop passed to `CompanyCard` exactly as today, just moved from an inline `.map()` into `RowCap`'s `renderItem` callback. Note `RowCap`'s `renderItem` signature only guarantees the item itself is passed as the first argument (see `RowCap`'s body: `visible.map(renderItem)`) — the `index` parameter received by `renderItem` here is the index within the *capped* `visible` array, not the original `shown` array; this is an acceptable, cosmetic-only difference (it only affects `CompanyCard`'s `index < 6` GitHub-badge-eligibility check, which remains correct for the first 5 always-visible cards and simply doesn't apply past the cap).
  </action>
  <acceptance_criteria>
    - `ExploreTab.jsx` imports `RowCap` from `./ui/Section.jsx`.
    - The company-card list render uses `<RowCap items={shown} cap={5} tier="ink" renderItem={...} />` instead of a raw `.map()`.
    - Every prop `CompanyCard` previously received (`company, index, isAdded, onAdd, onDismiss, onExpand, expanding, onFindPeople`) is still passed identically inside `RowCap`'s `renderItem`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "import { RowCap } from './ui/Section.jsx'" app/src/components/ExploreTab.jsx | grep -qx 1 && grep -c "<RowCap items={shown}" app/src/components/ExploreTab.jsx | grep -qx 1 && cd app && npm run build >/tmp/gsd-build-0303.log 2>&1 && echo PASS || (tail -40 /tmp/gsd-build-0303.log; exit 1)</automated>
  </verify>
  <done>The ranked company list renders via RowCap (cap=5, "+N more — Show all" expander), preserving every existing CompanyCard prop, and the app builds cleanly.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

- EDITED: `app/src/components/ExploreTab.jsx` — new optional prop `onTargetAdded` (called from `addToTargets`); own `<h2>` header removed; company list now rendered via `RowCap` (imported from `./ui/Section.jsx`, cap=5).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/exa`, `/claude-api`/`/openai-api` proxies | Unchanged — `ExploreTab.jsx`'s existing `findCompanies`/`moreLikeThis` calls (via `lib/companyFinder.js`) still go through the same `requireUser()`-gated, BYOK-keyed proxies this phase does not touch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-1 | Tampering | `addToTargets`'s new `onTargetAdded` callback | low | accept | Callback fires only after the existing, unchanged `setTargetCompanies`/Supabase-backed `useTargetCompanies` write already succeeded — it carries no new data into or out of the write path, purely a post-hoc notification to the parent component. |
| T-03-03-2 | Information Disclosure | `RowCap`'s capped rendering | low | accept | Capping only affects which already-fetched, already-authorized company results are visually shown at once — no new data is fetched or exposed by this change. |

</threat_model>

<verification>
1. `cd app && npm run build` succeeds.
2. `grep -c "Companies for you" app/src/components/ExploreTab.jsx` returns `0`.
3. `grep -c "onTargetAdded" app/src/components/ExploreTab.jsx` returns `2` (prop destructure + call site).
4. `grep -c "<RowCap items={shown}" app/src/components/ExploreTab.jsx` returns `1`.
5. Manual smoke check (folded into Plan 03-08's end-of-phase sweep): with ExploreTab still reachable only via its future Grow wrapper, confirm — once Plan 03-06/03-07 land — that adding a company to targets, dismissing a company, and "More like this" all still function exactly as before.
</verification>

<success_criteria>
- ExploreTab.jsx has no duplicate header, exposes `onTargetAdded`, and caps its ranked list to 5 with an expander — with zero regression to its existing ranking/add/dismiss/expand/find-people capabilities (GROW-02).
</success_criteria>

<output>
Create `.planning/phases/03-grow-discovery-funnel-merge/03-03-SUMMARY.md` when done
</output>
