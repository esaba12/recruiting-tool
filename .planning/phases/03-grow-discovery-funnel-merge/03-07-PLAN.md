---
phase: 03-grow-discovery-funnel-merge
plan: 07
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - app/src/App.jsx
autonomous: true
requirements: [GROW-01, GROW-02]

must_haves:
  truths:
    - "Sidebar's 'Grow' nav item renders GrowTab.jsx, not a blank pane or the old ExploreTab (GROW-01 criterion 1)"
    - "Pipeline's and Today's 'who could I meet here' panels both land on Grow with the People section pre-focused, not on a dead Network route (D-05, RESEARCH.md Pitfall 4)"
    - "NetworkTab's segmented control no longer offers Coverage or Discover chips (GROW-01 criterion 3)"
    - "The public /demo route is unaffected — 'grow' is not added to DEMO_NAV_ITEMS, mirroring 'explore''s prior absence"
  artifacts: []
  key_links:
    - "App.jsx's re-pointed goFindPeople sets growFocusCompany and setTab('grow'), threaded into <GrowTab initialPeopleFocus={growFocusCompany} />"
    - "Both PipelineTab's and TodayTab's onFindPeople={goFindPeople} wiring (already existing, unchanged prop names) now resolve through the re-pointed goFindPeople body"
---

<objective>
Wire the new `GrowTab.jsx` (Plan 03-06) into `App.jsx`'s routing, completing GROW-01's "single Grow destination" and GROW-01 criterion 3's "old destinations gone." Two edits: (1) re-point the shared `goFindPeople` deep-link helper from its old `Network → Discover` target to `Grow`'s People section, and swap `AppInner`'s `tab === 'explore'` branch for a `tab === 'grow'` branch rendering `GrowTab`; (2) clean up `NetworkTab`'s now-dead `coverage`/`discover` sub-views — trim `NETWORK_VIEWS`, remove the now-unused `ReferralCoverageTab`/`DiscoverTab` imports and their render branches, and remove the `focusCompany` state (both `NetworkTab`'s own internal copy and `AppInner`'s `networkFocusCompany`) that existed solely to support the old in-Network Coverage→Discover jump.

Per RESEARCH.md Pitfall 4: `goFindPeople` is shared by **two** call sites — `PipelineTab` (`onFindPeople={goFindPeople}`) and `TodayTab` (also `onFindPeople={goFindPeople}`, itself passed through to `ApplicationDetailModal`). Fixing `goFindPeople`'s body once, in this plan, automatically fixes both — no separate edit is needed at either call site, but both must be included in this plan's verification, not just Pipeline's (03-VALIDATION.md's Phase Requirements → Test Map explicitly lists both).

Purpose: this is the plan that actually makes Grow reachable and makes the old Explore/Coverage/Discover destinations disappear — the final structural piece of GROW-01.
Output: `app/src/App.jsx` (edited — `goFindPeople` re-pointed, `tab === 'grow'` routing added, `tab === 'explore'` routing removed, `NetworkTab`'s dead `coverage`/`discover` code removed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grow-discovery-funnel-merge/03-CONTEXT.md
@.planning/phases/03-grow-discovery-funnel-merge/03-RESEARCH.md
@.planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md
@app/src/App.jsx
@app/src/components/GrowTab.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-point goFindPeople to Grow and swap the tab === 'explore' branch for tab === 'grow'</name>
  <files>app/src/App.jsx</files>
  <read_first>
    app/src/App.jsx lines 1-32 (top-level imports), lines 232-240 (`AppInner`'s `tab`/`goFindPeople` state), lines 313-333 (the tab-branch JSX ternary chain), lines 355-366 (`DemoApp`'s doc comment and `DEMO_NAV_ITEMS` filter)
    app/src/components/GrowTab.jsx (from Plan 03-06 — confirm its exact prop names: `contacts, apps, interactions, contactRelationships, onRefresh, onRefreshRelationships, initialPeopleFocus`)
    .planning/phases/03-grow-discovery-funnel-merge/03-PATTERNS.md §"app/src/App.jsx (EDITED)" (the exact before/after for `goFindPeople` and the tab-branch swap)
  </read_first>
  <action>
    In `app/src/App.jsx`'s `AppInner` function: rename the state pair `const [networkFocusCompany, setNetworkFocusCompany] = useState(null)` to `const [growFocusCompany, setGrowFocusCompany] = useState(null)` (keep `networkInitialView`/`setNetworkInitialView` untouched — that pair is still needed for Overview's "Open graph" jump into Network's Graph view and is unrelated to this change). Update the doc comment immediately above `goFindPeople` (currently "Deep-link into Network → Discover, pre-searching one company — shared by Explore's 'Find people →' and Pipeline's 'who could I meet here' panel so both land in the same place.") to instead read "Deep-link into Grow's People section, pre-searching one company — shared by Pipeline's and Today's 'who could I meet here' panels so both land in the same place." (this corrects the stale nav-path reference per 03-UI-SPEC.md's Copywriting Contract "Other stale-copy check performed" finding, and corrects "Explore's" to "Today's" since Explore's own "Find people →" now goes through `GrowTab.jsx`'s internal `goToPeople`, not this top-level helper). Change `goFindPeople`'s body from setting `setNetworkFocusCompany`/`setNetworkInitialView('discover')`/`setTab('network')` to instead call `setGrowFocusCompany({ company, ts: Date.now() })` followed by `setTab('grow')` — two statements, no `networkInitialView` call needed since Grow is not a Network sub-view.

    Add `import GrowTab from './components/GrowTab.jsx'` to the import block, placed near the other same-directory component imports (e.g. next to the existing `import TodayTab from './components/TodayTab.jsx'` line). Remove the now-dead `import ExploreTab from './components/ExploreTab.jsx'` line entirely — `App.jsx` no longer renders `<ExploreTab>` directly anywhere once this task's next edit lands, and `GrowTab.jsx` has its own separate, independent import of `ExploreTab` inside its own file, so this is a safe removal with zero effect on `GrowTab.jsx`.

    In `AppInner`'s tab-branch JSX ternary chain, delete the entire `{!loading && tab === 'explore' && (<ExploreTab apps={apps} onFindPeople={goFindPeople} />)}` block, and in its place (same position in the chain, between the `tab === 'network'` block and the `tab === 'pipeline'` block) add `{!loading && tab === 'grow' && (<GrowTab contacts={contacts} apps={apps} interactions={interactions} contactRelationships={contactRelationships} onRefresh={load} onRefreshRelationships={refreshContactRelationships} initialPeopleFocus={growFocusCompany} />)}`. Update the `<NetworkTab .../>` render's `initialFocusCompany={networkFocusCompany}` prop reference to remove that prop entirely for now (it will be fully retired from `NetworkTab`'s own signature in Task 2 of this plan — removing the caller-side prop here first avoids a dangling reference to the just-renamed `networkFocusCompany` variable, which no longer exists after this task's rename).

    Finally, in `DemoApp`'s doc comment above `DEMO_NAV_ITEMS` (the block explaining why the demo route is trimmed), update the phrase "Explore/Discover/Outbox/Job Boards/Calendar/Settings all call Claude/OpenAI/Exa/GitHub/Google proxies" to "Grow/Outbox/Job Boards/Calendar/Settings all call Claude/OpenAI/Exa/GitHub/Google proxies" — `Discover`'s functionality is now inside `Grow`, so the sentence's excluded-tabs list should name `Grow`, not a now-nonexistent `Explore`/`Discover` pair. Confirm (no code change needed, this is a verification-only check) that `'grow'` is NOT added to the `DEMO_NAV_ITEMS` filter list — it must stay `['today', 'overview', 'network', 'pipeline']`, exactly mirroring `'explore'`'s prior absence, since Grow's 3 sections all call `requireUser()`-gated proxies that would 401 for an anonymous `/demo` visitor.
  </action>
  <acceptance_criteria>
    - `App.jsx` has no `tab === 'explore'` branch and no direct `<ExploreTab` JSX render; it has a `tab === 'grow'` branch rendering `<GrowTab .../>` with all 7 listed props.
    - `App.jsx` imports `GrowTab` and no longer imports `ExploreTab`.
    - `goFindPeople`'s body calls `setGrowFocusCompany` and `setTab('grow')`; no reference to `setNetworkFocusCompany`/`setNetworkInitialView('discover')` remains inside it.
    - The doc comment above `goFindPeople` no longer says "Network → Discover" or "Explore's".
    - `DEMO_NAV_ITEMS`'s filter list is unchanged (`['today', 'overview', 'network', 'pipeline']`) — `'grow'` is not present.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "tab === 'explore'" app/src/App.jsx | grep -qx 0 && grep -c "tab === 'grow'" app/src/App.jsx | grep -qx 1 && grep -c "import GrowTab from './components/GrowTab.jsx'" app/src/App.jsx | grep -qx 1 && grep -c "import ExploreTab" app/src/App.jsx | grep -qx 0 && grep -c "setGrowFocusCompany" app/src/App.jsx | grep -qx 2 && grep -c "Network → Discover" app/src/App.jsx | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>App.jsx routes tab='grow' to GrowTab.jsx with correct props; goFindPeople sets growFocusCompany and lands on Grow; the old Explore branch/import is gone; the demo nav filter is unaffected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove NetworkTab's dead coverage/discover sub-views and their supporting state/imports</name>
  <files>app/src/App.jsx</files>
  <read_first>
    app/src/App.jsx lines 34-127 (the full `NetworkTab` function: `NETWORK_VIEWS`, `DEMO_NETWORK_VIEWS`, the component's prop destructure including `focusCompany` state, and the `view === 'discover' ? ... : view === 'coverage' ? ...` render ternary)
    .planning/phases/03-grow-discovery-funnel-merge/03-RESEARCH.md §"Pitfall 5" (the exact list of dead imports/state/branches to remove)
  </read_first>
  <action>
    In `app/src/App.jsx`'s `NETWORK_VIEWS` array, delete the `{ key: 'coverage', label: 'Coverage', icon: Target }` and `{ key: 'discover', label: 'Discover', icon: UserSearch }` entries, leaving `table`, `cards`, `graph`, `outbox` in their existing order. Remove the now-unused `import ReferralCoverageTab from './components/ReferralCoverageTab.jsx'` and `import DiscoverTab from './components/DiscoverTab.jsx'` lines — both components are now imported only inside `GrowTab.jsx` itself (Plan 03-06), so these top-level `App.jsx` copies are dead. Remove `Target` and `UserSearch` from the `import { Table2, LayoutGrid, Share2, Target, Send, UserSearch } from 'lucide-react'` line (leaving `Table2, LayoutGrid, Share2, Send`) — confirmed by direct read that neither is referenced anywhere else in `App.jsx` outside the now-deleted `NETWORK_VIEWS` entries.

    Inside the `NetworkTab` function: remove `const [focusCompany, setFocusCompany] = useState(initialFocusCompany)` and remove `initialFocusCompany = null` from the function's prop destructure (both become dead once the branches below are removed). In the render ternary, delete the `view === 'discover' ? <DiscoverTab .../> : view === 'coverage' ? <ReferralCoverageTab .../> :` two clauses entirely, leaving the ternary chain starting directly at `view === 'graph' ? <NetworkGraphTab .../> : ...` (every other clause — `outbox`, the `filtered.length === 0` empty-state check, `table`, and the default Cards branch — stays exactly as-is, only its position in the chain shifts earlier).

    Update the doc comment directly above `DEMO_NETWORK_VIEWS` (currently explaining "Discover/Outbox call Exa+Claude/GPT... and Coverage's 'Find people' deep-links into Discover") to remove the now-stale "Coverage's 'Find people' deep-links into Discover" sentence fragment and change "Discover/Outbox call Exa+Claude/GPT" to "Outbox calls Exa+Claude/GPT" (singular — Discover is no longer a Network view) — keep the rest of that comment (the "Keep in Touch's 'Log'..." sentence and the closing "Table/Cards/Graph are pure local rendering..." sentence) unchanged, since neither references Coverage or Discover and are outside this plan's scope.
  </action>
  <acceptance_criteria>
    - `NETWORK_VIEWS` has exactly 4 entries: `table`, `cards`, `graph`, `outbox` — no `coverage` or `discover` key.
    - `App.jsx` no longer imports `ReferralCoverageTab`, `DiscoverTab`, `Target`, or `UserSearch`.
    - `NetworkTab`'s render ternary has no `view === 'discover'` or `view === 'coverage'` clause.
    - `NetworkTab`'s prop destructure no longer includes `initialFocusCompany`, and it declares no local `focusCompany` state.
    - `cd app && npm run build` succeeds (confirms no dangling reference to any removed import/state/prop).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "key: 'coverage'\|key: 'discover'" app/src/App.jsx | grep -qx 0 && grep -c "view === 'discover'\|view === 'coverage'" app/src/App.jsx | grep -qx 0 && grep -c "import ReferralCoverageTab\|import DiscoverTab" app/src/App.jsx | grep -qx 0 && grep -c "useState(initialFocusCompany)" app/src/App.jsx | grep -qx 0 && cd app && npm run build >/tmp/gsd-build-0307.log 2>&1 && echo PASS || (tail -40 /tmp/gsd-build-0307.log; exit 1)</automated>
  </verify>
  <done>NetworkTab no longer offers Coverage/Discover, all dead imports/state from the removal are gone, and the app builds cleanly.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

- EDITED: `app/src/App.jsx` — `goFindPeople` re-pointed to Grow (`growFocusCompany` state replaces `networkFocusCompany`); `tab === 'grow'` routing added, `tab === 'explore'` removed; `GrowTab` imported, `ExploreTab`/`ReferralCoverageTab`/`DiscoverTab`/`Target`/`UserSearch` imports removed as dead; `NetworkTab`'s `NETWORK_VIEWS` trimmed to `table/cards/graph/outbox`; `NetworkTab`'s `focusCompany` state and `coverage`/`discover` render branches removed; stale doc comments corrected.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser tab-state routing (`App.jsx`) | Unchanged trust model — this is client-side `useState`-driven view switching, not a router; no new server endpoint, no new auth gate, no change to which proxies any wrapped component calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-1 | Tampering | `growFocusCompany` deep-link state | low | accept | Set only by `goFindPeople`, itself only invoked from trusted in-app button handlers (`PipelineTab`/`TodayTab`'s "who could I meet here" panels) passing an already-known, already-authorized company string — never raw external input. |
| T-03-07-2 | Elevation of Privilege | Removed Network sub-views | low | accept | Deleting the `coverage`/`discover` `NETWORK_VIEWS` entries and their render branches only removes reachability of functionality that is still fully present (relocated to `GrowTab.jsx`, itself still behind the same `requireUser()`-gated proxies) — no privilege boundary is created or removed. |
| T-03-07-3 | Information Disclosure | `/demo` route scope | low | accept | Explicitly verified `'grow'` is not added to `DEMO_NAV_ITEMS` — an anonymous `/demo` visitor gets no new surface that would 401 or leak an auth-gated error state. |

</threat_model>

<verification>
1. `cd app && npm run build` succeeds.
2. `grep -c "tab === 'grow'" app/src/App.jsx` returns `1`; `grep -c "tab === 'explore'"` returns `0`.
3. `grep -c "key: 'coverage'\|key: 'discover'"` in `App.jsx` returns `0`.
4. `grep -n "DEMO_NAV_ITEMS = NAV_ITEMS.filter"` in `App.jsx` still filters to exactly `['today', 'overview', 'network', 'pipeline']` (no `'grow'` added).
5. Manual smoke check (folded into Plan 03-08's end-of-phase sweep): click "Grow" in the sidebar and confirm the 3-section page renders; from Pipeline, open an application and click "who could I meet here" and confirm it lands on Grow's People section, pre-focused; repeat identically from Today's equivalent panel (RESEARCH.md Pitfall 4 — both call sites must be checked, not just Pipeline's).
</verification>

<success_criteria>
- Grow is reachable from the sidebar and renders the merged page; both existing external deep-link entry points (Pipeline, Today) land on Grow/People correctly; the old Explore tab and Network's Coverage/Discover sub-views no longer exist anywhere in the app (GROW-01 criterion 3); the public /demo route is unaffected.
</success_criteria>

<output>
Create `.planning/phases/03-grow-discovery-funnel-merge/03-07-SUMMARY.md` when done
</output>
