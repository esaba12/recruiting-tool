---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/AddEventModal.jsx
  - app/src/components/EventDetailModal.jsx
  - app/src/components/LogInteractionModal.jsx
  - app/src/components/KeepInTouchTab.jsx
  - app/src/components/TodayTab.jsx
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "AddEventModal.jsx and EventDetailModal.jsx render their modal shells with rounded-t-md/md:rounded-md and border-ink-300 instead of rounded-2xl/shadow-2xl, matching ui/Modal.jsx's Plan 07-01 treatment"
    - "LogInteractionModal.jsx's 2 inline content cards and KeepInTouchTab.jsx's row cards render with rounded-md/border-ink-300, no shadow-sm"
    - "TodayTab.jsx's OverdueRow renders with rounded-md/border-ink-300, no persistent shadow-sm (hover:shadow-md preserved)"
  artifacts:
    - path: "app/src/components/AddEventModal.jsx"
      provides: "bordered-flat modal shell matching ui/Modal.jsx"
  key_links:
    - from: "app/src/components/AddEventModal.jsx"
      to: "app/src/components/ui/Modal.jsx (Plan 07-01)"
      via: "identical hand-rolled shell pattern, now visually consistent"
      pattern: "rounded-t-md md:rounded-md"
</must_haves>
---

<objective>
Continue the D-02 systematic shape-sweep into 2 hand-rolled modal shells that duplicate `ui/Modal.jsx`'s old pattern locally instead of importing it (`AddEventModal.jsx`, `EventDetailModal.jsx`), plus 3 files sharing the same "list-row card" generic-card pattern already fixed elsewhere in this phase (`LogInteractionModal.jsx`'s 2 inline cards, `KeepInTouchTab.jsx`'s row cards, and `TodayTab.jsx`'s `OverdueRow`).

Purpose: `AddEventModal.jsx`/`EventDetailModal.jsx` are the "+ Event" and calendar-event-detail modals — high-traffic surfaces that currently look like the pre-reskin Modal.jsx. `TodayTab.jsx`'s edit here is scoped narrowly to `OverdueRow` only (line ~174) — the file's `ActivitySection`/stat-tile/motion work is a separate, larger concern owned by Plan 07-08 (Wave 2), which depends on this plan landing first so it edits a file whose shape is already finished.
Output: Every calendar-event modal and the two remaining list-row card patterns render with the bordered-flat shape system.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddEventModal.jsx + EventDetailModal.jsx modal-shell shape</name>
  <files>app/src/components/AddEventModal.jsx, app/src/components/EventDetailModal.jsx</files>
  <read_first>
    app/src/components/AddEventModal.jsx line 34
    app/src/components/EventDetailModal.jsx line 35
    app/src/components/ui/Modal.jsx (post-Plan-07-01 state — the exact pattern these 2 files should now match)
  </read_first>
  <action>
    Both files hand-roll the same modal-overlay shell `ui/Modal.jsx` used to have, rather than importing it. Apply the identical fix Plan 07-01 already applied to `ui/Modal.jsx`:
    - `AddEventModal.jsx` line 34: `bg-white w-full md:max-w-md rounded-t-2xl md:rounded-2xl shadow-2xl max-h-[90vh] overflow-y-auto` to `bg-white w-full md:max-w-md rounded-t-md md:rounded-md border border-ink-300 max-h-[90vh] overflow-y-auto`.
    - `EventDetailModal.jsx` line 35: `bg-white w-full md:max-w-lg rounded-t-2xl md:rounded-2xl shadow-2xl max-h-[90vh] overflow-y-auto` to `bg-white w-full md:max-w-lg rounded-t-md md:rounded-md border border-ink-300 max-h-[90vh] overflow-y-auto`.
    Do not touch either file's `bg-ink-900/40` backdrop overlay div, animation props, or the purple/accent school-slot Badge in EventDetailModal.jsx line 43 (documented exception, see Plan 07-04) — shape only.
  </action>
  <verify>
    <automated>cd app && grep -c "rounded-2xl\|shadow-2xl" src/components/AddEventModal.jsx src/components/EventDetailModal.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-2xl\|shadow-2xl" app/src/components/AddEventModal.jsx app/src/components/EventDetailModal.jsx` sums to 0
    - `grep -c "border-ink-300" app/src/components/AddEventModal.jsx app/src/components/EventDetailModal.jsx` sums to 2
    - `grep -c "bg-purple-100 text-purple-700" app/src/components/EventDetailModal.jsx` returns 1 (school-slot exception untouched)
  </acceptance_criteria>
  <done>Both modal shells render rounded-t-md/md:rounded-md with a border-ink-300 bezel, visually matching ui/Modal.jsx.</done>
</task>

<task type="auto">
  <name>Task 2: LogInteractionModal.jsx inline cards</name>
  <files>app/src/components/LogInteractionModal.jsx</files>
  <read_first>
    app/src/components/LogInteractionModal.jsx lines 207, 238, 282
  </read_first>
  <action>
    Lines 207 and 238: `bg-white rounded-xl p-4 border border-ink-100 shadow-sm` (line 207) and `bg-white rounded-xl p-4 border border-ink-100 shadow-sm space-y-3` (line 238) both to the generic-card fix: rounded-xl to rounded-md, drop shadow-sm, border-ink-100 to border-ink-300 (`bg-white rounded-md p-4 border border-ink-300` and `bg-white rounded-md p-4 border border-ink-300 space-y-3` respectively).

    Line 282: `absolute z-10 mt-1 w-full bg-white border border-ink-200 rounded-lg shadow-lg overflow-hidden` — this is a floating autocomplete-suggestion dropdown, not a primary container. Per D-01's table (which scopes the shadow-to-border swap specifically to Card/SidePanel/Modal/Section "primary containers"), a dropdown menu's `shadow-lg` is a legitimate "floating above content" elevation cue, the same UI role as Sidebar.jsx's floating action buttons — LEAVE THIS LINE UNCHANGED. This is a documented, deliberate audit exception, not a skipped item.
  </action>
  <verify>
    <automated>cd app && grep -n "rounded-xl\|shadow-sm" src/components/LogInteractionModal.jsx | grep -v "^282:" | wc -l | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/src/components/LogInteractionModal.jsx` returns 0
    - `grep -c "rounded-xl" app/src/components/LogInteractionModal.jsx` returns 0
    - `grep -c "border-ink-300" app/src/components/LogInteractionModal.jsx` returns 2
    - `grep -c "shadow-lg" app/src/components/LogInteractionModal.jsx` returns 1 (dropdown exception, unchanged)
  </acceptance_criteria>
  <done>Both inline content cards use rounded-md/border-ink-300 with no shadow-sm; the autocomplete dropdown's shadow-lg is untouched by design.</done>
</task>

<task type="auto">
  <name>Task 3: KeepInTouchTab.jsx row card + TodayTab.jsx OverdueRow shape</name>
  <files>app/src/components/KeepInTouchTab.jsx, app/src/components/TodayTab.jsx</files>
  <read_first>
    app/src/components/KeepInTouchTab.jsx line 41
    app/src/components/TodayTab.jsx line 174 (OverdueRow function, near the top of the file)
  </read_first>
  <action>
    Both files share the identical row-card pattern already fixed on ApplicationsView.jsx/ExploreTab.jsx in Plan 07-04 (`bg-white rounded-xl px-4 py-3 shadow-sm border transition-shadow hover:shadow-md ${...}`). Apply the same rule: drop the persistent `shadow-sm`, `rounded-xl` to `rounded-md`, KEEP `hover:shadow-md` unchanged (D-01's interaction-feedback exception), base border color moves from ink-100 to ink-300:

    `KeepInTouchTab.jsx` line 41: `bg-white rounded-xl px-4 py-3 shadow-sm border transition-shadow hover:shadow-md ${overdue ? 'border-accent-200' : 'border-ink-100'}` to `bg-white rounded-md px-4 py-3 border transition-shadow hover:shadow-md ${overdue ? 'border-accent-200' : 'border-ink-300'}`.

    `TodayTab.jsx` line 174: `bg-white rounded-xl px-4 py-3 shadow-sm border transition-shadow hover:shadow-md ${overdue ? 'border-warning-300' : 'border-ink-100'}` to `bg-white rounded-md px-4 py-3 border transition-shadow hover:shadow-md ${overdue ? 'border-warning-300' : 'border-ink-300'}`. This is the ONLY edit this plan makes to TodayTab.jsx — do not touch `ActivitySection`, the stat-tile insertion point, or any motion/stagger wiring, all of which belong to Plan 07-08 (Wave 2, which depends on this plan).
  </action>
  <verify>
    <automated>cd app && grep -c "shadow-sm" src/components/KeepInTouchTab.jsx src/components/TodayTab.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/src/components/KeepInTouchTab.jsx` returns 0
    - `grep -c "rounded-xl" app/src/components/KeepInTouchTab.jsx` returns 0
    - `grep -c "hover:shadow-md" app/src/components/KeepInTouchTab.jsx app/src/components/TodayTab.jsx` sums to 2 (preserved)
    - `grep -c "border-ink-300" app/src/components/TodayTab.jsx` returns 1 (OverdueRow only — this task's sole TodayTab.jsx change)
  </acceptance_criteria>
  <done>KeepInTouchTab's row card and TodayTab's OverdueRow both render rounded-md with border-ink-300 and no persistent shadow; hover elevation feedback is preserved on both.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -rc "rounded-2xl\|shadow-2xl" app/src/components/AddEventModal.jsx app/src/components/EventDetailModal.jsx` sums to 0. `grep -rc "shadow-sm" app/src/components/LogInteractionModal.jsx app/src/components/KeepInTouchTab.jsx` sums to 0.
</verification>

<success_criteria>
Both calendar-event modal shells and the 3 remaining list-row card instances (LogInteractionModal x2, KeepInTouchTab, TodayTab's OverdueRow) match the bordered-flat shape system; the intentional dropdown-shadow and hover-elevation exceptions are documented, not silently dropped.
</success_criteria>

## Artifacts this phase produces

No new files or exported symbols. All 5 files get className edits only; `TodayTab.jsx`'s edit here is scoped to `OverdueRow`'s existing className string, not a new component or export.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-05-SUMMARY.md` when done
</output>
