---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ui/Card.jsx
  - app/src/components/ui/Section.jsx
  - app/src/components/ui/Button.jsx
  - app/src/components/ui/Badge.jsx
  - app/src/components/ui/Tabs.jsx
  - app/src/components/ui/ChipToggleGroup.jsx
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "Card.jsx renders bg-white rounded-md border border-ink-300 with no shadow-sm class"
    - "Section.jsx renders rounded-md with no shadow-sm class, keeping its existing tier-colored border"
    - "Button.jsx's base className uses rounded-md instead of rounded-xl"
    - "Badge.jsx, Tabs.jsx's outer container, and ChipToggleGroup.jsx's chip buttons use rounded-sm instead of rounded-full (none of these are genuinely circular elements)"
  artifacts:
    - path: "app/src/components/ui/Card.jsx"
      provides: "bordered-flat shape system default every Card consumer inherits for free"
    - path: "app/src/components/ui/Section.jsx"
      provides: "rounded-md Section chrome, tier-colored border preserved"
    - path: "app/src/components/ui/Button.jsx"
      provides: "rounded-md control shape"
    - path: "app/src/components/ui/Badge.jsx"
      provides: "rounded-sm pill shape"
  key_links:
    - from: "app/src/components/ui/Card.jsx"
      to: "every component importing Card"
      via: "shared primitive default — no consumer JSX edits needed"
      pattern: "rounded-md border border-ink-300"
</must_haves>
---

<objective>
Apply the D-01 shape-system tightening (rounded-xl/2xl/full → rounded-md/sm, soft shadow → border) to the remaining 6 shared `ui/` primitives not already touched by Plan 07-01 (Modal/SidePanel): `Card`, `Section`, `Button`, `Badge`, `Tabs`, `ChipToggleGroup`. This is the other half of "primitives change once, every consumer inherits for free" — Phase 1 precedent, now extended to shape.

Purpose: Card and Section are the two most-consumed container primitives in the app (dozens of call sites); fixing them here means the dozens of components that already use `<Card>`/`<Section>` need zero further edits for the shape system — only the ~40 files that hand-roll the equivalent Tailwind string instead of using the primitive (covered in Plans 07-04 through 07-07) need direct edits.
Output: All 8 shared `ui/` primitives (Card, Modal, SidePanel, Section, Button, Badge, Tabs, ChipToggleGroup) visibly reflect the new bordered-flat, tighter-radius shape system.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card.jsx + Section.jsx shape system</name>
  <files>app/src/components/ui/Card.jsx, app/src/components/ui/Section.jsx</files>
  <read_first>
    app/src/components/ui/Card.jsx (full file, 9 lines)
    app/src/components/ui/Section.jsx (full file — Section function at lines 12-25, current className at line 16)
    .planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md lines 40-63
  </read_first>
  <action>
    In app/src/components/ui/Card.jsx line 5: change `cn('bg-white rounded-xl shadow-sm border border-ink-100', className)` to `cn('bg-white rounded-md border border-ink-300', className)` — radius rounded-xl to rounded-md, drop shadow-sm, border-ink-100 to border-ink-300 per UI-SPEC's Card row (1px solid border-ink-300, hex #a2a7af, already the live token value — no index.css change needed).

    In app/src/components/ui/Section.jsx line 16: change the template literal `bg-white rounded-xl p-5 shadow-sm border ${border}` to `bg-white rounded-md p-5 border ${border}` — radius rounded-xl to rounded-md, drop shadow-sm. Deliberately KEEP the existing tier-keyed `border` variable (danger-200/warning-200/ink-200/accent-200 from line 13) rather than collapsing it to a flat border-ink-200 as UI-SPEC's generic Section table row suggests — Section's tier-colored border is an established, more-specific signal already shipped in Phases 2/3 (distinguishing danger/warning/ink/accent sections at a glance), and removing it would be a regression, not an improvement. This is the documented Claude's-Discretion call for "exact border-width/color pairing" per 07-CONTEXT.md's Claude's Discretion section.
  </action>
  <verify>
    <automated>cd app && grep -c "rounded-xl\|shadow-sm" src/components/ui/Card.jsx src/components/ui/Section.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-md border border-ink-300" app/src/components/ui/Card.jsx` returns 1
    - `grep -c "rounded-md p-5 border" app/src/components/ui/Section.jsx` returns 1
    - `grep -c "shadow-sm" app/src/components/ui/Card.jsx app/src/components/ui/Section.jsx` sums to 0
    - `grep -c "danger-200\|warning-200\|accent-200" app/src/components/ui/Section.jsx` returns 1 (tier-color border object untouched)
  </acceptance_criteria>
  <done>Card.jsx and Section.jsx both render rounded-md with no shadow-sm; Card uses border-ink-300, Section keeps its tier-colored border.</done>
</task>

<task type="auto">
  <name>Task 2: Button.jsx radius tightening</name>
  <files>app/src/components/ui/Button.jsx</files>
  <read_first>
    app/src/components/ui/Button.jsx (full file, 28 lines — base className at line 19)
  </read_first>
  <action>
    In app/src/components/ui/Button.jsx line 19: change `rounded-xl font-semibold transition-colors disabled:opacity-40 disabled:cursor-not-allowed` to `rounded-md font-semibold transition-colors disabled:opacity-40 disabled:cursor-not-allowed`. This is the one primitive not explicitly named in UI-SPEC's Radius Changes table, but it is a rectangular control (not a pill/chip), so it follows the same Card/Modal/Section/SidePanel rounded-xl to rounded-md tightening rather than the pill rounded-full to rounded-sm treatment — Button is a container/control in the "sharp edges" family, distinct from Badge/Tabs/ChipToggleGroup's pill shapes (handled in Task 3). No changes to VARIANTS or SIZES.
  </action>
  <verify>
    <automated>cd app && grep -c "rounded-md font-semibold" src/components/ui/Button.jsx | grep -qx 1 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-xl" app/src/components/ui/Button.jsx` returns 0
    - `grep -c "rounded-md font-semibold" app/src/components/ui/Button.jsx` returns 1
    - VARIANTS and SIZES objects byte-identical to before (no accidental edits)
  </acceptance_criteria>
  <done>Button.jsx's base control shape is rounded-md; all 4 variants and both sizes unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Badge.jsx, Tabs.jsx, ChipToggleGroup.jsx — pill shape tightening</name>
  <files>app/src/components/ui/Badge.jsx, app/src/components/ui/Tabs.jsx, app/src/components/ui/ChipToggleGroup.jsx</files>
  <read_first>
    app/src/components/ui/Badge.jsx (full file, 8 lines — className at line 3)
    app/src/components/ui/Tabs.jsx (full file, 23 lines — outer container className at line 6)
    app/src/components/ui/ChipToggleGroup.jsx (full file, 16 lines — button className at line 8)
    .planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md line 48 (pill/chip radius row)
  </read_first>
  <action>
    Per UI-SPEC's Radius Changes table row "Small chips/pills (Button variants, Tags): keep rounded-full only for genuinely circular elements (avatars, status dots); rounded-sm for pill-shaped elements" — none of these 3 primitives are circular, all are pill-shaped text chips, so all get `rounded-full` to `rounded-sm`:

    app/src/components/ui/Badge.jsx line 3: change `inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-semibold ${color}` to `inline-flex items-center gap-1 px-2 py-0.5 rounded-sm text-xs font-semibold ${color}`.

    app/src/components/ui/Tabs.jsx line 6: change `cn('inline-flex border border-ink-100 rounded-full overflow-hidden text-xs font-semibold bg-white', className)` to `cn('inline-flex border border-ink-100 rounded-sm overflow-hidden text-xs font-semibold bg-white', className)`. Leave the inner per-tab button classes at lines 12-15 unchanged (no radius classes there — the outer container's `overflow-hidden` already clips the buttons to match).

    app/src/components/ui/ChipToggleGroup.jsx line 8: change `px-2.5 py-1 rounded-full text-xs font-medium border transition-colors ${value.includes(tag) ...}` to `px-2.5 py-1 rounded-sm text-xs font-medium border transition-colors ${value.includes(tag) ...}` — only the `rounded-full` to `rounded-sm` swap, the conditional active/inactive color classes on the following lines stay unchanged.
  </action>
  <verify>
    <automated>cd app && grep -c "rounded-full" src/components/ui/Badge.jsx src/components/ui/Tabs.jsx src/components/ui/ChipToggleGroup.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-full" app/src/components/ui/Badge.jsx app/src/components/ui/Tabs.jsx app/src/components/ui/ChipToggleGroup.jsx` sums to 0
    - `grep -c "rounded-sm" app/src/components/ui/Badge.jsx` returns 1
    - `grep -c "rounded-sm" app/src/components/ui/Tabs.jsx` returns 1
    - `grep -c "rounded-sm" app/src/components/ui/ChipToggleGroup.jsx` returns 1
  </acceptance_criteria>
  <done>Badge, Tabs' segmented-control shell, and ChipToggleGroup chips all render rounded-sm instead of rounded-full.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -rc "rounded-xl\|shadow-sm" app/src/components/ui/Card.jsx app/src/components/ui/Section.jsx app/src/components/ui/Button.jsx` sums to 0. `grep -rc "rounded-full" app/src/components/ui/Badge.jsx app/src/components/ui/Tabs.jsx app/src/components/ui/ChipToggleGroup.jsx` sums to 0.
</verification>

<success_criteria>
All 6 primitives in this plan (Card, Section, Button, Badge, Tabs, ChipToggleGroup) render with the new tightened shape system; combined with Plan 07-01's Modal/SidePanel, all 8 shared `ui/` primitives are done.
</success_criteria>

## Artifacts this phase produces

No new files or exported symbols. This plan modifies existing className strings inside 6 already-exported primitives (`Card`, `Section`, `Button`, `Badge`, `Tabs`, `ChipToggleGroup`) — their component names, prop signatures, and export shapes are all unchanged; only the Tailwind radius/shadow classes they render change.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-02-SUMMARY.md` when done
</output>
