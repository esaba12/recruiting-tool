---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ContactsTable.jsx
  - app/src/components/jobBoards/CalendarView.jsx
  - app/src/components/charts/ChartTooltip.jsx
  - app/src/components/LoginPage.jsx
  - app/src/components/OutboxTab.jsx
  - app/src/components/AddToCalendarModal.jsx
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "ContactsTable.jsx's table wrapper, jobBoards/CalendarView.jsx's month card, charts/ChartTooltip.jsx's Recharts tooltip, LoginPage.jsx's auth card, OutboxTab.jsx's message row, and AddToCalendarModal.jsx's review card all render rounded-md/border-ink-300 with no persistent shadow-sm"
    - "LoginPage.jsx's sign-in/sign-up active-tab pill indicator (shadow-sm) is preserved unchanged — a toggle-state affordance, not a container"
  artifacts:
    - path: "app/src/components/charts/ChartTooltip.jsx"
      provides: "bordered-flat Recharts tooltip shared by BarChart/DonutChart/TrendChart"
  key_links:
    - from: "app/src/components/charts/ChartTooltip.jsx"
      to: "app/src/components/charts/BarChart.jsx, DonutChart.jsx, TrendChart.jsx"
      via: "<Tooltip content={<ChartTooltip .../>} /> — one shape fix, 3 chart consumers inherit for free"
      pattern: "ChartTooltip"
</must_haves>
---

<objective>
Continue the D-02 systematic shape-sweep across 6 remaining generic-card instances: a data table wrapper (`ContactsTable.jsx`), a calendar month-grid card (`jobBoards/CalendarView.jsx`), the shared Recharts tooltip (`charts/ChartTooltip.jsx` — one fix, inherited by all 3 chart wrappers), the auth card (`LoginPage.jsx`), a message-row card (`OutboxTab.jsx`), and a screenshot-review card (`AddToCalendarModal.jsx`).

Purpose: `ChartTooltip.jsx` is consumed by `BarChart.jsx`/`DonutChart.jsx`/`TrendChart.jsx` (all 3 already excluded from motion changes per D-05, but not from shape) — fixing its container once means every Recharts tooltip in the app (funnel, donut, trend, and the new stat-tile sparkline in Plan 07-08) is bordered-flat for free.
Output: Every remaining table/tooltip/dashboard-surface card in this cluster matches the shape system; the one legitimate non-container exception (LoginPage's active-tab pill) is preserved and documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactsTable.jsx + jobBoards/CalendarView.jsx</name>
  <files>app/src/components/ContactsTable.jsx, app/src/components/jobBoards/CalendarView.jsx</files>
  <read_first>
    app/src/components/ContactsTable.jsx line 133
    app/src/components/jobBoards/CalendarView.jsx line 32
  </read_first>
  <action>
    `ContactsTable.jsx` line 133: `bg-white rounded-xl border border-ink-100 shadow-sm overflow-x-auto` (the table's outer wrapper div) to `bg-white rounded-md border border-ink-300 overflow-x-auto`.
    `jobBoards/CalendarView.jsx` line 32: `bg-white rounded-xl p-5 shadow-sm border border-ink-100` (the month-grid card) to `bg-white rounded-md p-5 border border-ink-300`.
  </action>
  <verify>
    <automated>cd app && grep -c "shadow-sm\|rounded-xl" src/components/ContactsTable.jsx src/components/jobBoards/CalendarView.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm\|rounded-xl" app/src/components/ContactsTable.jsx app/src/components/jobBoards/CalendarView.jsx` sums to 0
    - `grep -c "border-ink-300" app/src/components/ContactsTable.jsx app/src/components/jobBoards/CalendarView.jsx` sums to 2
  </acceptance_criteria>
  <done>Both wrapper cards render rounded-md/border-ink-300 with no persistent shadow.</done>
</task>

<task type="auto">
  <name>Task 2: charts/ChartTooltip.jsx + LoginPage.jsx</name>
  <files>app/src/components/charts/ChartTooltip.jsx, app/src/components/LoginPage.jsx</files>
  <read_first>
    app/src/components/charts/ChartTooltip.jsx line 6
    app/src/components/LoginPage.jsx lines 66-74
  </read_first>
  <action>
    `charts/ChartTooltip.jsx` line 6: `bg-white border border-ink-100 shadow-sm rounded-xl px-3 py-2 text-xs` to `bg-white border border-ink-300 rounded-md px-3 py-2 text-xs`. This is a shared component imported by `charts/BarChart.jsx`, `DonutChart.jsx`, and `TrendChart.jsx` — fixing it here means all 3 chart wrappers' tooltips (and the new sparkline in Plan 07-08, if it reuses ChartTooltip) inherit the shape fix without any chart-wrapper edits.

    `LoginPage.jsx` line 66: `bg-white rounded-2xl border border-ink-100 shadow-sm p-6` (the auth card) to `bg-white rounded-md border border-ink-300 p-6`. Lines 69 and 73's `bg-white shadow-sm text-ink-900` (the active sign-in/sign-up segmented-toggle indicator pill) — LEAVE UNCHANGED. This `shadow-sm` is an interactive toggle-state indicator (the "active tab" thumb inside the pill-shaped `rounded-full bg-ink-50` selector at line 67), not a static container per D-01's Card/SidePanel/Modal/Section scope — the same category as a Button's hover/focus state, which D-01 explicitly says stays intact. Documented exception, not a skipped item.
  </action>
  <verify>
    <automated>cd app && grep -c "rounded-xl" src/components/charts/ChartTooltip.jsx | grep -qx 0 && grep -c "rounded-2xl" src/components/LoginPage.jsx | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-xl\|shadow-sm" app/src/components/charts/ChartTooltip.jsx` returns 0
    - `grep -c "rounded-2xl" app/src/components/LoginPage.jsx` returns 0
    - `grep -c "border-ink-300" app/src/components/LoginPage.jsx` returns 1 (auth card only)
    - `grep -c "shadow-sm" app/src/components/LoginPage.jsx` returns 2 (both active-tab-pill indicators, unchanged by design)
  </acceptance_criteria>
  <done>ChartTooltip renders bordered-flat (inherited by all 3 chart wrappers); LoginPage's auth card is bordered-flat while its active-tab pill indicator is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: OutboxTab.jsx + AddToCalendarModal.jsx</name>
  <files>app/src/components/OutboxTab.jsx, app/src/components/AddToCalendarModal.jsx</files>
  <read_first>
    app/src/components/OutboxTab.jsx line 9
    app/src/components/AddToCalendarModal.jsx line 175
  </read_first>
  <action>
    `OutboxTab.jsx` line 9: `bg-white rounded-xl px-4 py-3 shadow-sm border border-ink-100` (OutboxRow's message card) to `bg-white rounded-md px-4 py-3 border border-ink-300`.
    `AddToCalendarModal.jsx` line 175: `space-y-3 bg-white rounded-xl p-4 border border-ink-100 shadow-sm` (the extracted-event review card) to `space-y-3 bg-white rounded-md p-4 border border-ink-300`. Do not touch the file's textarea (`rounded-xl` at an earlier line — form-input radius is out of D-01's Card/SidePanel/Modal/Section/pill scope) or any other field.
  </action>
  <verify>
    <automated>cd app && grep -c "shadow-sm" src/components/OutboxTab.jsx src/components/AddToCalendarModal.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/src/components/OutboxTab.jsx app/src/components/AddToCalendarModal.jsx` sums to 0
    - `grep -c "rounded-md px-4 py-3 border border-ink-300" app/src/components/OutboxTab.jsx` returns 1
    - `grep -c "rounded-md p-4 border border-ink-300" app/src/components/AddToCalendarModal.jsx` returns 1
  </acceptance_criteria>
  <done>Both cards render rounded-md/border-ink-300 with no shadow-sm; the unrelated textarea is untouched.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -rc "shadow-sm\|rounded-xl\|rounded-2xl" app/src/components/ContactsTable.jsx app/src/components/jobBoards/CalendarView.jsx app/src/components/charts/ChartTooltip.jsx app/src/components/OutboxTab.jsx app/src/components/AddToCalendarModal.jsx` sums to 0. LoginPage.jsx's auth card is bordered-flat, its active-tab pill's shadow-sm untouched.
</verification>

<success_criteria>
All 6 files in this cluster render the bordered-flat shape system; the shared ChartTooltip fix propagates to all Recharts consumers with zero chart-wrapper edits; the one legitimate toggle-indicator exception is preserved and documented.
</success_criteria>

## Artifacts this phase produces

No new files or exported symbols. All 6 files get className edits only. `charts/ChartTooltip.jsx`'s existing export signature is unchanged — only its container className changes, and its 3 chart-wrapper consumers (BarChart/DonutChart/TrendChart) need zero edits to inherit the fix.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-06-SUMMARY.md` when done
</output>
