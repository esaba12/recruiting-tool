---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/charts/theme.js
  - app/src/components/jobBoards/helpers.js
  - app/src/components/jobBoards/RepoStats.jsx
  - app/src/components/jobBoards/UserProfileView.jsx
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "charts/theme.js's exported hex constants (CHART_SERIES, STATUS_CHART_COLORS, CHART_GRID, CHART_AXIS_TEXT, CHART_SURFACE, GRAPH_SURFACE_DARK, GRAPH_NODE_NEUTRAL_DARK, GRAPH_TEXT_DARK, GRAPH_LINK_DARK) exactly match the live @theme hex values in app/src/index.css"
    - "jobBoards/helpers.js's BUCKET_ACTIVE and BUCKET_TAG maps use only ink-/accent-/warning-/success-/danger- token classes, zero gray-/blue-/amber-/green-/red- literals"
    - "RepoStats.jsx and UserProfileView.jsx render their stat-pill/profile cards with rounded-md border border-ink-300, matching every other screen's shape system (the literal low-traffic example named in ROADMAP's Phase 7 success criterion 1)"
  artifacts:
    - path: "app/src/components/charts/theme.js"
      provides: "resynced hex mirror, closing the Phase 1-deferred item"
    - path: "app/src/components/jobBoards/helpers.js"
      provides: "token-based BUCKET_ACTIVE/BUCKET_TAG consumed by 5 other files for free"
  key_links:
    - from: "app/src/components/jobBoards/helpers.js"
      to: "TodayTab.jsx, panels/JobPanelBody.jsx, jobBoards/JobCard.jsx, jobBoards/RepoJobsView.jsx, panels/ApplicationPanelBody.jsx"
      via: "BUCKET_ACTIVE/BUCKET_TAG imported by all 5 — one fix, five consumers inherit for free"
      pattern: "BUCKET_ACTIVE\\[|BUCKET_TAG\\["
</must_haves>
---

<objective>
Close three independent, systematic-audit findings: (1) `charts/theme.js`'s hex mirror is stale against Phase 1's actual `index.css` token values (explicitly deferred to this phase per PROJECT.md's Key Decisions table); (2) `jobBoards/helpers.js`'s `BUCKET_ACTIVE`/`BUCKET_TAG` triage-bucket color maps use raw Tailwind `gray-/blue-/amber-/green-/red-` literals never remapped onto the 5-token system — a new find from this phase's D-02 systematic grep audit (not in the seed list, but consumed by 5 other files, so fixing it here fixes all 5 for free); (3) the two literal low-traffic screens (`RepoStats.jsx`, `UserProfileView.jsx`) ROADMAP's Phase 7 success criterion 1 names by example still carry the pre-Phase-1 generic-card pattern.

Purpose: charts/theme.js and jobBoards/helpers.js are both "one file, many consumers" fixes matching the phase's established leverage pattern; RepoStats/UserProfileView are literally named in the ROADMAP goal text as proof the sweep reached low-traffic screens.
Output: Chart colors match the real palette; Job Boards' triage-bucket chips use tokens everywhere they render; the two named low-traffic screens visually match the rest of the app.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
@app/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: charts/theme.js hex resync (D-02)</name>
  <files>app/src/components/charts/theme.js</files>
  <read_first>
    app/src/components/charts/theme.js (full file — every exported hex constant)
    app/src/index.css lines 12-63 (the live @theme block — source of truth)
  </read_first>
  <action>
    Replace every stale hex literal in app/src/components/charts/theme.js with the current live value from app/src/index.css's @theme block, keeping every export name, every inline comment's *token name* reference (e.g. "accent-600"), and the file's existing structure/comment prose otherwise unchanged — only the hex digits are wrong, not the design reasoning. Exact corrections (old to new):
    - `CHART_SERIES` (accent-600): `#e17f26` to `#c94e0a`
    - `STATUS_CHART_COLORS['🟢 Warm']` (success-500): `#3c9a46` to `#2f8f3d`
    - `STATUS_CHART_COLORS['🟡 Cooling']` (warning-500): `#d9a02b` to `#d99a12`
    - `STATUS_CHART_COLORS['🔴 Cold']` (danger-500): `#c94a4a` to `#b3312c`
    - `STATUS_CHART_COLORS['⭐ Champion']` (accent-500): `#f2994a` to `#f2680a`
    - `STATUS_CHART_COLORS['✅ Closed']` (ink-400): `#86868f` to `#666c74`
    - `CHART_GRID` (ink-100): `#e9e9eb` to `#e1e3e6`
    - `CHART_AXIS_TEXT` (ink-500): `#64646d` to `#4f555d`
    - `CHART_SURFACE` (canvas): `#fbf9f5` to `#f2f3f4`
    - `GRAPH_SURFACE_DARK` (ink-900): `#16171d` to `#101215`
    - `GRAPH_NODE_NEUTRAL_DARK` (ink-200): `#d3d3d7` to `#c7cbd0`
    - `GRAPH_TEXT_DARK` (ink-300): `#b0b0b7` to `#a2a7af`
    - `GRAPH_LINK_DARK` (ink-100): `#e9e9eb` to `#e1e3e6`

    After the hex swap, invoke the `dataviz` skill to re-run its six-check contrast validator against the corrected pairs, focused on the two comment blocks that cite specific ratios: (a) the `STATUS_CHART_COLORS` donut comment (currently claims a "contrast WARN on warning-500/accent-500... and a chroma-floor FAIL on ink-400") and (b) the `GRAPH_*_DARK` comment block (currently lists 5 specific ratios: "success-500 5.03:1 · warning-500 7.67:1 · danger-500 3.88:1 · accent-500 8.03:1 · ink-400 4.96:1"). Recompute each ratio against the corrected hex values and rewrite those two comment blocks' numbers in place — do not just copy the corrected hex without re-verifying whether the pass/warn/fail verdicts still hold, since the hex values moved (mostly darker/more saturated), which can shift a ratio across a WCAG threshold.
  </action>
  <verify>
    <automated>cd app && grep -c "#e17f26\|#3c9a46\|#d9a02b\|#c94a4a\|#f2994a\|#86868f\|#e9e9eb\|#64646d\|#fbf9f5\|#16171d\|#d3d3d7\|#b0b0b7" src/components/charts/theme.js | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#c94e0a" app/src/components/charts/theme.js` returns 1 (CHART_SERIES)
    - `grep -c "#101215" app/src/components/charts/theme.js` returns 1 (GRAPH_SURFACE_DARK — the exact example 07-CONTEXT.md D-02 called out)
    - None of the 12 stale hex values listed above appear anywhere in the file
    - All export names (`CHART_SERIES`, `STATUS_CHART_COLORS`, `CHART_GRID`, `CHART_AXIS_TEXT`, `CHART_SURFACE`, `GRAPH_SURFACE_DARK`, `GRAPH_NODE_NEUTRAL_DARK`, `GRAPH_TEXT_DARK`, `GRAPH_LINK_DARK`) unchanged (grep each name, count stays 1)
  </acceptance_criteria>
  <done>Every hex constant in theme.js matches its corresponding index.css @theme value exactly; the two ratio-citing comment blocks reflect freshly recomputed numbers.</done>
</task>

<task type="auto">
  <name>Task 2: jobBoards/helpers.js — BUCKET_ACTIVE/BUCKET_TAG token remap (audit find)</name>
  <files>app/src/components/jobBoards/helpers.js</files>
  <read_first>
    app/src/components/jobBoards/helpers.js lines 31-52 (BUCKET_CONFIG, BUCKET_ACTIVE, BUCKET_TAG)
    app/src/shared.jsx lines 39-48 (STAGE_COLOR — the established ink/accent/warning/success/danger semantic-mapping precedent this remap follows)
  </read_first>
  <action>
    `BUCKET_ACTIVE` and `BUCKET_TAG` (lines 40-52) use raw Tailwind `gray-/blue-/amber-/green-/red-` literals with zero token names — not in 07-UI-SPEC.md's seed list, but a direct hit under D-02's "systematic grep audit" mandate, and since 5 files import these two exported consts (TodayTab.jsx, panels/JobPanelBody.jsx, jobBoards/JobCard.jsx, jobBoards/RepoJobsView.jsx, panels/ApplicationPanelBody.jsx), fixing them here fixes all 5 consumers with zero consumer-file edits. Semantic mapping follows STAGE_COLOR's established ink=neutral/accent=primary-progress/warning=uncertain/success=positive/danger=declined convention:

    `BUCKET_ACTIVE` (bucket key: old to new):
    - `review`: `bg-gray-600 text-white border-gray-600` to `bg-ink-600 text-white border-ink-600`
    - `applying`: `bg-blue-600 text-white border-blue-600` to `bg-accent-600 text-white border-accent-600`
    - `maybe`: `bg-amber-500 text-white border-amber-500` to `bg-warning-600 text-white border-warning-600` (warning-500 white-text fails WCAG per Phase 1's contrast findings — use warning-600, the same fix RepoJobsView.jsx gets in Plan 07-04)
    - `applied`: `bg-green-600 text-white border-green-600` to `bg-success-600 text-white border-success-600`
    - `pass`: `bg-red-500 text-white border-red-500` to `bg-danger-600 text-white border-danger-600` (same white-text-contrast reasoning as maybe/warning — use the 600 step, not 500)

    `BUCKET_TAG` (bucket key: old to new):
    - `review`: `bg-gray-100 text-gray-600` to `bg-ink-100 text-ink-600`
    - `applying`: `bg-blue-100 text-blue-700` to `bg-accent-100 text-accent-700`
    - `maybe`: `bg-amber-100 text-amber-700` to `bg-warning-100 text-warning-800`
    - `applied`: `bg-green-100 text-green-700` to `bg-success-100 text-success-800`
    - `pass`: `bg-red-100 text-red-500` to `bg-danger-100 text-danger-600`

    Leave `LEVEL_COLOR` (line 4, GitHub contribution-heatmap green intensity scale) and `LANG_COLOR` (line 5, per-language dot colors) UNCHANGED and out of scope — both are literal-hex/categorical data-visualization palettes mimicking GitHub's own visual language (a heatmap intensity ramp and a many-way categorical language-distinguishing palette respectively), not UI chrome; forcing either onto the 5-family ink/accent/success/warning/danger token set would either break GitHub-visual-parity (LEVEL_COLOR) or reduce a 6-way distinguishable palette down to fewer usable hues (LANG_COLOR). This is a documented audit exception, not a skipped item.
  </action>
  <verify>
    <automated>cd app && grep -c "gray-\|blue-\|amber-\|green-600\|green-700\|red-500\|red-100" src/components/jobBoards/helpers.js | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-gray-\|bg-blue-\|bg-amber-\|border-gray-\|border-blue-\|border-amber-" app/src/components/jobBoards/helpers.js` returns 0
    - `grep -c "bg-ink-600 text-white border-ink-600" app/src/components/jobBoards/helpers.js` returns 1
    - `grep -c "bg-accent-600 text-white border-accent-600" app/src/components/jobBoards/helpers.js` returns 1
    - `LEVEL_COLOR` and `LANG_COLOR` array contents byte-identical to before
  </acceptance_criteria>
  <done>BUCKET_ACTIVE and BUCKET_TAG use only ink-/accent-/warning-/success-/danger- tokens; LEVEL_COLOR/LANG_COLOR explicitly untouched.</done>
</task>

<task type="auto">
  <name>Task 3: RepoStats.jsx + UserProfileView.jsx — low-traffic shape sweep</name>
  <files>app/src/components/jobBoards/RepoStats.jsx, app/src/components/jobBoards/UserProfileView.jsx</files>
  <read_first>
    app/src/components/jobBoards/RepoStats.jsx (full file, 70 lines)
    app/src/components/jobBoards/UserProfileView.jsx (full file — 6 card instances)
  </read_first>
  <action>
    Both files are named verbatim in 07-UI-SPEC.md's seed list as the literal low-traffic examples. Apply the identical generic-card fix everywhere `bg-white rounded-xl p-4/p-5 shadow-sm border border-ink-100` appears (drop shadow-sm, rounded-xl to rounded-md, border-ink-100 to border-ink-300):

    RepoStats.jsx: 3 occurrences at lines 29, 54, 66 (`bg-white rounded-xl p-4 shadow-sm border border-ink-100` to `bg-white rounded-md p-4 border border-ink-300`). Line 39's `bg-accent-50 border border-accent-100 rounded-xl p-4` (an already-tokened accent banner, no shadow) gets radius-only: rounded-xl to rounded-md. Line 43's tag chip `px-2.5 py-1 bg-white border border-accent-200 text-accent-800 text-xs rounded-full font-medium` gets `rounded-full` to `rounded-sm` (pill-shaped tech-stack tag, not circular — same D-01 pill rule as Badge/Tabs/ChipToggleGroup in Plan 07-02).

    UserProfileView.jsx: line 12's warning banner `p-4 bg-warning-50 border border-warning-200 rounded-xl text-sm text-warning-700` gets radius-only: rounded-xl to rounded-md (already correctly tokened, no shadow). Lines 19, 42, 49, 66, 88 (5 occurrences of `bg-white rounded-xl p-5 shadow-sm border border-ink-100`) get the full generic-card fix: `bg-white rounded-md p-5 border border-ink-300`.
  </action>
  <verify>
    <automated>cd app && grep -c "shadow-sm\|rounded-xl\|rounded-full" src/components/jobBoards/RepoStats.jsx src/components/jobBoards/UserProfileView.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/src/components/jobBoards/RepoStats.jsx app/src/components/jobBoards/UserProfileView.jsx` sums to 0
    - `grep -c "rounded-xl\|rounded-full" app/src/components/jobBoards/RepoStats.jsx app/src/components/jobBoards/UserProfileView.jsx` sums to 0
    - `grep -c "border-ink-300" app/src/components/jobBoards/RepoStats.jsx` returns 3
    - `grep -c "border-ink-300" app/src/components/jobBoards/UserProfileView.jsx` returns 5
  </acceptance_criteria>
  <done>Both named low-traffic screens render every stat-pill/profile card with rounded-md and border-ink-300, matching the rest of the app; already-tokened banners keep their color, radius-only.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -c "#e17f26\|#3c9a46\|#16171d" app/src/components/charts/theme.js` returns 0. `grep -c "bg-gray-\|bg-blue-\|bg-amber-" app/src/components/jobBoards/helpers.js` returns 0. `grep -c "shadow-sm\|rounded-xl" app/src/components/jobBoards/RepoStats.jsx app/src/components/jobBoards/UserProfileView.jsx` sums to 0.
</verification>

<success_criteria>
charts/theme.js exactly mirrors index.css; jobBoards/helpers.js's shared bucket-color maps use only the 5 locked token families (fixing 5 consumer files for free); the two named low-traffic screens visually match the rest of the reskinned app.
</success_criteria>

## Artifacts this phase produces

No new files or exported symbols. `charts/theme.js`'s 9 existing exports (`CHART_SERIES`, `STATUS_CHART_COLORS`, `CHART_GRID`, `CHART_AXIS_TEXT`, `CHART_SURFACE`, `GRAPH_SURFACE_DARK`, `GRAPH_NODE_NEUTRAL_DARK`, `GRAPH_TEXT_DARK`, `GRAPH_LINK_DARK`) and `jobBoards/helpers.js`'s `BUCKET_ACTIVE`/`BUCKET_TAG` exports keep their names and shapes — only their hex/class values change. `RepoStats.jsx`/`UserProfileView.jsx` get className-only edits.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-03-SUMMARY.md` when done
</output>
