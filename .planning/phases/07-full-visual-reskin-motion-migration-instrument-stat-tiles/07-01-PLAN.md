---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/src/components/layout/AppShell.jsx
  - app/src/components/NotFoundPage.jsx
  - app/src/lib/useMediaQuery.js
  - app/src/components/ui/Modal.jsx
  - app/src/components/ui/SidePanel.jsx
autonomous: false
requirements: [VIS-04, VIS-01]

must_haves:
  truths:
    - "app/package.json depends on `motion`, not `framer-motion`"
    - "All 5 files that import framer-motion (AppShell.jsx, NotFoundPage.jsx, Modal.jsx, SidePanel.jsx, useMediaQuery.js's comment) reference `motion`/`motion/react` instead"
    - "Modal.jsx and SidePanel.jsx render with rounded-md (not rounded-xl/2xl) corners and a 1px border-ink-300 in place of their old shadow-2xl"
    - "NetworkGraphTab.jsx and the 3 chart wrapper files still import zero motion package (D-05 exclusion holds after the swap)"
  artifacts:
    - path: "app/package.json"
      provides: "motion dependency replacing framer-motion"
    - path: "app/src/components/ui/Modal.jsx"
      provides: "motion/react import + rounded-md/border-ink-300 shape"
    - path: "app/src/components/ui/SidePanel.jsx"
      provides: "motion/react import + rounded-t-md/border-ink-300 shape"
  key_links:
    - from: "app/src/components/ui/Modal.jsx"
      to: "motion/react"
      via: "import { AnimatePresence, motion } from 'motion/react'"
      pattern: "from 'motion/react'"
</must_haves>
---

<objective>
Swap the `framer-motion` npm dependency for its official successor `motion` across all 5 current call sites (D-03), and apply the new shape-system defaults (D-01) to the two primitives whose files are already being touched for the import swap: `ui/Modal.jsx` and `ui/SidePanel.jsx`. Bundling these two primitives' shape edits here (rather than in the separate shape-system plan, 07-02) avoids a same-wave file conflict, since both concerns land in the same two files.

Purpose: Land the motion package migration first since Wave 2's stat-tile work (Plan 07-08) needs `motion/react` importable, and finish 2 of the phase's 4 primary "primitives change once" targets (Card/Section land in 07-02, in parallel).
Output: `motion` installed and imported everywhere `framer-motion` used to be; Modal/SidePanel visually match the new bordered-flat shape system.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify `motion` package legitimacy before install</name>
  <files></files>
  <what-built>Nothing yet — this phase has no RESEARCH.md (research is disabled project-wide), so no `## Package Legitimacy Audit` table exists to pre-clear this install. Per the Package Legitimacy Gate protocol, `motion` is treated as [ASSUMED] until manually confirmed.</what-built>
  <action>
    Do not run `npm install motion` yet. Open https://www.npmjs.com/package/motion in a browser and inspect the package listing before Task 2 proceeds with the install.
  </action>
  <how-to-verify>
    Visit https://www.npmjs.com/package/motion and confirm: (1) the package is published under the `framer`/`motion` maintainer org (same publisher as `framer-motion`), (2) weekly download count is in the millions (consistent with an actively-maintained, widely-used successor package, not a typosquat), (3) the README/description identifies it as "Framer Motion's successor" or equivalent. This matches 07-CONTEXT.md D-03's framing: "Motion is Framer Motion's own successor package with an API-compatible React import."
  </how-to-verify>
  <verify>
    <human-check>Confirm on npmjs.com/package/motion: publisher org matches framer-motion's, download count is in the millions/week, and the description/README self-identifies as Framer Motion's successor.</human-check>
  </verify>
  <acceptance_criteria>
    - npmjs.com/package/motion has been visually inspected and confirmed to match the 3 criteria above
  </acceptance_criteria>
  <resume-signal>Type "approved" once npmjs.com confirms the package is legitimate, or report what you found if something looks off.</resume-signal>
  <done>Package legitimacy confirmed on npmjs.com; Task 2 is cleared to run `npm install motion`.</done>
</task>

<task type="auto">
  <name>Task 2: Swap framer-motion for motion — dependency + 3 non-primitive import sites</name>
  <files>app/package.json, app/package-lock.json, app/src/components/layout/AppShell.jsx, app/src/components/NotFoundPage.jsx, app/src/lib/useMediaQuery.js</files>
  <read_first>
    app/package.json (current framer-motion entry, line 16)
    app/src/components/layout/AppShell.jsx (current `import { AnimatePresence, motion } from 'framer-motion'` at line 1)
    app/src/components/NotFoundPage.jsx (current `import { motion } from 'framer-motion'` at line 1)
    app/src/lib/useMediaQuery.js (current comment referencing framer-motion at line 1 — no actual import exists in this file, only a comment)
  </read_first>
  <action>
    In app/ (run from the app/ directory): remove `framer-motion` from dependencies and run `npm install motion` (let npm resolve and pin the current latest version into package.json/package-lock.json — do not hand-type a version number). Confirm afterward that package.json's dependencies block has a `"motion": "^X.Y.Z"` entry and no `"framer-motion"` entry.

    In app/src/components/layout/AppShell.jsx line 1: change `import { AnimatePresence, motion } from 'framer-motion'` to `import { AnimatePresence, motion } from 'motion/react'`. No other changes to this file — the `AnimatePresence`/`motion.div` usage below is API-identical.

    In app/src/components/NotFoundPage.jsx line 1: change `import { motion } from 'framer-motion'` to `import { motion } from 'motion/react'`. No other changes.

    In app/src/lib/useMediaQuery.js: this file has no actual framer-motion import (it only imports `useState`/`useEffect` from React), just a comment at line 1 referencing framer-motion by name. Reword the comment from "This hook exists because framer-motion cannot express different transform" to "This hook exists because CSS cannot express different motion/react transform" (or equivalent phrasing naming `motion` instead of `framer-motion`) — cosmetic comment-text fix only, no code change.
  </action>
  <verify>
    <automated>cd app && grep -rn "framer-motion" src/components/layout/AppShell.jsx src/components/NotFoundPage.jsx src/lib/useMediaQuery.js package.json | grep -v "^Binary" | wc -l | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c framer-motion app/package.json` returns 0
    - `grep -c "from 'motion/react'" app/src/components/layout/AppShell.jsx` returns 1
    - `grep -c "from 'motion/react'" app/src/components/NotFoundPage.jsx` returns 1
    - `grep -c framer-motion app/src/lib/useMediaQuery.js` returns 0
    - `app/node_modules/motion` exists after `npm install`
  </acceptance_criteria>
  <done>package.json depends on `motion`; AppShell.jsx and NotFoundPage.jsx import from `motion/react`; useMediaQuery.js's comment no longer names framer-motion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Modal.jsx + SidePanel.jsx — motion import swap + D-01 shape system</name>
  <files>app/src/components/ui/Modal.jsx, app/src/components/ui/SidePanel.jsx</files>
  <read_first>
    app/src/components/ui/Modal.jsx (full file — current `rounded-t-2xl md:rounded-2xl shadow-2xl` at line 30, framer-motion import at line 2)
    app/src/components/ui/SidePanel.jsx (full file — current `rounded-t-2xl md:rounded-none shadow-2xl` at line 57, framer-motion import at line 5, comment at line 3)
    .planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md lines 40-63 (Radius Changes / Shadow to Border Swap tables)
  </read_first>
  <action>
    In app/src/components/ui/Modal.jsx: line 2, change `import { AnimatePresence, motion } from 'framer-motion'` to `import { AnimatePresence, motion } from 'motion/react'`. Line 30's className string: change `'bg-white w-full rounded-t-2xl md:rounded-2xl shadow-2xl max-h-[90vh] overflow-y-auto'` to `'bg-white w-full rounded-t-md md:rounded-md border border-ink-300 max-h-[90vh] overflow-y-auto'` — radius tightened per UI-SPEC's Radius Changes table (Modal: rounded-2xl → rounded-md), shadow-2xl replaced with the 1px `border-ink-300` bezel per the Shadow → Border Swap table (hex `#a2a7af`, already the token's live value, no new hex needed).

    In app/src/components/ui/SidePanel.jsx: line 5, change `import { AnimatePresence, motion } from 'framer-motion'` to `import { AnimatePresence, motion } from 'motion/react'`. Line 3's comment "cannot select which framer-motion transform values apply" → reword to name `motion` instead of `framer-motion` (cosmetic only). Line 57's className string: change `'bg-white w-full md:w-[480px] md:max-w-[92vw] rounded-t-2xl md:rounded-none shadow-2xl max-h-[90vh] md:max-h-none md:h-full overflow-y-auto'` to `'bg-white w-full md:w-[480px] md:max-w-[92vw] rounded-t-md md:rounded-none border border-ink-300 max-h-[90vh] md:max-h-none md:h-full overflow-y-auto'` — mobile bottom-sheet top corners tighten from rounded-t-2xl to rounded-t-md (matching Modal's mobile treatment); `md:rounded-none` is left unchanged (the desktop panel is already flush/flat against the screen edge, which already matches the sharp-edges direction — nothing to tighten there); shadow-2xl replaced with `border border-ink-300` matching Modal and Card.

    After both edits, run `grep -rn "framer-motion\|react-force-graph\|motion/react" app/src/components/NetworkGraphTab.jsx app/src/components/charts/BarChart.jsx app/src/components/charts/DonutChart.jsx app/src/components/charts/TrendChart.jsx` and confirm it returns nothing for `framer-motion` or `motion/react` in any of those 4 files (D-05 exclusion — these must never import the motion package, before or after this migration).
  </action>
  <verify>
    <automated>cd app && grep -c "from 'motion/react'" src/components/ui/Modal.jsx src/components/ui/SidePanel.jsx | awk -F: '{sum+=$2} END {exit sum==2?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-t-2xl\|rounded-2xl\|shadow-2xl" app/src/components/ui/Modal.jsx` returns 0
    - `grep -c "rounded-t-2xl\|shadow-2xl" app/src/components/ui/SidePanel.jsx` returns 0
    - `grep -c "border-ink-300" app/src/components/ui/Modal.jsx` returns 1
    - `grep -c "border-ink-300" app/src/components/ui/SidePanel.jsx` returns 1
    - `grep -c "md:rounded-none" app/src/components/ui/SidePanel.jsx` returns 1 (unchanged desktop behavior)
    - `grep -rc "framer-motion" app/src/components/NetworkGraphTab.jsx app/src/components/charts/BarChart.jsx app/src/components/charts/DonutChart.jsx app/src/components/charts/TrendChart.jsx` sums to 0
    - `cd app && npm run build` exits 0
  </acceptance_criteria>
  <done>Modal.jsx and SidePanel.jsx both import from motion/react, render with rounded-md corners and a border-ink-300 bezel instead of shadow-2xl, and the D-05-excluded files remain untouched.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -rc framer-motion app/package.json app/src/components/layout/AppShell.jsx app/src/components/NotFoundPage.jsx app/src/components/ui/Modal.jsx app/src/components/ui/SidePanel.jsx` sums to 0 across all 5 files. `app/node_modules/motion` exists.
</verification>

<success_criteria>
`motion` replaces `framer-motion` in package.json and in every file that used to import it; Modal and SidePanel visually read as bordered-flat panels (rounded-md, border-ink-300) instead of soft-shadowed rounded-2xl panels; the NetworkGraphTab/Recharts D-05 exclusion is confirmed intact.
</success_criteria>

## Artifacts this phase produces

No new files or exported symbols. This plan modifies existing dependency declarations and className strings in place: `app/package.json`'s dependency list (framer-motion removed, motion added), and the import statements + shape-related className values inside `AppShell.jsx`, `NotFoundPage.jsx`, `Modal.jsx`, `SidePanel.jsx`, `useMediaQuery.js`. No new components, functions, or exports are created.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-01-SUMMARY.md` when done
</output>
