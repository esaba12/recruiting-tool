---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/panels/JobPanelBody.jsx
  - app/src/components/panels/ApplicationPanelBody.jsx
  - app/src/components/CalendarTab.jsx
  - app/src/lib/timeline.js
  - app/src/components/ExploreTab.jsx
  - app/src/components/ReferralCoverageTab.jsx
  - app/src/components/TimelineFindsPanel.jsx
  - app/src/components/ApplicationsView.jsx
  - app/src/components/jobBoards/RepoJobsView.jsx
  - app/src/components/jobBoards/JobCard.jsx
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "Zero indigo-*/orange-* Tailwind classes remain in JobPanelBody.jsx, ApplicationPanelBody.jsx, CalendarTab.jsx, lib/timeline.js, ExploreTab.jsx, ReferralCoverageTab.jsx, TimelineFindsPanel.jsx, or ApplicationsView.jsx (except the documented school-calendar-slot purple exception)"
    - "RepoJobsView.jsx's active 'Hide stale' toggle passes WCAG AA at >=4.5:1 (bg-warning-600, not bg-warning-500)"
    - "ApplicationsView.jsx's DuplicatesPanel renders with warning-* banner tokens and a danger-* destructive Archive button, not orange-*"
  artifacts:
    - path: "app/src/components/panels/JobPanelBody.jsx"
      provides: "accent-token Analyze button"
    - path: "app/src/lib/timeline.js"
      provides: "token-based Follow-up/Schedule/Reconnect badge colors, school-slot purple preserved"
  key_links:
    - from: "app/src/lib/timeline.js"
      to: "app/src/components/CalendarTab.jsx"
      via: "buildTimelineItems() badgeColor values rendered by CalendarTab's Feed view"
      pattern: "badgeColor"
</must_haves>
---

<objective>
Remap every remaining hardcoded `indigo-*`/`orange-*` Tailwind color across the 9 mid-traffic files 07-UI-SPEC.md's seed list names (plus `lib/timeline.js`, a systematic-audit find sharing the same "Follow-up" badge concern as `CalendarTab.jsx`), fix `RepoJobsView.jsx`'s pre-existing WCAG contrast failure, and fold in the generic-card shape fix for the same files where the audit already found it. This closes D-02's full named-seed-list scope plus one adjacent audit find.

Purpose: These are the highest-visibility off-token hits — AI-fit "Analyze" buttons on every job/application record, Calendar's Feed-view badges, and the Pipeline duplicate-detection banner all currently show colors outside the 5-token industrial palette.
Output: All AI-fit accent buttons, calendar-event badges, and the duplicates banner render in token colors; RepoJobsView's toggle passes WCAG AA.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
@app/src/shared.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI-fit "Analyze →" button — indigo to accent (JobPanelBody.jsx, ApplicationPanelBody.jsx)</name>
  <files>app/src/components/panels/JobPanelBody.jsx, app/src/components/panels/ApplicationPanelBody.jsx</files>
  <read_first>
    app/src/components/panels/JobPanelBody.jsx line 108
    app/src/components/panels/ApplicationPanelBody.jsx line 411
  </read_first>
  <action>
    Both files have an identical "Analyze →" AI-fit button pattern. In each file, change `bg-indigo-50 text-indigo-600 text-xs rounded-lg hover:bg-indigo-100 font-medium` (JobPanelBody.jsx line 108) and `bg-indigo-50 text-indigo-600 text-xs rounded-lg hover:bg-indigo-100 font-medium` (ApplicationPanelBody.jsx line 411) to `bg-accent-50 text-accent-600 text-xs rounded-lg hover:bg-accent-100 font-medium` in both — a direct 1:1 token swap (indigo-50 to accent-50, indigo-600 to accent-600, indigo-100 to accent-100), radius (`rounded-lg`) left as-is since it's not in D-01's rounded-xl/2xl/full scope.
  </action>
  <verify>
    <automated>cd app && grep -c "indigo-" src/components/panels/JobPanelBody.jsx src/components/panels/ApplicationPanelBody.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "indigo-" app/src/components/panels/JobPanelBody.jsx` returns 0
    - `grep -c "indigo-" app/src/components/panels/ApplicationPanelBody.jsx` returns 0
    - `grep -c "bg-accent-50 text-accent-600" app/src/components/panels/JobPanelBody.jsx app/src/components/panels/ApplicationPanelBody.jsx` sums to 2
  </acceptance_criteria>
  <done>Both Analyze buttons render in accent tokens, byte-identical logic otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar/badge indigo-purple-teal token remap (CalendarTab.jsx, lib/timeline.js, ExploreTab.jsx, ReferralCoverageTab.jsx, TimelineFindsPanel.jsx)</name>
  <files>app/src/components/CalendarTab.jsx, app/src/lib/timeline.js, app/src/components/ExploreTab.jsx, app/src/components/ReferralCoverageTab.jsx, app/src/components/TimelineFindsPanel.jsx</files>
  <read_first>
    app/src/components/CalendarTab.jsx lines 1-20 (FEED_LATER config), line 245 (Follow-up Badge), line 237 (school-slot Badge)
    app/src/lib/timeline.js lines 1-90 (EVENT_BADGE_COLOR, Follow-up/Schedule/Reconnect badgeColor assignments)
    app/src/components/ExploreTab.jsx line 179
    app/src/components/ReferralCoverageTab.jsx lines 95, 147
    app/src/components/TimelineFindsPanel.jsx line 15 (shape), line 42 (badge)
    app/src/shared.jsx lines 39-48 (STAGE_COLOR precedent for ink/accent/warning/success/danger semantic mapping)
  </read_first>
  <action>
    `lib/timeline.js` and `CalendarTab.jsx` share a "Follow-up" badge/dot concern — fix together for consistency:
    - `lib/timeline.js` line 11: `EVENT_BADGE_COLOR = { personal: 'bg-accent-100 text-accent-700', school: 'bg-purple-100 text-purple-700' }` — KEEP `school: 'bg-purple-100 text-purple-700'` UNCHANGED. This is a deliberate, documented exception: purple distinguishes the "School" Google Calendar connection from "Personal" (accent) per the Multi-Calendar feature shipped 2026-08-10 (see app/CLAUDE.md's Multi-calendar section — "accent = personal, purple = school"). Forcing this onto an existing token would make the two calendars visually indistinguishable, a functional regression, not a fix — the 5-token system has no 6th family for this genuinely-needed semantic distinction, so purple stays as an intentional exception here and at the 2 other sites below.
    - `lib/timeline.js` line 58: `badgeColor: 'bg-indigo-50 text-indigo-600'` (Follow-up) to `badgeColor: 'bg-accent-100 text-accent-700'`.
    - `lib/timeline.js` line 68: `badgeColor: 'bg-purple-50 text-purple-700'` (Schedule — unrelated to the calendar-slot distinction, a generic reminder-type tag) to `badgeColor: 'bg-ink-100 text-ink-600'` (matches STAGE_COLOR.Wishlist's neutral ink treatment).
    - `lib/timeline.js` (Reconnect badge, a few lines below Schedule): `badgeColor: 'bg-teal-50 text-teal-700'` to `badgeColor: 'bg-warning-100 text-warning-800'` (matches TodayTab's "Keep in Touch Due" Section which already uses accent="warning").
    - `CalendarTab.jsx` line 16: `{ key: 'followups', label: 'Follow-ups', dot: 'bg-indigo-500', chipActive: 'bg-indigo-500 text-white border-indigo-500' }` to `{ key: 'followups', label: 'Follow-ups', dot: 'bg-accent-500', chipActive: 'bg-accent-600 text-white border-accent-600' }`.
    - `CalendarTab.jsx` line 245: `<Badge label="Follow-up" color="bg-indigo-50 text-indigo-600" />` to `<Badge label="Follow-up" color="bg-accent-100 text-accent-700" />` (matches lib/timeline.js's Follow-up remap above).
    - `CalendarTab.jsx` line 237 (`event.slot === 'school' ? 'bg-purple-100 text-purple-700' : 'bg-accent-100 text-accent-700'`) — KEEP UNCHANGED, same documented school-slot exception.
    - `CalendarTab.jsx` also has 3 generic-card shape hits from the earlier sweep pass — lines 191, 343, and 353's `bg-white rounded-xl p-5/p-8 shadow-sm border border-ink-100` (or `border ${section.border}` at 353): drop shadow-sm, rounded-xl to rounded-md, and for lines 191/343 change border-ink-100 to border-ink-300; leave line 353's `${section.border}` variable itself unchanged (mirrors Section.jsx's tier-color pattern).

    `ExploreTab.jsx` line 179: `<Badge label={c.domain} color="bg-indigo-50 text-indigo-600" />` to `<Badge label={c.domain} color="bg-accent-100 text-accent-700" />`. Line 165's company-card container (a separate generic-card shape hit): `bg-white rounded-xl p-4 shadow-sm border border-ink-100 transition-colors ${c.website ? 'cursor-pointer hover:border-accent-300 hover:shadow-md' : ''}` to `bg-white rounded-md p-4 border border-ink-300 transition-colors ${c.website ? 'cursor-pointer hover:border-accent-300 hover:shadow-md' : ''}` — drop the persistent shadow-sm, KEEP hover:shadow-md (interaction-feedback exception).

    `ReferralCoverageTab.jsx` line 95: `rounded-xl border border-accent-100 bg-gradient-to-r from-accent-50 to-indigo-50 overflow-hidden` to `rounded-md border border-accent-100 bg-gradient-to-r from-accent-50 to-ink-100 overflow-hidden` (indigo-50 gradient end-stop to ink-100 — a decorative warm-to-cool gradient using only locked tokens; radius also tightened per D-01). Line 147: `text-[11px] text-indigo-600 mt-0.5 truncate` to `text-[11px] text-accent-600 mt-0.5 truncate`. Line 130's generic-card pattern (`bg-white rounded-xl p-4 shadow-sm border ...`) also gets the shape fix: rounded-xl to rounded-md, drop shadow-sm — KEEP the existing tier-colored border conditional (`border-danger-200`/`border-warning-200`/`border-ink-100` to `border-danger-200`/`border-warning-200`/`border-ink-300`, only the neutral-tier fallback moves from ink-100 to ink-300 to match Card's new border weight).

    `TimelineFindsPanel.jsx` line 15: `bg-white rounded-xl p-5 shadow-sm border border-accent-200` to `bg-white rounded-md p-5 border border-accent-200` (shape fix, accent-200 border kept as-is — it's this panel's own accent identity, not a generic ink border). Line 42: `<Badge label={SOURCE_LABEL[item.sourceType]} color="bg-indigo-50 text-indigo-600" />` to `<Badge label={SOURCE_LABEL[item.sourceType]} color="bg-accent-100 text-accent-700" />`.
  </action>
  <verify>
    <automated>cd app && grep -c "indigo-" src/components/CalendarTab.jsx src/lib/timeline.js src/components/ExploreTab.jsx src/components/ReferralCoverageTab.jsx src/components/TimelineFindsPanel.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "indigo-" app/src/components/CalendarTab.jsx app/src/lib/timeline.js app/src/components/ExploreTab.jsx app/src/components/ReferralCoverageTab.jsx app/src/components/TimelineFindsPanel.jsx` sums to 0
    - `grep -c "teal-" app/src/lib/timeline.js` returns 0
    - `grep -c "purple-" app/src/lib/timeline.js` returns 1 (school-slot exception only, on the EVENT_BADGE_COLOR line)
    - `grep -c "purple-" app/src/components/CalendarTab.jsx` returns 1 (school-slot exception only, line 237)
    - `grep -c "shadow-sm" app/src/components/CalendarTab.jsx app/src/components/ReferralCoverageTab.jsx app/src/components/TimelineFindsPanel.jsx app/src/components/ExploreTab.jsx` sums to 0
    - `grep -c "border-ink-300" app/src/components/ExploreTab.jsx` returns 1 (line 165's card)
  </acceptance_criteria>
  <done>Every Follow-up/domain/referral-chain/source badge and the two named gradients use token colors; school-calendar-slot purple is the sole documented, deliberate exception; folded-in shape fixes drop all shadow-sm on these 4 files (CalendarTab, ReferralCoverageTab, TimelineFindsPanel, ExploreTab).</done>
</task>

<task type="auto">
  <name>Task 3: ApplicationsView.jsx DuplicatesPanel remap + RepoJobsView.jsx WCAG fix + JobCard.jsx shape</name>
  <files>app/src/components/ApplicationsView.jsx, app/src/components/jobBoards/RepoJobsView.jsx, app/src/components/jobBoards/JobCard.jsx</files>
  <read_first>
    app/src/components/ApplicationsView.jsx lines 15-78 (full DuplicatesPanel function)
    app/src/components/jobBoards/RepoJobsView.jsx lines 165, 286-291
    app/src/components/ui/Button.jsx lines 4-7 (VARIANTS — danger variant reference: `bg-danger-600 text-white hover:bg-danger-700`)
    app/src/components/jobBoards/JobCard.jsx lines 1-33 (DeadlineBadge pills at lines 12/16, card container at line 33)
  </read_first>
  <action>
    `ApplicationsView.jsx`'s `DuplicatesPanel` (lines 15-78) is a "caution: duplicates found" alert banner, not a status badge — remap its container/text/Review-button to `warning-*` tokens (matches the banner's cautionary semantic), and its destructive "Archive" button to `danger-*` (matches Button.jsx's existing danger variant, since archiving is a bulk destructive action):
    - Line 42 container: `mb-4 bg-orange-50 border border-orange-200 rounded-xl p-4` to `mb-4 bg-warning-50 border border-warning-200 rounded-md p-4` (shape fix folded in: rounded-xl to rounded-md).
    - Line 45: `text-sm text-orange-800` to `text-sm text-warning-800`.
    - Line 49: `text-xs text-orange-600 mt-0.5` to `text-xs text-warning-700 mt-0.5`.
    - Line 53 (Review toggle button): `px-3 py-1.5 bg-white border border-orange-200 text-orange-700 text-xs rounded-lg hover:bg-orange-100 font-medium` to `px-3 py-1.5 bg-white border border-warning-200 text-warning-700 text-xs rounded-lg hover:bg-warning-100 font-medium`.
    - Line 58 (Archive button, destructive): `px-3 py-1.5 bg-orange-600 text-white text-xs rounded-lg hover:bg-orange-700 disabled:opacity-50 font-medium` to `px-3 py-1.5 bg-danger-600 text-white text-xs rounded-lg hover:bg-danger-700 disabled:opacity-50 font-medium`.
    - Line 69 (expanded group row): `bg-white rounded-lg border border-orange-100 px-3 py-2` to `bg-white rounded-lg border border-warning-100 px-3 py-2`.
    - Line 165's generic-card pattern (an application row, not part of DuplicatesPanel): `bg-white rounded-xl px-4 py-3 shadow-sm border transition-all cursor-pointer hover:shadow-md hover:border-accent-200 ${stale ? 'border-orange-200' : 'border-ink-100'}` to `bg-white rounded-md px-4 py-3 border transition-all cursor-pointer hover:shadow-md hover:border-accent-200 ${stale ? 'border-warning-200' : 'border-ink-300'}` — drop the persistent shadow-sm but KEEP `hover:shadow-md` unchanged (D-01's explicit exception: hover-triggered elevation feedback stays, only static container shadow is replaced), stale-row border orange-200 to warning-200, base border ink-100 to ink-300.
    - Line 186: `text-xs ${stale ? 'text-orange-600 font-medium' : 'text-ink-400'}` to `text-xs ${stale ? 'text-warning-700 font-medium' : 'text-ink-400'}`.

    `RepoJobsView.jsx` line 289 (the pre-existing WCAG failure flagged in STATE.md and Phase 1, explicitly deferred to this phase): `? 'bg-warning-500 text-white border-warning-500'` to `? 'bg-warning-600 text-white border-warning-600'` — 2.45:1 to 4.73:1, the exact value 07-UI-SPEC.md's seed-list table specifies as already validated in Phase 1. Do not touch the unselected/hover state on the following lines. Line 165's generic-card pattern (`bg-white rounded-xl p-4 shadow-sm border border-ink-100 flex items-center gap-3`) also gets the shape fix: `bg-white rounded-md p-4 border border-ink-300 flex items-center gap-3`.

    `JobCard.jsx` (a systematic-audit find — has no persistent `shadow-sm` to remove, but does have the un-tightened `rounded-xl`/`border-ink-100`/`rounded-full` D-01 targets): line 33's card container `bg-white rounded-xl border p-4 transition-all group` to `bg-white rounded-md border p-4 transition-all group`; the 3 conditional border colors on the following lines (`border-ink-100` for both the closed and default/stale cases) each move to `border-ink-300` — KEEP `hover:border-accent-200 hover:shadow-md hover:-translate-y-0.5` unchanged on the stale/default branches (interaction-feedback exception, no persistent shadow existed here to begin with). Lines 12 and 16 (`DeadlineBadge`'s two pill spans, `rounded-full bg-ink-50 text-ink-400` and `rounded-full ${DEADLINE_BADGE[tier]}`) both get `rounded-full` to `rounded-sm` — the same pill treatment as Badge.jsx/Tabs.jsx/ChipToggleGroup.jsx (Plan 07-02); these are small status pills, not circular elements.
  </action>
  <verify>
    <automated>cd app && grep -c "orange-" src/components/ApplicationsView.jsx | grep -qx 0 && grep -c "bg-warning-500 text-white border-warning-500" src/components/jobBoards/RepoJobsView.jsx | grep -qx 0 && grep -c "rounded-xl\|rounded-full" src/components/jobBoards/JobCard.jsx | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "orange-" app/src/components/ApplicationsView.jsx` returns 0
    - `grep -c "bg-danger-600 text-white hover:bg-danger-700" app/src/components/ApplicationsView.jsx` returns 1 (Archive button)
    - `grep -c "bg-warning-500 text-white border-warning-500" app/src/components/jobBoards/RepoJobsView.jsx` returns 0
    - `grep -c "bg-warning-600 text-white border-warning-600" app/src/components/jobBoards/RepoJobsView.jsx` returns 1
    - `grep -c "shadow-sm" app/src/components/ApplicationsView.jsx app/src/components/jobBoards/RepoJobsView.jsx` sums to 0
    - `grep -c "rounded-xl\|rounded-full" app/src/components/jobBoards/JobCard.jsx` returns 0
    - `grep -c "border-ink-300" app/src/components/jobBoards/JobCard.jsx` returns 2 (closed + stale/default branches)
    - `grep -c "rounded-sm" app/src/components/jobBoards/JobCard.jsx` returns 1 (or more — both DeadlineBadge pill spans, verified by reading the diff)
  </acceptance_criteria>
  <done>DuplicatesPanel renders in warning/danger tokens with no orange literals; RepoJobsView's active "Hide stale" toggle passes WCAG AA at 4.73:1; JobCard's container and deadline pills match the shape system.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -rc "indigo-\|orange-" app/src/components/panels/JobPanelBody.jsx app/src/components/panels/ApplicationPanelBody.jsx app/src/components/CalendarTab.jsx app/src/lib/timeline.js app/src/components/ExploreTab.jsx app/src/components/ReferralCoverageTab.jsx app/src/components/TimelineFindsPanel.jsx app/src/components/ApplicationsView.jsx` sums to 0. `grep -c "bg-warning-500 text-white border-warning-500" app/src/components/jobBoards/RepoJobsView.jsx` returns 0.
</verification>

<success_criteria>
Every named mid-traffic seed-list file plus lib/timeline.js uses only the 5 locked token families (with the single documented school-calendar-slot purple exception); RepoJobsView's WCAG failure is closed.
</success_criteria>

## Artifacts this phase produces

No new files or exported symbols. All 10 files in this plan get className/color-literal edits only — no new components, functions, or exports, and no changes to any existing function signature or component prop shape.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-04-SUMMARY.md` when done
</output>
