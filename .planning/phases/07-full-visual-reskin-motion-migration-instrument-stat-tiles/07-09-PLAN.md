---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 09
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08"]
files_modified: []
autonomous: true
requirements: [VIS-01, VIS-04, STAT-01]

must_haves:
  truths:
    - "A repo-wide grep for indigo-/purple-/orange- across app/src/components and app/src/lib returns only the 3 documented school-calendar-slot purple exceptions"
    - "A repo-wide grep for framer-motion returns zero matches anywhere in app/src"
    - "NetworkGraphTab.jsx and the 3 chart wrapper files (BarChart/DonutChart/TrendChart) import zero motion package, before and after the full phase merge"
    - "npm run build succeeds against the fully-merged Phase 7 tree"
    - "The full phase changeset matches the expected ~42-file list with zero unexplained diffs"
  artifacts: []
  key_links: []
</must_haves>
---

<objective>
Re-verify all 8 prior plans' deterministic gates hold together against the fully-merged Phase 7 tree (not just each plan's own branch state), confirm the D-02 systematic audit found and fixed every real hit with only the explicitly-documented exceptions left standing, re-confirm D-05's motion exclusion, run the production build, audit the exact changeset against the pre-phase base commit, and stage the end-of-phase human visual-verification checklist per `workflow.human_verify_mode=end-of-phase`.

Purpose: This is the phase's regression-sweep capstone, matching the pattern every prior phase in this milestone (1 through 6) closed with — confirm the merged whole, not just the sum of isolated plan branches.
Output: A verified, buildable, fully-reskinned Phase 7 tree with a staged human-check checklist and a dev server ready for review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide color/shape audit closure</name>
  <files></files>
  <read_first>
    All 8 prior plans' SUMMARY.md files in this phase directory (07-01-SUMMARY.md through 07-08-SUMMARY.md)
  </read_first>
  <action>
    Run `grep -rn "indigo-\|purple-\|orange-\|teal-\|amber-\|gray-[0-9]" app/src/components app/src/lib` and confirm every remaining hit is one of these documented, deliberate exceptions — nothing else:
    - `app/src/lib/timeline.js`'s `EVENT_BADGE_COLOR.school` (`bg-purple-100 text-purple-700`)
    - `app/src/components/CalendarTab.jsx` line ~237's `event.slot === 'school' ? 'bg-purple-100 text-purple-700' : ...`
    - `app/src/components/EventDetailModal.jsx` line ~43's `event.slot === 'school' ? 'bg-purple-100 text-purple-700' : ...`
    - `app/src/components/jobBoards/helpers.js`'s `LEVEL_COLOR` (GitHub contribution-heatmap hex ramp) and `LANG_COLOR` (per-language categorical dot colors) arrays
    If anything OTHER than these appears, it is a real gap this phase must close before the phase can be considered complete — fix it directly (apply the same ink/accent/success/warning/danger remapping logic used throughout Plans 07-03/07-04/07-07) rather than silently leaving it.

    Run `grep -rn "shadow-sm\|shadow-md\|shadow-lg\|shadow-xl\|shadow-2xl" app/src/components app/src/lib | grep -v "hover:shadow"` and confirm every remaining hit is one of these documented, deliberate exceptions:
    - `app/src/components/layout/Sidebar.jsx`'s 4 mobile floating-action-button `shadow-lg` instances (genuinely circular `w-12 h-12 rounded-full` FABs — elevation cue, not a container)
    - `app/src/components/LogInteractionModal.jsx`, `app/src/components/CompanyOnboarding.jsx`, `app/src/components/QuickScheduleModal.jsx`'s floating autocomplete-dropdown `shadow-lg` instances (transient overlay menus, same UI role as the FABs)
    - `app/src/components/LoginPage.jsx`'s 2 active-tab-pill `shadow-sm` instances (interactive toggle-state indicator, not a static container)
    - `app/src/components/NetworkGraphTab.jsx`'s dark-surface wrapper `shadow-sm` (deliberately kept as the sole visible depth cue on the ink-900 dark-island surface, per Plan 07-07)
    If anything else appears, it is a real gap — fix it using the same rounded-md/border-ink-300 (or tier-color) pattern used throughout this phase.

    Run `grep -rln "rounded-xl\|rounded-2xl" app/src/components/ui/ app/src/components/panels/` and confirm zero results (all 8 `ui/` primitives plus every `panels/*.jsx` file should be clear after Plans 07-01/07-02 — `panels/ContactPanelBody.jsx` was never in this phase's scope since it had no `rounded-xl`/`shadow-sm` hits to begin with, confirm that's still true).
  </action>
  <verify>
    <automated>cd app && grep -rc "indigo-\|orange-\|teal-\|amber-" src/components src/lib 2>/dev/null | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "indigo-\|orange-\|teal-\|amber-" app/src/components app/src/lib` sums to 0 (only the 2 `purple-` school-slot files plus `CalendarTab.jsx`/`EventDetailModal.jsx`'s 1 each remain, and `LEVEL_COLOR`/`LANG_COLOR` in helpers.js)
    - `grep -rc "purple-" app/src/components app/src/lib` sums to exactly 3 (lib/timeline.js, CalendarTab.jsx, EventDetailModal.jsx — one school-slot conditional each)
    - Every `shadow-*` hit outside the 4 documented exception categories above has been fixed, confirmed by re-reading the full grep output
    - `grep -rlc "rounded-xl\|rounded-2xl" app/src/components/ui/ app/src/components/panels/` returns empty (no files match)
  </acceptance_criteria>
  <done>The repo-wide sweep confirms zero unexplained off-token colors or unreplaced generic-card shadows remain; every exception is one of the 4 documented, deliberate categories.</done>
</task>

<task type="auto">
  <name>Task 2: D-05 motion exclusion + build + changeset audit</name>
  <files></files>
  <read_first>
    app/src/components/NetworkGraphTab.jsx, app/src/components/NetworkGraphView.jsx, app/src/components/charts/BarChart.jsx, app/src/components/charts/DonutChart.jsx, app/src/components/charts/TrendChart.jsx
  </read_first>
  <action>
    Run `grep -rn "framer-motion" app/src app/package.json` and confirm zero matches anywhere in the repo (the full migration is complete, not just the 5+2 files this phase directly touched).

    Run `grep -rn "framer-motion\|motion/react\|from 'motion'" app/src/components/NetworkGraphTab.jsx app/src/components/NetworkGraphView.jsx app/src/components/charts/BarChart.jsx app/src/components/charts/DonutChart.jsx app/src/components/charts/TrendChart.jsx` and confirm zero matches in all 5 files — D-05's exclusion held through the entire phase (Plans 07-01 and 07-07 both touched adjacent files without crossing into these 5).

    Run `cd app && npm run build` and confirm it exits 0 with no errors or warnings about missing `motion` exports.

    Run `git diff --stat --no-renames <base-commit>..HEAD -- app/` (base commit: `3a04c457b9db642bd6a3fecb632522bb117bc0cd`, the commit at the start of Phase 7 planning) and compare the file list against the union of all 8 prior plans' `files_modified` frontmatter (~42 unique paths: the 8 `ui/` primitives, `charts/theme.js` + `ChartTooltip.jsx`, `jobBoards/helpers.js` + `RepoStats.jsx` + `UserProfileView.jsx` + `RepoJobsView.jsx` + `JobCard.jsx` + `CalendarView.jsx` + `PreferencesPanel.jsx`, `panels/JobPanelBody.jsx` + `panels/ApplicationPanelBody.jsx`, `CalendarTab.jsx`, `lib/timeline.js`, `ExploreTab.jsx`, `ReferralCoverageTab.jsx`, `TimelineFindsPanel.jsx`, `ApplicationsView.jsx`, `AddEventModal.jsx`, `EventDetailModal.jsx`, `LogInteractionModal.jsx`, `KeepInTouchTab.jsx`, `TodayTab.jsx`, `ContactsTable.jsx`, `LoginPage.jsx`, `OutboxTab.jsx`, `AddToCalendarModal.jsx`, `NetworkGraphTab.jsx`, `QuickCaptureModal.jsx`, `DiscoverTab.jsx`, `CompanyOnboarding.jsx`, `lib/useMediaQuery.js`, `layout/AppShell.jsx`, `NotFoundPage.jsx`, `package.json`, `package-lock.json`, plus 2 new files `lib/statTiles.js` + `StatTileRow.jsx`). Flag any file in the diff NOT on this list, and any file on this list NOT in the diff, as a discrepancy to investigate before declaring the phase complete.
  </action>
  <verify>
    <automated>cd app && npm run build 2>&1 | tail -5 && grep -rc "framer-motion" src package.json | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "framer-motion" app/src app/package.json` sums to 0 repo-wide
    - `grep -rc "motion/react\|from 'motion'" app/src/components/NetworkGraphTab.jsx app/src/components/NetworkGraphView.jsx app/src/components/charts/BarChart.jsx app/src/components/charts/DonutChart.jsx app/src/components/charts/TrendChart.jsx` sums to 0
    - `cd app && npm run build` exits 0
    - `git diff --stat --no-renames 3a04c457b9db642bd6a3fecb632522bb117bc0cd..HEAD -- app/` file list matches the expected ~42-path set with zero unexplained entries
  </acceptance_criteria>
  <done>Zero framer-motion references remain anywhere in the app; the D-05 exclusion holds; the build is clean; the changeset matches the phase's full expected file list.</done>
</task>

<task type="auto">
  <name>Task 3: Stage end-of-phase human visual verification</name>
  <files></files>
  <read_first>
    .planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md (Checker Sign-Off section, lines 333-342)
  </read_first>
  <action>
    Start (or confirm already running) the dev server: `cd app && npm run dev` on localhost:3001. Stage the following manual checklist in this plan's SUMMARY.md for the human end-of-phase UAT pass (per `workflow.human_verify_mode=end-of-phase`, no blocking checkpoint task — this is documentation for the human's own review pass):
    1. Visit Today — confirm the new 3-tile row (Funnel / Next Deadline / Activity) renders above the existing funnel/donut/trend charts, tile numerals are in Mono (monospace), and the whole Section list visibly staggers in on page load/refresh.
    2. Visit Network, Grow, Pipeline (both Applications and Job Boards views), and Calendar — confirm cards/panels read as bordered-flat (visible border, no soft drop-shadow) with tighter corners than before.
    3. Open a contact, an application, and a job record (SidePanel) — confirm the panel shell is bordered-flat with rounded-md corners, not the old rounded-2xl/shadow-2xl look.
    4. Visit Job Boards' RepoStats and UserProfileView (the literal low-traffic examples) — confirm they visually match the rest of the app now, not a leftover pre-Phase-1 look.
    5. Toggle "Hide stale" in Job Boards — confirm the active state has clearly readable white text (WCAG fix).
    6. Open the Pipeline Applications view with duplicate applications present — confirm the DuplicatesPanel banner reads amber/warning-toned (not orange) with a red/danger Archive button.
    7. Compare Personal vs. School calendar events in Calendar/EventDetailModal — confirm they are still visually distinguishable (accent vs. purple), not collapsed to the same color.
    8. Confirm the Network Graph tab's force-directed canvas still renders and interacts normally (hover/click nodes) — no motion-migration regression.
    9. Screenshot 2-3 representative screens and compare against the stated "industrial/control-panel" aesthetic direction (sharp edges, dense data, bordered panels) per this repo's CLAUDE.md frontend-aesthetics directive.
  </action>
  <verify>
    <human-check>
      Dev server running at localhost:3001. Walk through the 9-step checklist above and confirm each one before treating Phase 7 as verified.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Dev server is reachable at localhost:3001
    - The 9-step checklist is recorded verbatim in `07-09-SUMMARY.md` for the human reviewer
  </acceptance_criteria>
  <done>Dev server is live and the end-of-phase manual verification checklist is staged in the SUMMARY for human review.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -rc "framer-motion" app/src app/package.json` sums to 0. `grep -rc "indigo-\|orange-\|teal-\|amber-" app/src/components app/src/lib` sums to 0. `git diff --stat --no-renames 3a04c457b9db642bd6a3fecb632522bb117bc0cd..HEAD -- app/` matches the expected ~42-file changeset. Dev server staged for human review.
</verification>

<success_criteria>
All 8 prior plans' deterministic gates re-verify green against the fully-merged tree; the D-02 systematic audit closes with only 4 documented exception categories remaining; D-05's motion exclusion holds repo-wide; the build is clean; the phase's full changeset is accounted for; the human end-of-phase UAT checklist is staged.
</success_criteria>

## Artifacts this phase produces

No new files. This is a verification-only plan (`files_modified: []`) — it audits and confirms the artifacts every prior plan in this phase already produced: `app/src/lib/statTiles.js` and `app/src/components/StatTileRow.jsx` (both new, from Plan 07-08), plus the ~40 existing files across Plans 07-01 through 07-08 whose className/dependency values were modified in place.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-09-SUMMARY.md` when done
</output>
