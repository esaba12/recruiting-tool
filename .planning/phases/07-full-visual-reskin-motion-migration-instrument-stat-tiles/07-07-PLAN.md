---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/NetworkGraphTab.jsx
  - app/src/components/QuickCaptureModal.jsx
  - app/src/components/DiscoverTab.jsx
  - app/src/components/CompanyOnboarding.jsx
  - app/src/components/jobBoards/PreferencesPanel.jsx
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "NetworkGraphTab.jsx's two wrapper cards (graph canvas frame, legend panel) render rounded-md; the force-graph canvas file itself (NetworkGraphView.jsx) is untouched and still imports zero motion package"
    - "QuickCaptureModal.jsx's 5 chat-bubble cards render rounded-md rounded-tl-sm with no persistent shadow-sm"
    - "DiscoverTab.jsx, CompanyOnboarding.jsx, and jobBoards/PreferencesPanel.jsx's decorative gradient headers end in ink-100, not indigo-50"
  artifacts:
    - path: "app/src/components/NetworkGraphTab.jsx"
      provides: "bordered-flat wrapper chrome around the untouched force-graph canvas"
  key_links:
    - from: "app/src/components/NetworkGraphTab.jsx"
      to: "app/src/components/NetworkGraphView.jsx"
      via: "wrapper div houses the canvas component — wrapper shape changes, canvas file untouched"
      pattern: "NetworkGraphView"
</must_haves>
---

<objective>
Finish the D-02 systematic shape/color sweep on the last cluster of files: `NetworkGraphTab.jsx`'s two wrapper cards (explicitly shape-only, re-confirming D-05's motion exclusion holds), `QuickCaptureModal.jsx`'s 5 chat-bubble cards, and 3 files sharing an identical decorative `accent-50 to indigo-50` gradient-header pattern (`DiscoverTab.jsx`, `CompanyOnboarding.jsx`, `jobBoards/PreferencesPanel.jsx`) plus `PreferencesPanel.jsx`'s own pill-shaped `Chip` component.

Purpose: `NetworkGraphTab.jsx` is the one file in this phase where a wrong move could accidentally violate D-05 (adding motion to the graph canvas) — this task is scoped narrowly to prove that didn't happen. The 3 gradient-header files share one exact 1-line fix, applied consistently.
Output: The Network Graph tab's chrome (not its canvas) matches the shape system; Quick Capture's AI-drafted chat bubbles are tightened; all 3 gradient headers use only locked tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NetworkGraphTab.jsx wrapper shape (D-05 re-verification)</name>
  <files>app/src/components/NetworkGraphTab.jsx</files>
  <read_first>
    app/src/components/NetworkGraphTab.jsx (full file, 33 lines)
  </read_first>
  <action>
    Line 14: `md:col-span-2 bg-ink-900 rounded-xl border border-ink-800 shadow-sm overflow-hidden` (the dark wrapper frame housing `NetworkGraphView`) to `md:col-span-2 bg-ink-900 rounded-md border border-ink-800 shadow-sm overflow-hidden` — radius only (rounded-xl to rounded-md); KEEP `shadow-sm` here since this frame sits on the dark ink-900 "self-contained dark island" surface (per app/CLAUDE.md's Network Graph documentation) where a border-only treatment would be nearly invisible against the same-family `border-ink-800` — shadow is the only visible depth cue on a dark background, unlike the light-canvas Card/Modal/SidePanel/Section cases. Line 18: `bg-white rounded-xl border border-ink-100 shadow-sm p-4` (the legend/detail panel, on the light canvas) to `bg-white rounded-md border border-ink-300 p-4` — full generic-card fix, this one IS on the light canvas so the border-only rule applies normally.

    Do NOT touch `NetworkGraphView.jsx` (the actual force-graph canvas component) at all — it is out of this plan's file scope entirely. After the 2 wrapper edits, run `grep -c "framer-motion\|motion/react" app/src/components/NetworkGraphTab.jsx app/src/components/NetworkGraphView.jsx` and confirm it returns 0 for both files — re-confirming D-05's exclusion holds after this phase's shape edits too, not just after Plan 07-01's motion edits.
  </action>
  <verify>
    <automated>cd app && grep -c "rounded-xl" src/components/NetworkGraphTab.jsx | grep -qx 0 && grep -c "framer-motion\|motion/react" src/components/NetworkGraphTab.jsx src/components/NetworkGraphView.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-xl" app/src/components/NetworkGraphTab.jsx` returns 0
    - `grep -c "rounded-md" app/src/components/NetworkGraphTab.jsx` returns 2
    - `grep -c "shadow-sm" app/src/components/NetworkGraphTab.jsx` returns 1 (dark-surface wrapper only, deliberately preserved)
    - `grep -c "framer-motion\|motion/react" app/src/components/NetworkGraphTab.jsx app/src/components/NetworkGraphView.jsx` sums to 0
  </acceptance_criteria>
  <done>Both wrapper cards tightened to rounded-md; the dark-surface frame's shadow-sm deliberately kept for depth-cue visibility; the canvas file untouched and confirmed motion-free.</done>
</task>

<task type="auto">
  <name>Task 2: QuickCaptureModal.jsx chat-bubble shape</name>
  <files>app/src/components/QuickCaptureModal.jsx</files>
  <read_first>
    app/src/components/QuickCaptureModal.jsx lines 314, 362, 440, 492, 515
  </read_first>
  <action>
    All 5 occurrences share the identical AI-drafted chat-bubble pattern: `max-w-[90%] bg-white border border-ink-200 rounded-2xl rounded-tl-sm shadow-sm p-3.5 space-y-3` (lines 314, 440, 492, 515) or `max-w-[95%] ...` (line 362, same suffix). At each of the 5 lines, change `rounded-2xl rounded-tl-sm shadow-sm` to `rounded-md rounded-tl-sm` — drop the persistent shadow-sm; tighten the bubble's main corners from rounded-2xl to rounded-md (matching the rest of the phase's radius scale) while explicitly KEEPING `rounded-tl-sm` unchanged, since that one sharp top-left corner is what gives the element its "chat bubble tail" read and is already consistent with the new sharper-corners direction (no change needed there). Leave `border border-ink-200` and all spacing/width classes unchanged on all 5 lines.
  </action>
  <verify>
    <automated>cd app && grep -c "rounded-2xl\|shadow-sm" src/components/QuickCaptureModal.jsx | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-2xl" app/src/components/QuickCaptureModal.jsx` returns 0
    - `grep -c "shadow-sm" app/src/components/QuickCaptureModal.jsx` returns 0
    - `grep -c "rounded-md rounded-tl-sm" app/src/components/QuickCaptureModal.jsx` returns 5
  </acceptance_criteria>
  <done>All 5 chat-bubble cards render rounded-md rounded-tl-sm with no persistent shadow; the bubble-tail corner is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Gradient-header sweep (DiscoverTab.jsx, CompanyOnboarding.jsx, jobBoards/PreferencesPanel.jsx) + PreferencesPanel Chip pill</name>
  <files>app/src/components/DiscoverTab.jsx, app/src/components/CompanyOnboarding.jsx, app/src/components/jobBoards/PreferencesPanel.jsx</files>
  <read_first>
    app/src/components/DiscoverTab.jsx lines 282, 480
    app/src/components/CompanyOnboarding.jsx line 15
    app/src/components/jobBoards/PreferencesPanel.jsx lines 17-21 (Chip component), line 18 (gradient header)
  </read_first>
  <action>
    All 3 files share the exact same decorative gradient header: `rounded-xl border border-accent-100 bg-gradient-to-r from-accent-50 to-indigo-50 overflow-hidden`. In each file (DiscoverTab.jsx line 480, CompanyOnboarding.jsx line 15, jobBoards/PreferencesPanel.jsx line 18) change this to `rounded-md border border-accent-100 bg-gradient-to-r from-accent-50 to-ink-100 overflow-hidden` — radius tightened (rounded-xl to rounded-md) and the gradient's second stop moves from the off-token `indigo-50` to the locked `ink-100` token, keeping the same warm-to-cool decorative gradient effect using only the 5 locked token families.

    `DiscoverTab.jsx` line 282 (a separate, unrelated card — the Recommended-list row, already tier-color-bordered like ReferralCoverageTab.jsx's Plan-07-04 fix): `bg-white rounded-xl p-4 shadow-sm border transition-shadow ${focus ...} ${r.status === 'gap' ? 'border-danger-200' : r.status === 'weak' ? 'border-warning-200' : 'border-ink-100'}` to `bg-white rounded-md p-4 border transition-shadow ${focus ...} ${r.status === 'gap' ? 'border-danger-200' : r.status === 'weak' ? 'border-warning-200' : 'border-ink-300'}` — drop shadow-sm, rounded-xl to rounded-md, only the neutral-tier fallback border moves from ink-100 to ink-300 (gap/weak tier colors unchanged).

    `jobBoards/PreferencesPanel.jsx`'s local `Chip` component (around line 19): change `px-3 py-1 rounded-full text-xs font-medium border transition-colors ${active ? ...}` to `px-3 py-1 rounded-sm text-xs font-medium border transition-colors ${active ? ...}` — this is the same pill-shaped chip-toggle pattern as `ui/ChipToggleGroup.jsx` (fixed in Plan 07-02), just a locally-defined duplicate in this file, so it needs its own `rounded-full` to `rounded-sm` fix rather than inheriting Plan 07-02's primitive change.
  </action>
  <verify>
    <automated>cd app && grep -c "indigo-" src/components/DiscoverTab.jsx src/components/CompanyOnboarding.jsx src/components/jobBoards/PreferencesPanel.jsx | awk -F: '{sum+=$2} END {exit sum==0?0:1}' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "indigo-" app/src/components/DiscoverTab.jsx app/src/components/CompanyOnboarding.jsx app/src/components/jobBoards/PreferencesPanel.jsx` sums to 0
    - `grep -c "to-ink-100" app/src/components/DiscoverTab.jsx app/src/components/CompanyOnboarding.jsx app/src/components/jobBoards/PreferencesPanel.jsx` sums to 3
    - `grep -c "shadow-sm" app/src/components/DiscoverTab.jsx` returns 0 (line 282's card, this task's only DiscoverTab.jsx edit)
    - `grep -c "rounded-full" app/src/components/jobBoards/PreferencesPanel.jsx` returns 0
  </acceptance_criteria>
  <done>All 3 gradient headers use ink-100 instead of indigo-50; DiscoverTab's Recommended-list row is bordered-flat; PreferencesPanel's local Chip pill is rounded-sm.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -rc "indigo-" app/src/components/DiscoverTab.jsx app/src/components/CompanyOnboarding.jsx app/src/components/jobBoards/PreferencesPanel.jsx` sums to 0. `grep -c "rounded-2xl\|shadow-sm" app/src/components/QuickCaptureModal.jsx` sums to 0. `grep -c "framer-motion\|motion/react" app/src/components/NetworkGraphTab.jsx app/src/components/NetworkGraphView.jsx` sums to 0.
</verification>

<success_criteria>
NetworkGraphTab's wrapper chrome is bordered-flat with the canvas file confirmed untouched; QuickCaptureModal's chat bubbles are tightened; all 3 gradient-header files and PreferencesPanel's local Chip use only locked tokens.
</success_criteria>

## Artifacts this phase produces

No new files or exported symbols. All 5 files get className edits only; `NetworkGraphView.jsx` (the force-graph canvas) is explicitly untouched by this plan.

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-07-SUMMARY.md` when done
</output>
