---
phase: 07-full-visual-reskin-motion-migration-instrument-stat-tiles
plan: 08
type: execute
wave: 2
depends_on: ["07-01", "07-05"]
files_modified:
  - app/src/lib/statTiles.js
  - app/src/components/StatTileRow.jsx
  - app/src/components/TodayTab.jsx
autonomous: true
requirements: [STAT-01, VIS-04]

must_haves:
  truths:
    - "Today's ActivitySection shows a 3-tile row (Funnel, Next Deadline, Activity) above the existing funnel/donut/trend charts, not replacing them"
    - "The Next Deadline tile only ever shows a real, cached, confirmed deadline (or 'No known deadlines') — it never invents/estimates a date for a rolling posting"
    - "Every tile numeral renders via the Mono primitive"
    - "Every Section in TodayTab's return (all 8 conditional ones, TimelineFindsPanel, and ActivitySection) fades/slides in on mount via motion/react, staggered ~100ms apart"
  artifacts:
    - path: "app/src/lib/statTiles.js"
      provides: "nextDeadlines(apps, limit) — cross-references Pipeline apps against the rec_job_deadlines cache"
    - path: "app/src/components/StatTileRow.jsx"
      provides: "3-tile instrument-panel readout row, additive above ActivitySection's existing charts"
  key_links:
    - from: "app/src/components/StatTileRow.jsx"
      to: "app/src/lib/statTiles.js"
      via: "nextDeadlines(apps) import"
      pattern: "nextDeadlines"
    - from: "app/src/lib/statTiles.js"
      to: "app/src/components/jobBoards/helpers.js"
      via: "reuses jobId()/daysUntilDeadline()/lsGet() — same rec_job_deadlines key format useJobDeadlines.js already writes"
      pattern: "jobId\\(|daysUntilDeadline\\("
</must_haves>
---

<objective>
Build STAT-01's instrument-panel stat-tile row (D-06/D-07/D-08) and add the D-04 staggered-reveal motion to Today's Section list and the new tile row. This is the phase's only Wave-2 plan — it depends on Plan 07-01 (the `motion` package must be installed for `motion/react` to resolve) and Plan 07-05 (which already tightened `TodayTab.jsx`'s `OverdueRow` shape; this plan's edits land in the same file afterward, avoiding a same-wave conflict).

Purpose: Deliver the third and final phase requirement (STAT-01) — Today's "glance" summary of funnel progress, the soonest real application deadline, and recent networking activity, all reusing the app's existing data pipelines (no new Supabase queries, no new AI calls).
Output: A new `lib/statTiles.js` helper, a new `StatTileRow.jsx` presentational component, and `TodayTab.jsx` wired to render it plus a staggered mount animation across its whole Section list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-CONTEXT.md
@.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md
@app/src/components/jobBoards/useJobDeadlines.js
@app/src/lib/timeline.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: lib/statTiles.js — deadline cross-reference helper (D-07)</name>
  <files>app/src/lib/statTiles.js</files>
  <read_first>
    app/src/components/jobBoards/helpers.js lines 1-2, 57-64, 108-112 (jobId, daysUntilDeadline, the re-exported lsGet)
    app/src/components/jobBoards/useJobDeadlines.js (confirms the cache is written under localStorage key `rec_job_deadlines`, keyed by `jobId({company, role})`)
    app/src/lib/timeline.js lines 86-97 (the closest existing precedent for cross-referencing `apps` against this same cache — do not copy its stage filter, see below)
    app/src/db.js lines 308-334 (fetchApplications' returned shape — confirms `app.company`, `app.role`, `app.triage`, `app.sourceRepo` field names)
  </read_first>
  <action>
    Create app/src/lib/statTiles.js exporting one function: `nextDeadlines(apps, limit = 3)`. Import `lsGet`, `jobId`, `daysUntilDeadline` from `../components/jobBoards/helpers.js` (all 3 already exported there — `lsGet` is itself a re-export from `scopedStorage.js`).

    Implementation requirements (D-07's literal spec): read the `rec_job_deadlines` cache via `lsGet('rec_job_deadlines') || {}`. Filter `apps` to entries where `a.sourceRepo` is truthy AND `a.triage` is exactly `'Needs Review'` or `'Applying'` — this is intentionally narrower than `lib/timeline.js`'s own deadline cross-reference (which uses `!isUntriaged(a)`, i.e. EXCLUDES Needs-Review apps) because D-07 specifically wants "applications you haven't yet acted on," the opposite scope. For each surviving app, look up `cache[jobId({ company: a.company, role: a.role })]`; keep only entries where that lookup has a truthy `.deadline` field (never fabricate one for a missing/rolling entry — `daysUntilDeadline` already returns `null` for a missing `.deadline`, filter those out too). For each surviving match, compute `days = daysUntilDeadline(info)` and build a plain object `{ company: a.company, role: a.role, deadline: info.deadline, days }`. Sort the resulting array ascending by `.days` (soonest first) and return the first `limit` entries. Return an empty array (not null/undefined) when nothing matches — the caller treats an empty array as the fail-soft "no known deadlines" case.
  </action>
  <verify>
    <automated>cd app && node -e "require('fs').existsSync('src/lib/statTiles.js') && console.log('PASS')" 2>/dev/null | grep -qx PASS && grep -c "export function nextDeadlines" src/lib/statTiles.js | grep -qx 1 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/lib/statTiles.js` exists and exports `nextDeadlines`
    - `grep -c "jobId\|daysUntilDeadline\|lsGet" app/src/lib/statTiles.js` returns 3 or more (all 3 imports used)
    - `grep -c "'Needs Review'" app/src/lib/statTiles.js` returns 1 and `grep -c "'Applying'" app/src/lib/statTiles.js` returns 1 (both triage values checked)
    - `grep -c "rec_job_deadlines" app/src/lib/statTiles.js` returns 1
    - Function never returns a `days` value for an entry lacking `info.deadline` (verified by code inspection: the `.filter` step precedes the `days` computation)
  </acceptance_criteria>
  <done>lib/statTiles.js exports nextDeadlines(apps, limit), reusing jobBoards/helpers.js's existing jobId/daysUntilDeadline/lsGet, filtered to Needs-Review/Applying apps with a sourceRepo and a confirmed cached deadline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: StatTileRow.jsx + wire into TodayTab.jsx's ActivitySection</name>
  <files>app/src/components/StatTileRow.jsx, app/src/components/TodayTab.jsx</files>
  <read_first>
    app/src/components/TodayTab.jsx lines 319-393 (ActivitySection — stageCounts/triagedApps/funnelData/trendData/trendWeeks already computed here)
    app/src/components/ui/Mono.jsx (Mono primitive — reused for every tile numeral)
    .planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-UI-SPEC.md lines 157-251 (Instrument-Panel Stat Tiles section — exact readout formats, styles, layout)
  </read_first>
  <action>
    Create app/src/components/StatTileRow.jsx, default-exporting `StatTileRow({ triagedApps, apps, trendData })`. It renders a `grid grid-cols-1 md:grid-cols-3 gap-4 pb-4 mb-4 border-b border-ink-200` container (matches UI-SPEC's Tile Row Layout section) holding 3 tiles, each a `<div className="rounded-md border border-ink-300 bg-white p-4">` per D-01's shape system:

    Tile 1 "Funnel": compute `appliedOrBeyond = triagedApps.filter(a => a.stage !== 'Wishlist').length` and `pct = triagedApps.length ? Math.round(appliedOrBeyond / triagedApps.length * 100) : 0`. Render a `text-xs font-semibold text-ink-600` title "Funnel", a `<Mono>` numeral showing `appliedOrBeyond`, a small `text-xs text-ink-400` caption like "applied of N ({pct}%)", and a thin `h-1 bg-ink-100 rounded-sm overflow-hidden` bar with an inner `bg-accent-600` fill sized to `pct}%` width as the tick-mark/segmented motif D-06 calls for.

    Tile 2 "Next Deadline": import `nextDeadlines` from `../lib/statTiles.js`, call `nextDeadlines(apps, 1)`. If it returns an entry, render the title, a `<Mono>` numeral showing `{days}d`, and a caption with the company name; add the thin gradient tick bar UI-SPEC suggests (`h-1 bg-gradient-to-r from-danger-500 to-warning-500 rounded-sm`, width scaled proportionally e.g. `Math.max(4, Math.min(100, 100 - (days / 30) * 100))%`). If the array is empty, render "No known deadlines" in `text-sm text-ink-400` instead of a Mono numeral — never a placeholder countdown (D-07's explicit fail-soft rule).

    Tile 3 "Activity": take `trendData.slice(-6)` (the trailing 6 of the 10 weeks `ActivitySection` already computed), sum their `.count` into a `<Mono>` numeral with a "last 6 weeks" caption, and render a 6-column micro bar-graph below it (`flex items-end gap-1 h-6`, each week a `flex-1 bg-accent-400 rounded-sm` div with `style={{height: ...}}` proportional to that week's count against the 6-week max, floored at a visible minimum height so a zero week still shows a sliver) — a CSS-based sparkline per UI-SPEC's explicitly-sanctioned "Option A" compact form, deliberately not reusing `charts/BarChart.jsx` (which has no axis-hiding prop and would require modifying a primitive shared by 3 other charts just for this one compact use).

    Wrap each of the 3 tiles in its own `<motion.div>` (import `{ motion }` from `'motion/react'`) using a local `container`/`item` variants pair (`hidden`/`show` with `staggerChildren: 0.1` on the container, `opacity`/`y` fade on each item) so the 3 tiles stagger in together — a small, self-contained stagger independent of TodayTab's own Section-list stagger (Task 3), matching CONTEXT.md's Claude's-Discretion note that exact stagger-scope wiring is an implementation detail.

    Then wire it into `TodayTab.jsx`'s `ActivitySection` function (lines 319-393): import `StatTileRow` from `./StatTileRow.jsx`, and insert `<StatTileRow triagedApps={triagedApps} apps={apps} trendData={trendData} />` as the FIRST child inside the returned `<Section title="Activity" accent="ink" icon={Activity}>`, immediately before the existing `{hasRecruitingActivity && (...)}` funnel-chart block — additive, above the existing charts, per D-06 and STAT-01's "reusing the existing Recharts wiring, not replacing it" constraint. `triagedApps` and `trendData` are already local variables in `ActivitySection`'s scope at that point; `apps` is already a prop of `ActivitySection`.
  </action>
  <verify>
    <automated>cd app && node -e "require('fs').existsSync('src/components/StatTileRow.jsx') && console.log('PASS')" 2>/dev/null | grep -qx PASS && grep -c "StatTileRow" src/components/TodayTab.jsx | grep -qx 2 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/components/StatTileRow.jsx` exists, default-exports a component, and imports `Mono` and `nextDeadlines`
    - `grep -c "from 'motion/react'" app/src/components/StatTileRow.jsx` returns 1
    - `grep -c "No known deadlines" app/src/components/StatTileRow.jsx` returns 1
    - `grep -c "rounded-md border border-ink-300" app/src/components/StatTileRow.jsx` returns 3 (one per tile)
    - `grep -c "import StatTileRow" app/src/components/TodayTab.jsx` returns 1
    - `grep -c "<StatTileRow" app/src/components/TodayTab.jsx` returns 1, positioned before `{hasRecruitingActivity` in ActivitySection's JSX (verified by reading the diff — StatTileRow's line number is less than hasRecruitingActivity's line number within the same return block)
    - `cd app && npm run build` exits 0
  </acceptance_criteria>
  <done>ActivitySection renders the 3-tile row above its existing funnel/donut/trend charts; the Next Deadline tile fails soft to "No known deadlines" when the cache has no match for a Needs-Review/Applying app.</done>
</task>

<task type="auto">
  <name>Task 3: TodayTab.jsx Section-list stagger (D-04)</name>
  <files>app/src/components/TodayTab.jsx</files>
  <read_first>
    app/src/components/TodayTab.jsx lines 441-513 (the full return block — 8 conditional `<Section>` blocks at lines 444, 451, 459, 466, 476, 483, 490, 497, plus the TimelineFindsPanel conditional at line 504 and the unconditional `<ActivitySection>` call at line 511)
    app/src/components/NotFoundPage.jsx lines 1-13 (the existing `container`/`rise` variants precedent already shipped in this codebase — replicate the same shape, just import from `motion/react` instead of `framer-motion`)
  </read_first>
  <action>
    Add `import { motion } from 'motion/react'` to TodayTab.jsx's import block. Define two local variant objects near the top of the file (module scope, same pattern as NotFoundPage.jsx's `container`/`rise`, but with lighter values matching UI-SPEC's "~100ms per section" cue): `container = { hidden: {}, show: { transition: { staggerChildren: 0.1 } } }` and `rise = { hidden: { opacity: 0, y: 8 }, show: { opacity: 1, y: 0, transition: { duration: 0.3 } } }`.

    Change line 441's `<div className="space-y-4">` to `<motion.div className="space-y-4" variants={container} initial="hidden" animate="show">`, and its matching closing `</div>` (the one immediately before the component's final `)` `}`, after the last `</SidePanel>`) to `</motion.div>`.

    For each of the 8 conditional Section blocks at lines 444, 451, 459, 466, 476, 483, 490, 497 (each shaped `{someList.length > 0 && (<Section ...>...</Section>)}`) and the TimelineFindsPanel conditional at line 504 (`{!isDemoMode && !allEmpty && (<TimelineFindsPanel .../>)}`): wrap the JSX element being conditionally rendered in a `<motion.div variants={rise}>...</motion.div>` — i.e. insert `<motion.div variants={rise}>` immediately after the `(` that opens each conditional's JSX and `</motion.div>` immediately before the matching `)`. Do the same for the unconditional `<ActivitySection .../>` call at line 511 — wrap it as `<motion.div variants={rise}><ActivitySection contacts={contacts} apps={apps} interactions={interactions} /></motion.div>`.

    Do NOT wrap line 442's `<EmptyState .../>` (the all-caught-up message — not part of the "Section list", shown instead of it) or either `<SidePanel>`/`<LogInteractionModal>` block near the end of the return (those are on-demand overlays, not part of the mount-time list; being non-motion children of the outer `motion.div`, they will not consume `variants={container}` and will not receive any stagger animation — this is correct, verify no `motion.div` wrapper was accidentally added around either of them).
  </action>
  <verify>
    <automated>cd app && grep -c "variants={rise}" src/components/TodayTab.jsx | grep -qx 10 && grep -c "variants={container}" src/components/TodayTab.jsx | grep -qx 1 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'motion/react'" app/src/components/TodayTab.jsx` returns 1
    - `grep -c "variants={rise}" app/src/components/TodayTab.jsx` returns 10 (8 conditional Sections + TimelineFindsPanel + ActivitySection wrapper — StatTileRow's own internal tile stagger from Task 2 uses a separate local variants object defined inside StatTileRow.jsx, not this file's `rise`, so it does not add to this count)
    - `grep -c "variants={container}" app/src/components/TodayTab.jsx` returns 1 (the outer motion.div only)
    - `grep -c "<SidePanel" app/src/components/TodayTab.jsx` unchanged from before this task (2 — contact panel, application panel), neither wrapped in a new motion.div
    - `cd app && npm run build` exits 0
  </acceptance_criteria>
  <done>TodayTab's outer container and all 10 mount-time blocks (8 Sections, TimelineFindsPanel, ActivitySection) fade/slide in staggered ~100ms apart on mount; on-demand SidePanel/LogInteractionModal overlays are unaffected.</done>
</task>

</tasks>

<verification>
`cd app && npm run build` succeeds. `grep -c "from 'motion/react'" app/src/components/TodayTab.jsx app/src/components/StatTileRow.jsx` sums to 2. `app/src/lib/statTiles.js` exports `nextDeadlines`. Visiting Today in dev with at least one Needs-Review/Applying application that has a cached deadline shows a real day count in the Next Deadline tile; with none, it shows "No known deadlines".
</verification>

<success_criteria>
Today's Activity section shows the 3-tile instrument-panel row above its existing charts, built entirely on existing data pipelines with zero new Supabase/AI calls; the whole Section list (plus the new tile row) staggers in on mount via the `motion` package.
</success_criteria>

## Artifacts this phase produces

New files:
- `app/src/lib/statTiles.js` — exports `nextDeadlines(apps, limit = 3)`, a function returning `Array<{ company, role, deadline, days }>` sorted soonest-first, cross-referencing `apps` against the `rec_job_deadlines` localStorage cache via `jobId()`/`daysUntilDeadline()`/`lsGet()` (all reused from `app/src/components/jobBoards/helpers.js`).
- `app/src/components/StatTileRow.jsx` — default-exports `StatTileRow({ triagedApps, apps, trendData })`, a presentational component rendering the 3-tile instrument-panel row (Funnel, Next Deadline, Activity), each tile using the `Mono` primitive and the new shape system, with its own internal `motion/react` stagger.

Modified (no new exports, existing signature preserved): `app/src/components/TodayTab.jsx` — `ActivitySection` gains a `<StatTileRow>` call; the file's default export `TodayTab` and every other named local function keep their existing signatures; two new module-scope `container`/`rise` motion-variant objects are added (not exported).

<output>
Create `.planning/phases/07-full-visual-reskin-motion-migration-instrument-stat-tiles/07-08-SUMMARY.md` when done
</output>
