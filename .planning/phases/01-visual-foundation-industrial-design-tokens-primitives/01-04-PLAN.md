---
phase: 01-visual-foundation-industrial-design-tokens-primitives
plan: 4
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - app/src/components/jobBoards/JobCard.jsx
  - app/src/components/jobBoards/JobDetailModal.jsx
autonomous: true
requirements: [VIS-02]

must_haves:
  truths:
    - "Job Boards' card grid shows posting dates, deadline-countdown badges, and stale-listing day counts in IBM Plex Mono — the third of RESEARCH.md's named VIS-02 target directories, completing the dense-data typography rollout alongside Network and Pipeline."
    - "JobDetailModal's posted-date, no-update-in-Nd, and deadline-countdown text render in Mono, matching the same treatment applied to the card grid so a job's summary and detail views are typographically consistent."
    - "All existing deadline-urgency color coding (urgent/soon/known/rolling badge backgrounds) is unchanged — Mono adds typography only, never a color decision."
  artifacts: []
  key_links:
    - "app/src/components/ui/Mono.jsx (created in 01-02-PLAN.md, Wave 1) -> new import statements in JobCard.jsx and JobDetailModal.jsx — this plan cannot start before 01-02 lands, hence Wave 2."
    - "helpers.js's urgencyTier()/daysUntilDeadline()/jobAgeDays() (unchanged) -> the label strings these two files already build from them, now wrapped in Mono at the render call site rather than changed at the computation layer."
---

<objective>
Wrap the dense date/deadline-countdown text in `jobBoards/JobCard.jsx` (the card grid's posted-date, deadline badge, and stale-listing day count) and `jobBoards/JobDetailModal.jsx` (the detail panel's posted-date, no-update-days, and deadline-countdown text) in the `<Mono>` primitive from `01-02-PLAN.md` — completing VIS-02's rollout across the third and last directory RESEARCH.md named (`ContactsTable.jsx`/`PipelineTab.jsx` covered in `01-03-PLAN.md`).

Purpose: Job Boards is the app's most-developed feature and its "real deadline extraction... I never want to miss a deadline" capability lives entirely in these dense, glanceable date/countdown strings — exactly the VIS-02 target: "deadlines... in tables and panels."
Output: `JobCard.jsx` and `JobDetailModal.jsx` with every posted-date/deadline-countdown/stale-day-count string wrapped in `<Mono>`, all existing urgency-tier color coding unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-PATTERNS.md
@app/src/components/jobBoards/JobCard.jsx
@app/src/components/jobBoards/JobDetailModal.jsx
@app/src/components/ui/Mono.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap JobCard.jsx's date/deadline/stale text in Mono</name>
  <files>app/src/components/jobBoards/JobCard.jsx</files>
  <read_first>
    app/src/components/jobBoards/JobCard.jsx (current file, 96 lines — `DeadlineBadge` at lines 10-17, `job.dateAdded` span at line 64, stale badge at lines 66-71); app/src/components/ui/Mono.jsx; 01-PATTERNS.md's "app/src/components/jobBoards/*" section (lines 244-247) — names this directory as an in-scope VIS-02 target using the identical `fmt()`-in-`&lt;span&gt;` wrap pattern established in `01-03-PLAN.md`.
  </read_first>
  <action>
    Add `import Mono from '../ui/Mono.jsx'` to the import block (note the `../` depth — this file lives at `components/jobBoards/JobCard.jsx`, one level deeper than `components/*.jsx`, matching the existing `import { BUCKET_ICON, LOCATION_ICON } from '../../lib/icons.js'` relative-depth convention already in this file). Make 3 wraps, changing typography only, never the surrounding color/badge classes:
    1. In the `DeadlineBadge` function, the `label` variable (`days &lt; 0 ? 'Closed' : days === 0 ? 'Closes today' : \`Closes in ${days}d\``) is currently rendered as plain text inside the badge's `&lt;span&gt;`: `&lt;span className={...}&gt;⏰ {label}&lt;/span&gt;`. Wrap just `{label}` in `&lt;Mono&gt;`, i.e. `⏰ &lt;Mono&gt;{label}&lt;/Mono&gt;`, keeping the emoji, the outer span, and its `DEADLINE_BADGE[tier]` conditional background/text color exactly as-is.
    2. Line 64's `{job.dateAdded &amp;&amp; &lt;span className="text-xs text-ink-400"&gt;{job.dateAdded}&lt;/span&gt;}` — wrap `{job.dateAdded}` in `&lt;Mono&gt;` inside the existing span: `&lt;span className="text-xs text-ink-400"&gt;&lt;Mono&gt;{job.dateAdded}&lt;/Mono&gt;&lt;/span&gt;`.
    3. Lines 66-71's stale badge — the current body is `👻 Stale {ageDays}d` inside a `&lt;span title={...} className="text-[10px] font-medium px-1.5 py-0.5 rounded-full bg-warning-100 text-warning-800"&gt;`. Wrap just `{ageDays}` in `&lt;Mono&gt;`: `👻 Stale &lt;Mono&gt;{ageDays}&lt;/Mono&gt;d`, keeping the emoji, the "d" suffix, and the outer span's classes untouched.
    Do not touch the `DEADLINE_BADGE` object, `urgencyTier`/`daysUntilDeadline`/`jobAgeDays`/`isGhostJob` imports or usage, the ♥/♡ status toggle, or the bucket-status pill at lines 76-90.
  </action>
  <acceptance_criteria>
    - `JobCard.jsx` imports `Mono` from `'../ui/Mono.jsx'`.
    - `DeadlineBadge`'s label text, the `job.dateAdded` value, and the stale badge's `ageDays` value are each wrapped in `Mono`, with the emoji/suffix text and every existing conditional className left exactly as before.
    - `DEADLINE_BADGE`'s 3-tier color map (`urgent`/`soon`/`known`) is byte-identical to before this edit.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=app/src/components/jobBoards/JobCard.jsx; grep -qF "from '"'"'../ui/Mono.jsx'"'"'" "$F" || { echo "FAIL: Mono import missing"; exit 1; }; grep -qF "<Mono>{label}</Mono>" "$F" || { echo "FAIL: DeadlineBadge label not wrapped"; exit 1; }; grep -qF "<Mono>{job.dateAdded}</Mono>" "$F" || { echo "FAIL: dateAdded not wrapped"; exit 1; }; grep -qF "Stale <Mono>{ageDays}</Mono>d" "$F" || { echo "FAIL: stale ageDays not wrapped"; exit 1; }; grep -qF "urgent: '"'"'bg-danger-500 text-white'"'"'," "$F" || { echo "FAIL: DEADLINE_BADGE map changed unexpectedly"; exit 1; }; echo "JobCard.jsx VERIFIED"'</automated>
  </verify>
  <done>JobCard.jsx's deadline label, posted date, and stale day count all render in Mono, with all urgency-tier colors unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap JobDetailModal.jsx's date/deadline text in Mono</name>
  <files>app/src/components/jobBoards/JobDetailModal.jsx</files>
  <read_first>
    app/src/components/jobBoards/JobDetailModal.jsx (current file — header date-row at lines 43-51, deadline-countdown block at lines 52-66); app/src/components/ui/Mono.jsx; 01-PATTERNS.md's jobBoards section (lines 244-247).
  </read_first>
  <action>
    Add `import Mono from '../ui/Mono.jsx'` to the import block (same `../` depth as Task 1 — this file is also at `components/jobBoards/`). Make 3 wraps, typography only:
    1. Line 45: `{job.dateAdded &amp;&amp; &lt;span&gt;📅 posted {job.dateAdded}&lt;/span&gt;}` — wrap just `{job.dateAdded}` in `&lt;Mono&gt;`: `📅 posted &lt;Mono&gt;{job.dateAdded}&lt;/Mono&gt;`, keeping the emoji/"posted" text and the plain `&lt;span&gt;` (no className) as-is.
    2. Lines 46-50's stale indicator: `👻 No update in {ageDays}d` inside `&lt;span className="px-1.5 py-0.5 rounded-full bg-warning-100 text-warning-800 text-[10px] font-medium"&gt;` — wrap `{ageDays}` in `&lt;Mono&gt;`: `👻 No update in &lt;Mono&gt;{ageDays}&lt;/Mono&gt;d`, class unchanged.
    3. Lines 53-56's deadline-countdown span: the current body is `⏰ {deadlineDays &lt; 0 ? 'Deadline passed' : deadlineDays === 0 ? 'Closes today' : \`Closes in ${deadlineDays}d\`} ({deadline.deadline})` inside `&lt;span className={\`px-2 py-1 rounded-lg text-xs font-semibold ${DEADLINE_TEXT[tier]}\`}&gt;`. Wrap the entire computed countdown expression (the ternary chain producing "Deadline passed"/"Closes today"/`Closes in Nd`) in `&lt;Mono&gt;`, keeping `⏰ ` and the trailing `({deadline.deadline})` raw-text parenthetical outside the `Mono` wrap (that parenthetical echoes the apply page's own stated date string, not a computed day-count — leave it as plain text): result is `⏰ &lt;Mono&gt;{deadlineDays &lt; 0 ? 'Deadline passed' : deadlineDays === 0 ? 'Closes today' : \`Closes in ${deadlineDays}d\`}&lt;/Mono&gt; ({deadline.deadline})`.
    Do not touch the "Rolling — no stated deadline" or "Checking apply page..." branches (lines 57-61 — these are static status labels, not dense numeric/date data, out of VIS-02's scope per its literal wording), the `DEADLINE_TEXT` map, the `↻ recheck` button, or the AI-analysis section below it.
  </action>
  <acceptance_criteria>
    - `JobDetailModal.jsx` imports `Mono` from `'../ui/Mono.jsx'`.
    - The posted-date value, the no-update day count, and the full deadline-countdown ternary expression are each wrapped in `Mono`, with emoji/label text and the `DEADLINE_TEXT[tier]`-driven className left exactly as before.
    - The "Rolling — no stated deadline" and "Checking apply page…" static-label branches are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=app/src/components/jobBoards/JobDetailModal.jsx; grep -qF "from '"'"'../ui/Mono.jsx'"'"'" "$F" || { echo "FAIL: Mono import missing"; exit 1; }; grep -qF "posted <Mono>{job.dateAdded}</Mono>" "$F" || { echo "FAIL: dateAdded not wrapped"; exit 1; }; grep -qF "No update in <Mono>{ageDays}</Mono>d" "$F" || { echo "FAIL: ageDays not wrapped"; exit 1; }; grep -qF "<Mono>{deadlineDays < 0" "$F" || { echo "FAIL: deadline countdown not wrapped"; exit 1; }; grep -qF "Rolling — no stated deadline" "$F" || { echo "FAIL: rolling label changed unexpectedly"; exit 1; }; echo "JobDetailModal.jsx VERIFIED"'</automated>
  </verify>
  <done>JobDetailModal.jsx's posted-date, no-update-days, and deadline-countdown text all render in Mono, with all urgency-tier colors and static labels unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | This plan wraps already-rendered strings (job-board posting dates parsed from GitHub README content, and AI-extracted deadline text already displayed before this phase) in a presentational typography component. No new data source, no new parsing/extraction logic, no change to what's fetched or how — only how already-rendered values are typeset. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched |
| V5 Input Validation | No | No new input surface — `job.dateAdded`/`deadline.deadline`/computed day-counts were already rendered before this plan; this plan only changes their typography wrapper |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | (none identified) | JobCard.jsx / JobDetailModal.jsx Mono call sites | low | accept | `Mono` renders only already-displayed date/countdown strings (parsed board postings, AI-extracted deadline text per `lib/deadlines.js`'s existing "never invent a date" constraint) — no new rendering of untrusted raw HTML, no `dangerouslySetInnerHTML`. |

</threat_model>

<verification>
Run both tasks' `<verify><automated>` commands after this plan's edits land. Confirm the diff touches exactly `JobCard.jsx` and `JobDetailModal.jsx`.
</verification>

<success_criteria>
- JobCard.jsx's deadline badge, posted date, and stale-listing day count render in IBM Plex Mono.
- JobDetailModal.jsx's posted-date, no-update-days, and deadline-countdown text render in IBM Plex Mono.
- All existing urgency-tier color coding (urgent/soon/known/rolling) and static status labels are unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-04-SUMMARY.md` when done
</output>
