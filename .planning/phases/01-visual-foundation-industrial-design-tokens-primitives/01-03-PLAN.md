---
phase: 01-visual-foundation-industrial-design-tokens-primitives
plan: 3
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - app/src/components/ContactsTable.jsx
  - app/src/components/PipelineTab.jsx
autonomous: true
requirements: [VIS-02]

must_haves:
  truths:
    - "Network table's Last-interaction and Follow-Up date columns render in IBM Plex Mono with tabular-nums, replacing the current default Public Sans body font."
    - "Pipeline's applied/closed dates and stage-age day counts render in IBM Plex Mono — the columns RESEARCH.md identified as the concrete evidence for VIS-02's 'reserved, barely used' framing."
    - "The overdue-follow-up red styling on ContactsTable's Follow-Up column and the stale-application styling on Pipeline's day-count still render correctly — Mono only adds typography, it does not replace the existing conditional color logic."
  artifacts: []
  key_links:
    - "app/src/components/ui/Mono.jsx (created in 01-02-PLAN.md, Wave 1) -> new import statements in ContactsTable.jsx and PipelineTab.jsx — this plan cannot start before 01-02 lands, hence Wave 2."
    - "fmt()/daysSince()/daysBetween() in shared.jsx (unchanged, still return plain strings/numbers) -> <Mono> wraps their output at the JSX call site, per RESEARCH.md Pattern 3's explicit finding that fmt() itself must stay a plain string function."
---

<objective>
Wrap the dense date/day-count values in `ContactsTable.jsx`'s "Last"/"Follow-Up" columns and `PipelineTab.jsx`'s applied-date/closed-date/stage-age displays in the new `<Mono>` primitive from `01-02-PLAN.md` — the two exact files `01-RESEARCH.md`'s Pattern 3 named as concrete evidence that IBM Plex Mono is currently "reserved, barely used" (13 existing `font-mono` uses, none on these table columns).

Purpose: This is the highest-traffic half of VIS-02's rollout — Network and Pipeline are two of the app's most-viewed surfaces, and their date/count columns are exactly the "dense data fields... in tables and panels" VIS-02 names.
Output: `ContactsTable.jsx` and `PipelineTab.jsx` with every existing `fmt(...)`/day-count display wrapped in `<Mono>`, preserving all existing conditional styling (overdue red, stale amber) unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-PATTERNS.md
@app/src/components/ContactsTable.jsx
@app/src/components/PipelineTab.jsx
@app/src/components/ui/Mono.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap ContactsTable.jsx's date columns in Mono</name>
  <files>app/src/components/ContactsTable.jsx</files>
  <read_first>
    app/src/components/ContactsTable.jsx (current file, imports at lines 1-9, `lastInteraction`/`followUpDate` column definitions at lines 86-98); app/src/components/ui/Mono.jsx (the component being wired in — created by 01-02-PLAN.md); 01-PATTERNS.md's "app/src/components/ContactsTable.jsx (Mono rollout call sites)" section (lines 194-221) — exact current code and exact required edit pattern; 01-UI-SPEC.md's "IBM Plex Mono rollout" paragraph (lines 74-81).
  </read_first>
  <action>
    Add `import Mono from './ui/Mono.jsx'` to the import block (same relative-path convention already used by other `components/*.jsx` files importing `ui/` primitives, e.g. `AddToCalendarModal.jsx`'s `import Modal from './ui/Modal.jsx'`). In the `lastInteraction` column's `cell` function, change `cell: info => fmt(info.getValue())` to `cell: info => &lt;Mono&gt;{fmt(info.getValue())}&lt;/Mono&gt;`. In the `followUpDate` column's `cell` function, the current body returns `&lt;span className={overdue ? 'text-danger-600 font-medium' : ''}&gt;{fmt(v)}&lt;/span&gt;` — replace the outer `&lt;span&gt;` with `&lt;Mono&gt;`, moving the exact same conditional className (`overdue ? 'text-danger-600 font-medium' : ''`) onto `Mono`'s `className` prop rather than nesting `Mono` inside the existing span (Mono already forwards `className` via `cn()`, so this avoids a redundant nested element) — result: `return &lt;Mono className={overdue ? 'text-danger-600 font-medium' : ''}&gt;{fmt(v)}&lt;/Mono&gt;`. Do not touch the `sortingFn: 'datetime'` lines, the `overdue` computation itself, or any other column definition in this file.
  </action>
  <acceptance_criteria>
    - `ContactsTable.jsx` imports `Mono` from `./ui/Mono.jsx`.
    - The `lastInteraction` column's cell renders `fmt(info.getValue())` inside a `Mono` element, not a bare string/span.
    - The `followUpDate` column's cell renders `fmt(v)` inside a `Mono` element whose `className` prop carries the exact same `overdue ? 'text-danger-600 font-medium' : ''` conditional that previously lived on the outer `span`.
    - No stray/orphaned `<span>` wraps `fmt(v)` in the `followUpDate` cell (the `Mono` element itself replaces it, not wraps it).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=app/src/components/ContactsTable.jsx; grep -qF "from '"'"'./ui/Mono.jsx'"'"'" "$F" || { echo "FAIL: Mono import missing"; exit 1; }; grep -qF "<Mono>{fmt(info.getValue())}</Mono>" "$F" || { echo "FAIL: lastInteraction cell not wrapped"; exit 1; }; grep -qF "<Mono className={overdue ? '"'"'text-danger-600 font-medium'"'"' : '"'"''"'"'}>{fmt(v)}</Mono>" "$F" || { echo "FAIL: followUpDate cell not wrapped correctly"; exit 1; }; echo "ContactsTable.jsx VERIFIED"'</automated>
  </verify>
  <done>ContactsTable.jsx's Last and Follow-Up columns render in Mono, with the overdue-red conditional styling preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap PipelineTab.jsx's date/day-count displays in Mono</name>
  <files>app/src/components/PipelineTab.jsx</files>
  <read_first>
    app/src/components/PipelineTab.jsx (current file — duplicate-group meta line at line 72, stage-age computation at line 158, applied/closed-date displays at lines 177/180, stale day-count display at lines 183-187); app/src/components/ui/Mono.jsx; 01-PATTERNS.md's "app/src/components/PipelineTab.jsx (Mono rollout call sites)" section (lines 225-241) — exact current lines and exact required edit pattern.
  </read_first>
  <action>
    Add `import Mono from './ui/Mono.jsx'` to the import block (same convention as Task 1). Wrap each of these 4 existing `fmt(...)`/day-count renders in `&lt;Mono&gt;`, keeping every surrounding element and conditional class exactly as-is — `Mono` supplies only the typography, never a color decision:
    1. Line 72's duplicate-group meta line: wrap just `fmt(a.createdTime)` in `&lt;Mono&gt;` — i.e. `... {fmt(a.createdTime)}` becomes `... &lt;Mono&gt;{fmt(a.createdTime)}&lt;/Mono&gt;`, leaving the rest of the interpolated string (`{j === 0 ? '✓ keep' : '✕ archive'} · {a.stage} · {a.triage}...`) untouched.
    2. Line 177: `&lt;span className="text-xs text-ink-400"&gt;Applied {fmt(a.appliedDate)}&lt;/span&gt;` becomes `&lt;span className="text-xs text-ink-400"&gt;Applied &lt;Mono&gt;{fmt(a.appliedDate)}&lt;/Mono&gt;&lt;/span&gt;` — keep the existing outer span and its `text-ink-400` color, only wrap the date value itself.
    3. Lines 178-181's closed-date span: wrap `fmt(a.closedDate)` in `&lt;Mono&gt;` the same way, leaving the `(${daysBetween(...)}d)` day-count suffix and the outer span/color untouched for this call site.
    4. Lines 183-187's stage-age display: the current body is `{days}d in stage{stale ? ' ⚠' : ''}` inside a `&lt;span className={...}&gt;`. Wrap just the `{days}` value in `&lt;Mono&gt;`, i.e. `&lt;Mono&gt;{days}&lt;/Mono&gt;d in stage{stale ? ' ⚠' : ''}`, keeping the outer span's conditional `stale ? 'text-orange-600 font-medium' : 'text-ink-400'` className exactly as-is (that pre-existing `text-orange-600` literal is a known, separate finding outside this phase's locked scope — see this phase's QA checkpoint plan for the tracked note; do not fix it as part of this task).
    Do not touch `daysSince`/`daysBetween`/`isUntriaged`/`findDuplicateGroups` imports or logic, and do not modify `ApplicationDetailModal.jsx` (out of scope — its duplicate-count headline stays on `font-heading`+`tabular-nums`, per UI-SPEC's explicit exclusion).
  </action>
  <acceptance_criteria>
    - `PipelineTab.jsx` imports `Mono` from `./ui/Mono.jsx`.
    - All 4 named call sites (duplicate-group meta date, applied-date, closed-date, stage-age day count) wrap only the numeric/date value itself in `Mono`, not the surrounding label text or conditional-color wrapper span.
    - The `stale ? 'text-orange-600 font-medium' : 'text-ink-400'` conditional on the stage-age span is unchanged (not remapped to a token color this task).
    - `app/src/components/ApplicationDetailModal.jsx` has zero diff from this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=app/src/components/PipelineTab.jsx; grep -qF "from '"'"'./ui/Mono.jsx'"'"'" "$F" || { echo "FAIL: Mono import missing"; exit 1; }; grep -qF "<Mono>{fmt(a.createdTime)}</Mono>" "$F" || { echo "FAIL: duplicate-group date not wrapped"; exit 1; }; grep -qF "Applied <Mono>{fmt(a.appliedDate)}</Mono>" "$F" || { echo "FAIL: appliedDate not wrapped"; exit 1; }; grep -qF "Closed <Mono>{fmt(a.closedDate)}</Mono>" "$F" || { echo "FAIL: closedDate not wrapped"; exit 1; }; grep -qF "<Mono>{days}</Mono>d in stage" "$F" || { echo "FAIL: stage-age days not wrapped"; exit 1; }; echo "PipelineTab.jsx VERIFIED"'</automated>
  </verify>
  <done>PipelineTab.jsx's 4 dense date/day-count displays render in Mono, with all existing conditional styling and surrounding text unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | This plan wraps already-rendered, already-trusted formatted strings (`fmt()`/day-count output, sourced from the app's own Supabase-fetched, RLS-scoped data) in a presentational typography component. No new data source, no new user input, no change to what data is fetched or how. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched — RLS-scoped data fetching (`db.js`) is unchanged; this plan only edits how already-fetched values render |
| V5 Input Validation | No | No new input surface — `Mono` renders the same `fmt()`/numeric output that was already rendering, just wrapped |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | (none identified) | ContactsTable.jsx / PipelineTab.jsx Mono call sites | low | accept | `Mono` renders only pre-formatted date strings and numeric day-counts already produced by trusted `shared.jsx` helpers; no raw user string is newly rendered as HTML. |

</threat_model>

<verification>
Run both tasks' `<verify><automated>` commands after this plan's edits land. Confirm the diff touches exactly `ContactsTable.jsx` and `PipelineTab.jsx`.
</verification>

<success_criteria>
- ContactsTable.jsx's Last/Follow-Up columns and PipelineTab.jsx's applied/closed-date and stage-age displays all render in IBM Plex Mono via the shared `<Mono>` component.
- All pre-existing conditional color styling (overdue red, stale styling) is preserved exactly.
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-03-SUMMARY.md` when done
</output>
