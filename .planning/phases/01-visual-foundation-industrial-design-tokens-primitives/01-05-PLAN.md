---
phase: 01-visual-foundation-industrial-design-tokens-primitives
plan: 5
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified: []
autonomous: true
requirements: [VIS-02, VIS-03]

must_haves:
  truths:
    - "All 8 existing top-level tabs (Overview, Network, Explore, Pipeline, Actions, Calendar, Job Boards, Settings) plus the public /demo route render without visual regression after the full token-value swap — Success Criterion 4."
    - "A human has visually confirmed the industrial/control-panel direction (cool gunmetal neutrals, safety-orange accent, indicator-light status hues) is actually reflected across Button/Badge/Card/Tabs/Input/Select/Modal/EmptyState, per the project's standing render-and-screenshot verification directive."
    - "The full set of deterministic grep gates from 01-01 through 01-04 (no orange-/purple- literals in shared.jsx, Mono present in all 4 rollout files, all 44 token values present in index.css) re-verify green as one combined sweep, proving no later edit in this phase silently regressed an earlier plan's work."
  artifacts: []
  key_links:
    - "01-01/01-02/01-03/01-04's individual diffs -> this plan's combined regression sweep — the first point in the phase where all 5 plans' changes coexist in the working tree simultaneously, so this is the only place a cross-plan interaction (e.g. an accidental Mono import collision, or a re-edited index.css line) would surface."
---

<objective>
Run the phase-closing verification pass: a combined automated regression sweep across every grep/value gate established in `01-01` through `01-04`, followed by the mandatory human render-and-screenshot check (per the project's global frontend-aesthetics directive: "render it, screenshot it, and check it against the stated aesthetic direction before declaring the task done") across all 8 existing tabs plus `/demo`.

Purpose: This is the phase's goal-backward closing gate — Success Criterion 4 ("no regressions") can only be meaningfully checked once all 4 code-editing plans have landed together, and the aesthetic-direction check requires a live render, which no earlier plan's automated verify can substitute for.
Output: A recorded pass/fail for every regression gate plus a human-confirmed screenshot pass, documented in this plan's SUMMARY.md — the artifact `/gsd-verify-work` reads to close out Phase 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combined automated regression sweep</name>
  <files></files>
  <read_first>
    01-VALIDATION.md's "Phase Requirements → Test Map" (lines 45-53) and "Manual-Only Verifications" table (lines 67-72) — the exact commands and the exact rationale for why the aesthetic/regression checks are manual, not scripted; the SUMMARY.md files produced by 01-01 through 01-04 (confirm each plan's own verify commands were reported green before starting this sweep).
  </read_first>
  <action>
    Re-run, as one combined pass against the final working tree (all of 01-01 through 01-04 landed), the deterministic checks each earlier plan already established individually: (1) `app/src/index.css` contains all 44 new industrial hex values under the unchanged token names (01-01 Task 1's check); (2) `app/src/shared.jsx` contains zero `orange-`/`purple-` literals across its 5 color maps (01-01 Task 2's check); (3) `app/src/components/ui/Mono.jsx` exists with the exact locked implementation (01-02 Task 1's check); (4) `Button.jsx`/`Badge.jsx`/`Tabs.jsx` carry their locked weight/hex edits (01-02 Tasks 2-3's checks); (5) `Mono` is imported and wired into all 4 rollout files — `ContactsTable.jsx`, `PipelineTab.jsx`, `jobBoards/JobCard.jsx`, `jobBoards/JobDetailModal.jsx` (01-03/01-04's checks). Running this as one combined sweep against the fully-merged tree (rather than trusting each plan's individually-passing verify in isolation) is the only way to catch a cross-plan interaction — e.g. a later plan's edit accidentally touching a file an earlier plan already finalized.
  </action>
  <acceptance_criteria>
    - All 5 categories of checks above pass with zero FAIL against the current working tree.
    - `git diff --stat` (or equivalent) for this phase's full changeset shows exactly these files touched: `app/src/index.css`, `app/src/shared.jsx`, `app/src/components/ui/Mono.jsx` (new), `app/src/components/ui/Button.jsx`, `app/src/components/ui/Badge.jsx`, `app/src/components/ui/Tabs.jsx`, `app/src/components/ContactsTable.jsx`, `app/src/components/PipelineTab.jsx`, `app/src/components/jobBoards/JobCard.jsx`, `app/src/components/jobBoards/JobDetailModal.jsx` — no other file in the repo has an unexplained diff.
    - `app/src/components/ui/Card.jsx`, `Input.jsx`, `Select.jsx`, `Modal.jsx`, `EmptyState.jsx`, `ChipToggleGroup.jsx` show zero diff (confirming 01-PATTERNS.md's "these 7 files need no code diff" finding held true in practice).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; ok=1; F1=app/src/index.css; F2=app/src/shared.jsx; F3=app/src/components/ui/Mono.jsx; F4=app/src/components/ui/Button.jsx; F5=app/src/components/ui/Badge.jsx; F6=app/src/components/ui/Tabs.jsx; F7=app/src/components/ContactsTable.jsx; F8=app/src/components/PipelineTab.jsx; F9=app/src/components/jobBoards/JobCard.jsx; F10=app/src/components/jobBoards/JobDetailModal.jsx; grep -qF "--color-ink-900: #101215" "$F1" || { echo "FAIL index.css"; ok=0; }; C=$(grep -Ec "orange-|purple-" "$F2" || true); [ "$C" = "0" ] || { echo "FAIL shared.jsx still has $C stock literals"; ok=0; }; grep -qF "export default function Mono" "$F3" || { echo "FAIL Mono.jsx"; ok=0; }; grep -qF "bg-accent-600 text-white hover:bg-accent-700" "$F4" || { echo "FAIL Button.jsx"; ok=0; }; grep -qF "font-semibold" "$F5" || { echo "FAIL Badge.jsx"; ok=0; }; grep -qF "font-semibold" "$F6" || { echo "FAIL Tabs.jsx"; ok=0; }; grep -qF "from '"'"'./ui/Mono.jsx'"'"'" "$F7" || { echo "FAIL ContactsTable.jsx import"; ok=0; }; grep -qF "from '"'"'./ui/Mono.jsx'"'"'" "$F8" || { echo "FAIL PipelineTab.jsx import"; ok=0; }; grep -qF "from '"'"'../ui/Mono.jsx'"'"'" "$F9" || { echo "FAIL JobCard.jsx import"; ok=0; }; grep -qF "from '"'"'../ui/Mono.jsx'"'"'" "$F10" || { echo "FAIL JobDetailModal.jsx import"; ok=0; }; [ "$ok" = "1" ] && echo "COMBINED REGRESSION SWEEP: ALL GREEN" || { echo "COMBINED REGRESSION SWEEP: FAILURES ABOVE"; exit 1; }'</automated>
  </verify>
  <done>All 5 categories of deterministic checks pass against the fully-merged phase diff; no file outside the expected 10-file changeset was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Start the dev server and stage the visual verification for end-of-phase review</name>
  <files></files>
  <read_first>
    01-UI-SPEC.md's aesthetic-direction paragraph (lines 14) and Color/Typography sections; the global frontend-aesthetics directive (`~/.claude/CLAUDE.md`) requiring a render-and-screenshot pass before declaring UI work done; this project's `workflow.human_verify_mode` config (defaults to `end-of-phase` — no mid-flight `checkpoint:human-verify` task is used here; the verification steps below are embedded in this task's `<verify><human-check>` block per that mode, and get harvested into this phase's UAT.md for one batched review at end-of-phase rather than a blocking mid-flight pause).
  </read_first>
  <action>
    Start `cd app && npm run dev` in the background, wait for Vite's "ready in" signal, and confirm the local dev server responds (fetch returns 200) before considering this task's automated portion complete. Leave the server running — the human-check verification below depends on it, and later plans/phases may reuse it.
  </action>
  <acceptance_criteria>
    - The dev server is running and reachable at its local URL (default http://localhost:3001 per this project's documented dev port).
    - No stray port-conflict or startup error in the server's output.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd app && (npm run dev &> /tmp/recruiter-dev-server.log &) ; for i in $(seq 1 30); do node -e "fetch(\"http://localhost:3001\").then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))" 2>/dev/null && { echo "DEV SERVER READY"; exit 0; }; sleep 1; done; echo "DEV SERVER DID NOT BECOME READY — see /tmp/recruiter-dev-server.log"; exit 1'</automated>
    <human-check>
      With the dev server running at http://localhost:3001:
      1. Visit each of the 8 existing top-level tabs (Overview, Network, Explore, Pipeline, Actions, Calendar, Job Boards, Settings) plus `/demo`. Confirm nothing is broken or illegible — every screen should render, no unstyled/default-browser-black text, no missing colors.
      2. On Network's table (or Cards) view, confirm contact Status/Champion badges, and on Pipeline confirm Stage/Technical/Onsite badges, all render in the new palette (not stock Tailwind orange/purple) — the direct visual check for `01-01-PLAN.md`'s off-token fix.
      3. On Network's table, confirm the "Last" and "Follow-Up" date columns visibly render in a monospace typeface distinct from the rest of the row (IBM Plex Mono vs. Public Sans). Repeat on Pipeline's applied/closed-date and day-in-stage displays, and on Job Boards' card grid (posted date, deadline badge) and a job's detail panel (posted date, deadline countdown).
      4. Click a primary Button (e.g. "+ Contact", "+ Log Interaction") and confirm its background reads as a richer/darker orange than before, with a visibly bolder (semibold) label — the WCAG-contrast fix from `01-02-PLAN.md`.
      5. Screenshot at least 3 of the above screens and compare against the industrial/control-panel direction locked in `01-UI-SPEC.md` (cool steel canvas, gunmetal neutrals, punchy safety-orange accent, indicator-light status hues) — confirm it reads as that direction, not the prior warm-paper/soft-amber look.
      6. Two known, deliberately out-of-scope items to be aware of while reviewing (not defects, do not report as regressions): (a) the Overview tab's "Network by Status" donut chart will still show the OLD palette — `charts/theme.js`'s hex mirror sync is explicitly deferred to Phase 7 per `01-UI-SPEC.md`'s "Open Questions Resolved #2"; (b) Pipeline's "stale application" indicator (the amber/orange left-border and day-count text on stale rows) still uses a few pre-existing stock-Tailwind `orange-` literals directly in `PipelineTab.jsx`'s JSX (distinct from the 8 `shared.jsx` map entries this phase fixed) — found during planning but not part of `01-UI-SPEC.md`'s locked remap scope, left for a future pass rather than silently patched with an unreviewed color choice.
    </human-check>
  </verify>
  <done>Dev server running and reachable; the 6-step visual verification is staged in this task's human-check block for end-of-phase batch review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev server (client browser <-> Vite dev server) | Standard local development loop, already covered by this repo's existing auth/RLS/BYOK model (unchanged by this phase). This task only renders the app locally for visual inspection — no new endpoint, no new data path. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched — verification uses the existing signed-in session or `/demo`'s no-auth path, both unchanged by this phase |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched |
| V5 Input Validation | No | No new input surface — this task is read-only visual inspection |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | (none identified) | Local dev server used for visual verification | low | accept | Standard `npm run dev` local loop already in daily use for this project; no new exposure introduced by running it for a screenshot pass. |

</threat_model>

<verification>
Task 1's automated sweep must be green before Task 2 starts the dev server. Task 2's `<verify><human-check>` block is harvested by the verifier at end-of-phase (per `workflow.human_verify_mode=end-of-phase`) into this phase's UAT.md for one batched human review — do not mark Phase 1 complete until that review confirms all 8 tabs + `/demo` render correctly and the industrial direction reads clearly.
</verification>

<success_criteria>
- Combined regression sweep across all 5 plans' deterministic checks passes with zero FAIL.
- Dev server starts cleanly and the 6-step visual-verification human-check is staged for end-of-phase UAT review, confirming all 8 tabs + /demo render without regression and the industrial direction is clearly reflected.
- The two known, deliberately-deferred items (charts/theme.js sync, PipelineTab.jsx's pre-existing orange- literals) are documented, not silently missed.
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-05-SUMMARY.md` when done
</output>
