---
phase: 01-visual-foundation-industrial-design-tokens-primitives
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ui/Mono.jsx
  - app/src/components/ui/Button.jsx
  - app/src/components/ui/Badge.jsx
  - app/src/components/ui/Tabs.jsx
autonomous: true
requirements: [VIS-02]

must_haves:
  truths:
    - "A new, reusable Mono primitive exists at app/src/components/ui/Mono.jsx, matching Badge.jsx's small single-purpose <span>-wrapper convention, ready for every dense-data call site in Plans 3 and 4 to import."
    - "Button's primary variant no longer fails WCAG contrast (was 2.23:1 on accent-500/white, pre-existing and undetected before this phase) — it now uses accent-600/accent-700 (4.59:1 / 6.09:1) with a semibold (600) label weight."
    - "Badge and Tabs — the app's two most-used Label-role text surfaces (every status/stage/type chip, every segmented control) — render at the locked 600 (semibold) weight instead of the prior 500 (medium)."
  artifacts:
    - "app/src/components/ui/Mono.jsx (new file — default export Mono({className, children}))"
    - "app/src/components/ui/Button.jsx (primary variant hex-step + base font-weight bump)"
    - "app/src/components/ui/Badge.jsx (font-weight bump)"
    - "app/src/components/ui/Tabs.jsx (font-weight bump)"
  key_links:
    - "Mono.jsx's default export -> the import statements Plans 3 and 4 add in ContactsTable.jsx/PipelineTab.jsx/JobCard.jsx/JobDetailModal.jsx — if this file doesn't exist with this exact export shape before those plans run, their builds fail immediately (fast-fail dependency, this is why Mono.jsx is Wave 1 not Wave 2)."
    - "Button.jsx's VARIANTS.primary string -> every primary-action button app-wide (Login, Save, Create, Add to targets, etc.) — a typo here is visible on the single most common interactive element in the app."
---

<objective>
Create the new `Mono` typography primitive (the shared component every dense-data call site in Plans 3 and 4 will import) and land the 3 concrete JSX edits among the phase's 8 named `ui/` primitives that actually require a code change — `Button.jsx` (WCAG-fix hex step + weight), `Badge.jsx` and `Tabs.jsx` (Label-role weight bump). The other 5 primitives (`Card`, `Input`, `Select`, `Modal`, `EmptyState`) plus `ChipToggleGroup` need zero JSX changes per `01-PATTERNS.md`'s explicit finding — they pick up the new palette automatically once `01-01-PLAN.md`'s token swap lands, and are not touched by this plan.

Purpose: `Mono.jsx` is the interface every later Mono-rollout task depends on — it must exist and match the exact locked shape before Plans 3/4 can import it. Button/Badge/Tabs are the only primitives among the phase's named 8 with an actual pre-existing WCAG failure (Button) or a locked typography-weight change (Badge/Tabs) to apply.
Output: `app/src/components/ui/Mono.jsx` (new); `Button.jsx`/`Badge.jsx`/`Tabs.jsx` each with their one locked structural edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-PATTERNS.md
@app/src/components/ui/Button.jsx
@app/src/components/ui/Badge.jsx
@app/src/components/ui/Tabs.jsx
@app/src/lib/cn.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Mono primitive (app/src/components/ui/Mono.jsx)</name>
  <files>app/src/components/ui/Mono.jsx</files>
  <read_first>
    app/src/components/ui/Badge.jsx (the direct structural analog — 8 lines, single-purpose `<span>` wrapper, one style string); app/src/lib/cn.js (the `clsx`+`tailwind-merge` helper Mono must use); 01-UI-SPEC.md's "IBM Plex Mono rollout — exact scope (VIS-02)" section (lines 74-81) — the literal locked implementation and the "do not wrap large single-hero-number displays" scope boundary; 01-PATTERNS.md's "app/src/components/ui/Mono.jsx (new file...)" section (lines 180-190).
  </read_first>
  <action>
    Create `app/src/components/ui/Mono.jsx` as a new file. It imports `cn` as a named import from `../../lib/cn.js` (matching the relative-import depth used by every other `ui/` primitive that imports `cn`). It exports a default function component named `Mono` accepting a `{ className, children }` props object, and returns a `<span>` element whose `className` is built via `cn('font-mono text-xs font-normal tabular-nums tracking-wide', className)`, rendering `children` inside the span. Use `cn()` (not a raw template literal) — this matches the majority convention among the 8 existing primitives (`Button`, `Card`, `Tabs`, `Input`, `Select`, `Modal` all use `cn()`; only `Badge`/`ChipToggleGroup` use raw template literals, and `Mono` is new so it should follow the majority pattern). The weight is deliberately `font-normal` (400) per UI-SPEC's "two weights, not three" rule — do not use `font-medium`(500) or `font-semibold`(600) here. `tracking-wide` is the legibility compensation for the regular-weight cut at 12px, not a stylistic flourish — keep it.
  </action>
  <acceptance_criteria>
    - `app/src/components/ui/Mono.jsx` exists, imports `{ cn }` from `'../../lib/cn.js'`, and default-exports a `Mono` function component.
    - The rendered `<span>`'s base className string is exactly `font-mono text-xs font-normal tabular-nums tracking-wide` before any `className` prop is merged in via `cn()`.
    - The component accepts and forwards `children` as the span's content and merges an optional `className` prop via `cn()` (not string concatenation).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=app/src/components/ui/Mono.jsx; [ -f "$F" ] || { echo "FAIL: file missing"; exit 1; }; grep -qF "from '"'"'../../lib/cn.js'"'"'" "$F" || { echo "FAIL: cn import missing"; exit 1; }; grep -qF "font-mono text-xs font-normal tabular-nums tracking-wide" "$F" || { echo "FAIL: exact class string missing"; exit 1; }; grep -q "export default function Mono" "$F" || { echo "FAIL: default export shape wrong"; exit 1; }; echo "Mono.jsx VERIFIED"'</automated>
  </verify>
  <done>app/src/components/ui/Mono.jsx exists with the exact locked implementation — the structural analog of Badge.jsx, ready for Plans 3/4 to import.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Button.jsx's primary variant (WCAG hex-step + weight bump)</name>
  <files>app/src/components/ui/Button.jsx</files>
  <read_first>
    app/src/components/ui/Button.jsx (current file, 27 lines); 01-UI-SPEC.md's "Fixes beyond the reskin" #1 (lines 110-112) — the pre-existing 2.23:1 failure and its exact fix; 01-UI-SPEC.md's "Button label weight bump (500 -> 600)" paragraph (lines 72); 01-PATTERNS.md's "app/src/components/ui/Button.jsx" section (lines 69-99) — the exact current file content and exact required edits.
  </read_first>
  <action>
    Two edits to the same file. First, in the `VARIANTS.primary` string, change `bg-accent-500 text-white hover:bg-accent-600 border border-transparent` to `bg-accent-600 text-white hover:bg-accent-700 border border-transparent` — this is the WCAG fix: the old `accent-500` + white measured 2.23:1 (a pre-existing failure, not introduced by this phase), the new `accent-600` + white measures 4.59:1 (hover `accent-700` measures 6.09:1). `accent-500` stays reserved elsewhere (borders/icons/rings/dark-text-on-light-badge contexts) — do not change any other variant's hex step. Second, in the shared base class string passed to `cn()` (the string currently containing `'rounded-xl font-medium transition-colors disabled:opacity-40 disabled:cursor-not-allowed'`), change `font-medium` to `font-semibold` — this applies to all 4 variants since it lives on the shared base string, not per-variant, and is the correct "chunky, stenciled control-panel label" weight per UI-SPEC's Typography contract (also gives the primary variant's white-on-accent-600 text a large-bold-text safety margin on top of the 4.59:1 ratio). Do not touch `secondary`/`ghost`/`danger` variant color strings, the `SIZES` object, or the `px-3 py-1.5`/`px-4 py-2.5` spacing — UI-SPEC explicitly defers non-4px-multiple chrome padding to Phase 7.
  </action>
  <acceptance_criteria>
    - `VARIANTS.primary` is exactly `bg-accent-600 text-white hover:bg-accent-700 border border-transparent`.
    - The shared base className string uses `font-semibold`, not `font-medium`.
    - `VARIANTS.secondary`, `VARIANTS.ghost`, `VARIANTS.danger`, and `SIZES` are byte-identical to before this edit.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=app/src/components/ui/Button.jsx; grep -qF "bg-accent-600 text-white hover:bg-accent-700 border border-transparent" "$F" || { echo "FAIL: primary variant not updated"; exit 1; }; grep -qF "font-semibold" "$F" || { echo "FAIL: base weight not bumped"; exit 1; }; grep -qF "bg-white text-ink-700 border border-ink-100 hover:bg-ink-50" "$F" || { echo "FAIL: secondary variant changed unexpectedly"; exit 1; }; grep -qF "bg-danger-600 text-white hover:bg-danger-700 border border-transparent" "$F" || { echo "FAIL: danger variant changed unexpectedly"; exit 1; }; echo "Button.jsx VERIFIED"'</automated>
  </verify>
  <done>Button.jsx's primary variant clears WCAG 4.5:1 (was 2.23:1) and all 4 variants render at font-semibold (600), matching UI-SPEC's locked contract.</done>
</task>

<task type="auto">
  <name>Task 3: Bump Badge.jsx and Tabs.jsx to the locked Label-role weight (600)</name>
  <files>app/src/components/ui/Badge.jsx, app/src/components/ui/Tabs.jsx</files>
  <read_first>
    app/src/components/ui/Badge.jsx (current file, 8 lines); app/src/components/ui/Tabs.jsx (current file, 23 lines); 01-UI-SPEC.md's Typography table (lines 60-68) — "Label (Badge, Tabs, Input/Select `&lt;label&gt;`) ... 600 (semibold, up from current 500/medium)" — and the "Two weights, not three" paragraph (line 70) which repeats "600 (semibold) covers headings, Badge/Tabs/Button/Label text"; 01-PATTERNS.md's Badge.jsx section (lines 102-126, explicit weight-bump instruction) and Card/Tabs/etc. section (lines 130-140, which found "no edit needed" for Tabs.jsx on structural/color grounds but did not evaluate the weight dimension against UI-SPEC's Typography table).
  </read_first>
  <action>
    Badge.jsx: in the template-literal className string (`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium ${color}`), change `font-medium` to `font-semibold`. Leave the `color` prop default (`bg-ink-100 text-ink-600`) and every other part of the string untouched — colors pick up the new hex automatically via 01-01-PLAN.md's token swap.
    Tabs.jsx: in the outer wrapper `<div>`'s className (built via `cn('inline-flex border border-ink-100 rounded-full overflow-hidden text-xs font-medium bg-white', className)`), change `font-medium` to `font-semibold`. This applies UI-SPEC's Typography table literally — Tabs is explicitly named alongside Badge in the same "Label" row ("Badge, Tabs, Input/Select `&lt;label&gt;`") and the same row's parenthetical ("up from current 500/medium") matches Tabs's actual current code (`font-medium` on the wrapper div) exactly, the same way it matches Badge's and Button's current code. 01-PATTERNS.md's file-by-file breakdown examined Tabs.jsx's color/border classes in detail but its "no edit needed" conclusion did not address the weight dimension — this task reconciles that gap by applying UI-SPEC's typography table as written. Do not touch Input.jsx or Select.jsx's `<label>` elements: those currently carry no `font-weight` utility at all (default/regular, not "500/medium"), so UI-SPEC's "up from current 500/medium" clause does not literally apply to them, and 01-PATTERNS.md explicitly found no edit needed there on both color and (by omission) weight grounds — leave both files completely untouched this task.
  </action>
  <acceptance_criteria>
    - Badge.jsx's className template literal contains `font-semibold`, not `font-medium`.
    - Tabs.jsx's wrapper `<div>` className contains `font-semibold`, not `font-medium`.
    - Badge.jsx's `color` prop default and Tabs.jsx's active/inactive button color classes (`bg-ink-900 text-white`, `text-ink-500 hover:bg-ink-50`) are byte-identical to before this edit.
    - `app/src/components/ui/Input.jsx` and `app/src/components/ui/Select.jsx` have zero diff from this task (not modified).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'B=app/src/components/ui/Badge.jsx; T=app/src/components/ui/Tabs.jsx; grep -qF "font-semibold" "$B" || { echo "FAIL: Badge.jsx weight not bumped"; exit 1; }; grep -qF "font-medium" "$B" && { echo "FAIL: Badge.jsx still has font-medium"; exit 1; }; grep -qF "font-semibold" "$T" || { echo "FAIL: Tabs.jsx weight not bumped"; exit 1; }; grep -qF "font-medium" "$T" && { echo "FAIL: Tabs.jsx still has font-medium"; exit 1; }; grep -qF "bg-ink-900 text-white" "$T" || { echo "FAIL: Tabs.jsx active-state class changed unexpectedly"; exit 1; }; echo "Badge.jsx + Tabs.jsx VERIFIED"'</automated>
  </verify>
  <done>Badge.jsx and Tabs.jsx both render Label-role text at font-semibold (600) per UI-SPEC's Typography contract; Input.jsx/Select.jsx untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | This plan creates one new presentational `<span>` wrapper component and edits className strings in 3 existing presentational components. No network request, no user input, no auth/session/access-control code, no data persistence. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched |
| V5 Input Validation | No | No new input surface — `Mono`/`Button`/`Badge`/`Tabs` accept only `children`/`className`/already-existing props, no new user-controlled data path |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | (none identified) | app/src/components/ui/Mono.jsx | low | accept | New presentational component; renders only `children` passed by existing trusted call sites (formatted date/count strings already produced by `fmt()`/`daysSince()` etc.), no `dangerouslySetInnerHTML`, no raw HTML injection. |
| T-01-02 | (none identified) | Button.jsx / Badge.jsx / Tabs.jsx | low | accept | className-string edits only; no props/behavior surface changed. |

</threat_model>

<verification>
Run all 3 tasks' `<verify><automated>` commands after this plan's edits land. Confirm the diff touches exactly `Mono.jsx` (new), `Button.jsx`, `Badge.jsx`, `Tabs.jsx` — no other `ui/` primitive file changes.
</verification>

<success_criteria>
- `app/src/components/ui/Mono.jsx` exists with the exact locked implementation, ready for Plans 3/4.
- Button's primary variant clears WCAG 4.5:1 with a semibold label.
- Badge and Tabs render Label-role text at the locked 600 weight.
- The other 5 named primitives (Card, Input, Select, Modal, EmptyState) plus ChipToggleGroup remain untouched by this plan (they pick up the new palette automatically via 01-01-PLAN.md).
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-02-SUMMARY.md` when done
</output>
