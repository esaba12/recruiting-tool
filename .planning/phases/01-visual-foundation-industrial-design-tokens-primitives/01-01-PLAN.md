---
phase: 01-visual-foundation-industrial-design-tokens-primitives
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/index.css
  - app/src/shared.jsx
autonomous: true
requirements: [VIS-03]

must_haves:
  truths:
    - "The @theme block in app/src/index.css carries the new industrial (cool-gunmetal/safety-orange) hex values under the exact same 6 token-family names (canvas, ink, accent, success, warning, danger) — no token renamed, no token family added."
    - "shared.jsx's STATUS_COLOR, URGENCY_COLOR, TYPE_COLOR, REFERRAL_STATUS_COLOR, and STAGE_COLOR maps contain zero stock-Tailwind orange-*/purple-* literals — every entry resolves through the app's own token scale."
    - "Every text/background color pair this phase touches (Button, Badge, Stage, Urgency, ChipToggleGroup, Tabs, body text, sidebar text) clears the WCAG ratio documented in 01-UI-SPEC.md's Validation Evidence table, confirmed by re-running the dataviz skill's contrast(a,b) function against the values actually landed in index.css."
  artifacts:
    - "app/src/index.css (@theme block — 44 --color-* declarations updated in place)"
    - "app/src/shared.jsx (STATUS_COLOR/URGENCY_COLOR/TYPE_COLOR/REFERRAL_STATUS_COLOR/STAGE_COLOR — 8 entries remapped)"
  key_links:
    - "index.css's @theme custom-property values -> every ink-*/accent-*/success-*/warning-*/danger-* Tailwind utility class across ~50 app/src files (automatic recompilation, zero call-site edits) — a typo in a --color-* name here silently breaks every consumer of that step."
    - "shared.jsx's 5 color-map exports -> Badge component call sites app-wide (Network table Status badges, Pipeline Stage badges, interaction Type chips, urgency pills, referral-status pills) — this is the only place old orange-*/purple-* literals could survive the reskin."
---

<objective>
Land the industrial/control-panel token-VALUE swap in `app/src/index.css`'s existing `@theme` block (same token names, new hex values, zero call-site edits elsewhere — the mechanism VIS-03 depends on), fix the 8 off-token hardcoded `orange-*`/`purple-*` entries in `app/src/shared.jsx` that would otherwise silently stay on Tailwind's stock palette after the swap, and prove every touched text/background pair clears WCAG contrast via the `dataviz` skill's `contrast(a,b)` function — per D-locked values in `01-UI-SPEC.md` (checker-approved, all 6 dimensions PASS).

Purpose: This plan is the foundation every other plan in this phase (and every later milestone phase) builds on — it's the one place the actual hex values change. Everything downstream (Button/Badge/Tabs restyling, the Mono rollout, all 8 existing tabs) depends on these values being correct, complete, and validated before anything else lands.
Output: `app/src/index.css` carrying the full new industrial ramp; `app/src/shared.jsx` with zero remaining stock-Tailwind color literals in its 5 badge-color maps; a recorded, passing contrast-validation run against the values as they actually exist in the file (not just as documented in UI-SPEC).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-PATTERNS.md
@app/src/index.css
@app/src/shared.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap @theme token values to the industrial ramp (index.css)</name>
  <files>app/src/index.css</files>
  <read_first>
    app/src/index.css (current file, 73 lines — the entire @theme block, lines 6-64); 01-UI-SPEC.md's "Color" section, "Token values" table and the 5 "Full ... ramp" lines (lines 89-108); 01-PATTERNS.md's "app/src/index.css (config, token values)" section (lines 29-65) confirming this is a value-only edit, token names frozen.
  </read_first>
  <action>
    Replace each `--color-*` hex literal inside the existing `@theme { }` block with its new value below. Token *names* are unchanged — do not add, remove, or rename any `--color-*` declaration, and do not touch `--font-heading`/`--font-body`/`--font-mono`, the `@custom-variant dark` line, or the `body`/`h1,h2,h3` font-family rules at the bottom of the file. Apply exactly these 44 old-to-new pairs (current value -> new value, same declaration order as the file):

    | Token | Old | New |
    |---|---|---|
    | --color-canvas | #fbf9f5 | #f2f3f4 |
    | --color-ink-50 | #f6f6f7 | #f1f2f3 |
    | --color-ink-100 | #e9e9eb | #e1e3e6 |
    | --color-ink-200 | #d3d3d7 | #c7cbd0 |
    | --color-ink-300 | #b0b0b7 | #a2a7af |
    | --color-ink-400 | #86868f | #666c74 |
    | --color-ink-500 | #64646d | #4f555d |
    | --color-ink-600 | #4a4a52 | #3a3f45 |
    | --color-ink-700 | #37373d | #2a2e33 |
    | --color-ink-800 | #24242a | #1a1c20 |
    | --color-ink-900 | #16171d | #101215 |
    | --color-accent-50 | #fef3e7 | #fff4e6 |
    | --color-accent-100 | #fde3c4 | #ffe0b8 |
    | --color-accent-200 | #fbc888 | #ffc27a |
    | --color-accent-300 | #f7ad63 | #ff9f40 |
    | --color-accent-400 | #f5a13f | #f97e1c |
    | --color-accent-500 | #f2994a | #f2680a |
    | --color-accent-600 | #e17f26 | #c94e0a |
    | --color-accent-700 | #c7761f | #a8420a |
    | --color-accent-800 | #9c5b18 | #7c3009 |
    | --color-accent-900 | #7a4712 | #5c2308 |
    | --color-success-50 | #eef7ee | #eaf6ec |
    | --color-success-100 | #d7eed8 | #cfead4 |
    | --color-success-200 | #b7e0ba | #a3d7ac |
    | --color-success-500 | #3c9a46 | #2f8f3d |
    | --color-success-600 | #2f8038 | #23732f |
    | --color-success-700 | #256730 | #1c5c26 |
    | --color-success-800 | #1c4f24 | #164a1e |
    | --color-success-900 | #143a1a | #0f3516 |
    | --color-warning-50 | #fdf6e3 | #fdf3dc |
    | --color-warning-100 | #fae8b8 | #f8e2a8 |
    | --color-warning-200 | #f5d98a | #f0cd6e |
    | --color-warning-400 | #e3b559 | #e0ab28 |
    | --color-warning-500 | #d9a02b | #d99a12 |
    | --color-warning-600 | #b9841f | #9c690d |
    | --color-warning-700 | #8f6518 | #8a630c |
    | --color-warning-800 | #6b4b12 | #6b4b12 (unchanged — same value in both palettes, write it identically) |
    | --color-danger-50 | #fbeaea | #fbe9e8 |
    | --color-danger-100 | #f5cccc | #f3c6c4 |
    | --color-danger-200 | #ecacac | #e59e9b |
    | --color-danger-400 | #d97070 | #cc5955 |
    | --color-danger-500 | #c94a4a | #b3312c |
    | --color-danger-600 | #ad3838 | #942720 |
    | --color-danger-700 | #8a2c2c | #741e19 |

    Update the two source comments above the ramps ("Warm off-white page canvas", "Charcoal scale — dominant color", "Warm amber/orange — the one sharp accent") only if they now read as factually wrong (e.g. "warm" no longer describes the cooler industrial values) — a comment-text edit, not a code-behavior change, purely to avoid a stale/misleading comment.
  </action>
  <acceptance_criteria>
    - All 44 `--color-*` declarations inside `@theme { }` carry the exact new hex value from the table above.
    - `--font-heading`, `--font-body`, `--font-mono`, the `@custom-variant dark` line, and the `body`/`h1,h2,h3` rules are byte-identical to before this edit.
    - No new `--color-*` token family (a 7th name beyond canvas/ink/accent/success/warning/danger) exists in the file.
    - Declaration count for each family is unchanged: ink=10, accent=10, success=8, warning=8, danger=7, canvas=1 (44 total).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; F=app/src/index.css; for kv in "--color-canvas: #f2f3f4" "--color-ink-50: #f1f2f3" "--color-ink-100: #e1e3e6" "--color-ink-200: #c7cbd0" "--color-ink-300: #a2a7af" "--color-ink-400: #666c74" "--color-ink-500: #4f555d" "--color-ink-600: #3a3f45" "--color-ink-700: #2a2e33" "--color-ink-800: #1a1c20" "--color-ink-900: #101215" "--color-accent-50: #fff4e6" "--color-accent-100: #ffe0b8" "--color-accent-200: #ffc27a" "--color-accent-300: #ff9f40" "--color-accent-400: #f97e1c" "--color-accent-500: #f2680a" "--color-accent-600: #c94e0a" "--color-accent-700: #a8420a" "--color-accent-800: #7c3009" "--color-accent-900: #5c2308" "--color-success-50: #eaf6ec" "--color-success-100: #cfead4" "--color-success-200: #a3d7ac" "--color-success-500: #2f8f3d" "--color-success-600: #23732f" "--color-success-700: #1c5c26" "--color-success-800: #164a1e" "--color-success-900: #0f3516" "--color-warning-50: #fdf3dc" "--color-warning-100: #f8e2a8" "--color-warning-200: #f0cd6e" "--color-warning-400: #e0ab28" "--color-warning-500: #d99a12" "--color-warning-600: #9c690d" "--color-warning-700: #8a630c" "--color-warning-800: #6b4b12" "--color-danger-50: #fbe9e8" "--color-danger-100: #f3c6c4" "--color-danger-200: #e59e9b" "--color-danger-400: #cc5955" "--color-danger-500: #b3312c" "--color-danger-600: #942720" "--color-danger-700: #741e19"; do grep -qF "$kv" "$F" || { echo "MISSING: $kv"; exit 1; }; done; N=$(grep -cE "^\s*--color-(canvas|ink-|accent-|success-|warning-|danger-)" "$F"); [ "$N" = "44" ] || { echo "EXPECTED 44 declarations, got $N"; exit 1; }; echo "ALL 44 TOKEN VALUES PRESENT, COUNT OK"'</automated>
  </verify>
  <done>index.css's @theme block contains all 44 new industrial hex values under the unchanged token names, with fonts/dark-variant/body rules untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix shared.jsx's off-token orange/purple badge-color entries</name>
  <files>app/src/shared.jsx</files>
  <read_first>
    app/src/shared.jsx (current file, lines 6-48 — STATUS_COLOR, URGENCY_COLOR, TYPE_COLOR, REFERRAL_STATUS_COLOR, STAGE_COLOR); 01-UI-SPEC.md's "Open Questions Resolved" #1 table (lines 166-176) and "Fixes beyond the reskin" #2/#3 (lines 110-116) — the exact locked remap, checker-approved; 01-PATTERNS.md's "app/src/shared.jsx (constants, transform)" section (lines 144-176).
  </read_first>
  <action>
    Replace exactly these 8 map-entry values in shared.jsx, leaving every other entry in these 5 maps (STATUS_COLOR's Warm/Cooling/Cold/Closed, TYPE_COLOR's Email/Call/Other, REFERRAL_STATUS_COLOR's Asked/Confirmed/Declined, STAGE_COLOR's Wishlist/Applied/Phone Screen/Offer/Accepted/Rejected) byte-identical since they are already token-driven and pick up the new hex automatically via Task 1's swap:

    | Map | Entry | Old value | New value |
    |---|---|---|---|
    | STATUS_COLOR | Champion (the `⭐ Champion` key) | `bg-orange-100 text-orange-800` | `bg-accent-100 text-accent-800` |
    | URGENCY_COLOR | MED | `bg-warning-400 text-white` | `bg-warning-600 text-white` |
    | URGENCY_COLOR | LOW | `bg-ink-100 text-ink-400` | `bg-ink-100 text-ink-500` |
    | TYPE_COLOR | LinkedIn | `bg-purple-100 text-purple-700` | `bg-warning-800 text-warning-100` |
    | TYPE_COLOR | Meeting | `bg-orange-100 text-orange-700` | `bg-ink-200 text-ink-700` |
    | REFERRAL_STATUS_COLOR | Not Asked | `bg-ink-100 text-ink-400` | `bg-ink-100 text-ink-500` |
    | STAGE_COLOR | Technical | `bg-orange-100 text-orange-800` | `bg-warning-200 text-warning-800` |
    | STAGE_COLOR | Onsite | `bg-purple-100 text-purple-800` | `bg-warning-600 text-white` |

    None of the other 3 files in this phase's scope (index.css, the `ui/` primitives) contain any stock-Tailwind color literal — this is the only file where the off-token fix applies. Do not touch `STAGE_COLOR.Accepted`'s existing `font-semibold` modifier or any other structural aspect of these 5 exported constants.
  </action>
  <!-- planner-discipline-allow: orange- -->
  <!-- planner-discipline-allow: purple- -->
  <acceptance_criteria>
    - `shared.jsx` contains zero occurrences of the stock-Tailwind substrings that the 5 color-map objects previously used for these two hues (i.e. no lingering non-token literal remains in `STATUS_COLOR`/`URGENCY_COLOR`/`TYPE_COLOR`/`REFERRAL_STATUS_COLOR`/`STAGE_COLOR`).
    - `bg-warning-600 text-white` appears exactly twice (URGENCY_COLOR.MED and STAGE_COLOR.Onsite share this exact new value).
    - `bg-ink-100 text-ink-500` appears exactly twice (URGENCY_COLOR.LOW and REFERRAL_STATUS_COLOR['Not Asked'] share this exact new value).
    - `bg-accent-100 text-accent-800`, `bg-warning-800 text-warning-100`, `bg-ink-200 text-ink-700`, and `bg-warning-200 text-warning-800` each appear exactly once.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=app/src/shared.jsx; C=$(grep -Ec "orange-|purple-" "$F" || true); [ "$C" = "0" ] || { echo "FAIL: $C stock-Tailwind orange-/purple- literals remain"; exit 1; }; for kvn in "bg-accent-100 text-accent-800:1" "bg-warning-600 text-white:2" "bg-ink-100 text-ink-500:2" "bg-warning-800 text-warning-100:1" "bg-ink-200 text-ink-700:1" "bg-warning-200 text-warning-800:1"; do kv="${kvn%:*}"; n="${kvn#*:}"; got=$(grep -cF "$kv" "$F"); [ "$got" = "$n" ] || { echo "FAIL: expected $n occurrences of [$kv], got $got"; exit 1; }; done; echo "shared.jsx off-token fix VERIFIED"'</automated>
  </verify>
  <done>shared.jsx's 5 badge-color maps contain zero stock-Tailwind orange-/purple- literals; all 8 fixed entries resolve through the ink/accent/warning token scale.</done>
</task>

<task type="auto">
  <name>Task 3: Re-validate every touched color pair against the values actually landed in index.css</name>
  <files>app/src/index.css</files>
  <read_first>
    01-UI-SPEC.md's "Validation Evidence" table (lines 118-160) — the 19 already-validated WCAG pairs plus the categorical six-check note; 01-RESEARCH.md's "Pattern 2" (lines 157-162) confirming `contrast(a,b)` — not the six-check categorical validator — is the correct tool for these single text/background pairs; 01-VALIDATION.md's "Sampling Rate" (line 34): "Contrast validator run on the final chosen token values must be zero-FAIL... before /gsd-verify-work."
  </read_first>
  <action>
    Invoke the `dataviz` skill via the Skill tool with a lookup-only request (no palette review needed) to obtain its "Base directory for this skill" path for the current session — call this `DATAVIZ_DIR`. Then run a Node ESM one-liner that dynamically imports `contrast` from `DATAVIZ_DIR/scripts/validate_palette.js` and checks it against the 19 pairs from UI-SPEC's Validation Evidence table, using the exact hex values now present in `app/src/index.css` (Task 1's output) rather than re-typing them from memory: [white,accent-600]=4.59, [white,accent-700]=6.09, [white,danger-600]=8.17, [ink-700,white]=13.66, [ink-900,canvas]=16.89, [success-800,success-100]=8.06, [warning-800,warning-100]=6.22, [danger-700,danger-100]=7.05, [ink-500,ink-100]=5.85, [accent-800,accent-100]=7.27, [accent-700,accent-100]=4.82, [white,warning-600]=4.73, [success-900,success-200]=8.35, [white,danger-500]=6.19, [white,ink-900]=18.76, [ink-50,ink-900]=16.74. For each pair, assert the freshly computed ratio is greater than or equal to the documented value minus a 0.05 rounding tolerance. The categorical six-check validator (for `STATUS_CHART_COLORS`/`charts/theme.js`) is explicitly out of scope this task — that file's edit is deferred to Phase 7 per UI-SPEC's "Open Questions Resolved #2" and RESEARCH's Pitfall 3; UI-SPEC already ran the six-check for informational parity and recorded a documented, accepted WARN (contrast-vs-surface on 2 slots, same as the pre-existing palette) plus an intentional chroma-floor exception for the desaturated "Closed" slot — no re-run needed here since that file isn't touched this phase.
  </action>
  <acceptance_criteria>
    - Every one of the 16 distinct pairs listed above (some UI-SPEC rows share an identical pair, e.g. Button primary/ChipToggleGroup selected both resolve to [white, accent-600]) computes a contrast ratio at or above its documented value (within 0.05 rounding tolerance) using the hex values actually present in index.css after Task 1.
    - Zero pairs FAIL the applicable WCAG floor (4.5:1 normal text, or the lower documented ratio only for rows UI-SPEC explicitly marks as large-text/UI-component context).
    - The six-check categorical validator is not invoked against `STATUS_CHART_COLORS` this task (confirmed out of scope, deferred to Phase 7).
  </acceptance_criteria>
  <verify>
    <automated>Invoke Skill(dataviz) to resolve DATAVIZ_DIR for this session, then run: node --input-type=module -e "import('file://'+process.env.DATAVIZ_DIR+'/scripts/validate_palette.js').then(async m=>{const pairs=[['#ffffff','#c94e0a',4.59],['#ffffff','#a8420a',6.09],['#ffffff','#942720',8.17],['#2a2e33','#ffffff',13.66],['#101215','#f2f3f4',16.89],['#164a1e','#cfead4',8.06],['#6b4b12','#f8e2a8',6.22],['#741e19','#f3c6c4',7.05],['#4f555d','#e1e3e6',5.85],['#7c3009','#ffe0b8',7.27],['#a8420a','#ffe0b8',4.82],['#ffffff','#9c690d',4.73],['#0f3516','#a3d7ac',8.35],['#ffffff','#b3312c',6.19],['#ffffff','#101215',18.76],['#f1f2f3','#101215',16.74]];let fail=false;for(const[a,b,exp] of pairs){const r=m.contrast(a,b);if(r<exp-0.05){console.error('FAIL',a,b,'got',r,'expected>=',exp);fail=true}else{console.log('PASS',a,b,r.toFixed(2))}}process.exit(fail?1:0)})" DATAVIZ_DIR="$DATAVIZ_DIR" (run with DATAVIZ_DIR env var set to the path resolved from the Skill invocation)</automated>
  </verify>
  <done>All 16 distinct WCAG text/background pairs touched by this phase are re-confirmed passing against the live index.css values via the dataviz skill's contrast() function, with zero unmitigated FAIL — VIS-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | This plan edits CSS custom-property values (`app/src/index.css`) and JavaScript object-literal color-class strings (`app/src/shared.jsx`). No network request, no user input, no authentication/session/access-control code, no data persistence. |

## ASVS Applicability

| ASVS Category | Applies | Note |
|---|---|---|
| V2 Authentication | No | Not touched |
| V3 Session Management | No | Not touched |
| V4 Access Control | No | Not touched — no RLS/route-guard code in scope |
| V5 Input Validation | No | No new input surface introduced by a CSS-value/constant-map edit |
| V6 Cryptography | No | Not touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | (none identified) | app/src/index.css @theme block | low | accept | Static, build-time-compiled CSS custom properties with no runtime input surface; confirmed negligible by 01-RESEARCH.md's dedicated Security Domain section. |
| T-01-02 | (none identified) | app/src/shared.jsx color-map constants | low | accept | Pure string-literal object maps consumed by a presentational Badge component; no user input flows into these values. |

</threat_model>

<verification>
Run all 3 tasks' `<verify><automated>` commands in order (Task 1 -> Task 2 -> Task 3) after this plan's edits land. Task 3 must show zero FAIL. Confirm `app/src/index.css` and `app/src/shared.jsx` are the only 2 files in this plan's diff.
</verification>

<success_criteria>
- index.css's @theme block carries all 44 new industrial hex values under unchanged token names.
- shared.jsx contains zero stock-Tailwind orange-/purple- literals across its 5 color-map exports.
- Every touched WCAG text/background pair clears its documented ratio, confirmed live against the values in index.css (not just as recorded in UI-SPEC.md).
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-industrial-design-tokens-primitives/01-01-SUMMARY.md` when done
</output>
