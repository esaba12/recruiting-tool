transcripts/*
jds/*
!transcripts/.gitkeep
!jds/.gitkeep

# credentials & personal context — never commit
context.md
CLAUDE.local.md
.env
.env.local
.env*.local
*.local.md

# clasp — .clasp.json holds a Script ID tied to a personal Google account/Drive
.clasp.json
.clasprc.json

# local one-off / verification scripts
app/.verify-*

node_modules/
.worktrees/
.claude/worktrees/
.vercel

# graphify knowledge-graph output — regenerate with `graphify . --no-viz --code-only`
graphify-out/
.playwright-mcp/

# Video-production working files (HyperFrames output, briefs) — kept locally, not part of the project.
brag-output/
video-brief.md
